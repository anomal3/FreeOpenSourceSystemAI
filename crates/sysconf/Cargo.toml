[package]
name = "sysconf"
description = "Reading and editing the key=value files in /etc, shared by the kernel and the tools."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[dependencies]

[lints]
workspace = true
