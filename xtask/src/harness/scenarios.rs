//! Сценарии стенда: что именно проверяется прогоном.
//!
//! # Правило проверок
//!
//! Утверждения делаются **только по серийной линии**. Снимок экрана
//! доказательством не является: он показывает последний нарисованный кадр, а не
//! текущее состояние, и после падения отстаёт на два-три экрана. Снимки здесь
//! отвечают на вопрос «как это выглядело», журнал — на вопрос «что произошло».
//!
//! # Почему сценарии — код, а не файлы данных
//!
//! Потому что их пишет тот же человек, который меняет ядро, и в тот же момент.
//! Формат файла пришлось бы разбирать и проверять, а его ошибки вылезали бы во
//! время прогона; здесь опечатку в имени клавиши ловит компилятор.

use super::aim::{self, Aim};
use crate::arch::Arch;
use crate::qemu::DiskBus;

/// Носитель, с которого грузится сценарий.
#[derive(Clone, Copy, PartialEq, Eq)]
pub enum Target {
    /// Каталог хоста, выданный за FAT-раздел: самый быстрый цикл, но разметки
    /// не существует, и проверять на ней нечего, кроме самой системы.
    Live,
    /// Настоящий образ диска: наша разметка, наша файловая система.
    Image,
    /// Установочный носитель плюс чистый целевой диск.
    Installer,
    /// Диск, на который ставил установщик.
    Installed,
    /// Загрузочный ISO, подключённый приводом — ровно так, как его подключит
    /// человек в VirtualBox.
    Iso,
    /// Система грузится с каталога хоста, а рядом подключён диск, на который
    /// ставил установщик.
    ///
    /// Нужно там, где проверяется **драйвер диска**, а не загрузка с него.
    /// Прошивка `ArmVirtQemu` не умеет SATA вовсе (`map: No mapping found`), так
    /// что подключить установленный диск по AHCI и ждать, что она с него
    /// стартует, можно только на x86-64 — а драйвер в ядре один на обе
    /// архитектуры, и проверять его надо на обеих. Здесь загрузка идёт тем
    /// путём, который прошивка умеет всегда, а проверяемый диск оказывается
    /// вторым носителем: ядро обязано найти корень **на нём**, перебрав оба.
    LiveAndDisk,
    /// Система грузится с каталога хоста, а рядом подключён диск с томом
    /// **btrfs**, созданным `mkfs.btrfs`.
    ///
    /// Второй носитель здесь не «ещё один диск», а весь смысл проверки: том
    /// сделан чужой программой, и система обязана прочитать его, ничего о нём
    /// не зная заранее. Корня FreeOS на этом диске нет — ядро оставляет корнем
    /// initrd и монтирует только раздел данных.
    LiveAndBtrfs,
}

impl Target {
    pub const fn needs_installer(self) -> bool {
        matches!(self, Target::Installer)
    }

    /// Работает ли сценарий с тем самым диском, на который ставил установщик.
    ///
    /// По этому признаку параллельный прогон собирает сценарии в неделимую
    /// цепочку. Диск здесь — общая память между сценариями, причём в обе
    /// стороны: `install` его пишет, `installed` и `write` читают, `persist`
    /// проверяет записанное **отдельной загрузкой**, `rollback` и `update`
    /// меняют активный слот, а `install4k` пересоздаёт его целиком с сектором
    /// в 4096 байт. Разложить такое по воркерам нельзя: у каждого воркера свой
    /// диск, и сценарий, уехавший к соседу, читал бы либо чужую установку,
    /// либо пустой файл.
    pub const fn shares_target_disk(self) -> bool {
        matches!(
            self,
            Target::Installer | Target::Installed | Target::LiveAndDisk
        )
    }

    pub const fn title(self) -> &'static str {
        match self {
            Target::Live => "каталог хоста через VVFAT",
            Target::Image => "образ диска GPT + FAT32",
            Target::Installer => "установочный носитель и чистый диск",
            Target::Installed => "диск после установки",
            Target::Iso => "загрузочный ISO в приводе",
            Target::LiveAndDisk => "каталог хоста плюс диск после установки",
            Target::LiveAndBtrfs => "каталог хоста плюс диск с томом btrfs",
        }
    }
}

/// Шаг сценария.
pub enum Step {
    /// Дождаться подстроки в серийной линии (мс).
    ///
    /// Поиск идёт от места, где закончилось прошлое ожидание: приглашение
    /// `freeos> ` встречается в выводе десятки раз, и поиск с начала считал бы
    /// выполненным то, что ещё не началось.
    Await(&'static str, u64),
    /// Дождаться подстроки и запомнить число, стоящее сразу за ней.
    ///
    /// Запомненное подставляется вместо `{}` в последующих [`Step::Await`],
    /// [`Step::Expect`], [`Step::Absent`] и [`Step::Line`].
    ///
    /// Существует потому, что номер задачи — не свойство сценария, а свойство
    /// системы: одна служебная задача, заведённая в ядре, сдвигает нумерацию, и
    /// сценарий с числом в строке начинает проверять не то, что собирался.
    Capture(&'static str, u64),
    /// То же, но во **вторую** ячейку: подставляется вместо `{2}`.
    ///
    /// Одной ячейки хватало ровно до того дня, когда прогон пошёл в несколько
    /// потоков. Сценарий одновременности запускает две копии программы и ждёт
    /// конца каждой; первая ищется по своему номеру где угодно в выводе, а
    /// вторая ждалась «следующей строкой с `: done`» — то есть в предположении,
    /// что копии заканчиваются в том порядке, в котором запущены. Под нагрузкой
    /// вторая копия досчитала раньше первой, её строка оказалась позади
    /// курсора, и сценарий упал, доказывая обратное тому, что проверяет.
    Capture2(&'static str, u64),
    /// Дождаться подстроки **где угодно** в выводе, в том числе позади курсора.
    ///
    /// Нужно там, где сценарий ждёт события от двух задач сразу: порядок, в
    /// котором они допишут свои строки, ему не подчиняется, а обычное ожидание
    /// ищет только вперёд и теряет то, что уже напечатано. Подставлять сюда
    /// стоит строку с номером задачи из [`Step::Capture`] — иначе одно и то же
    /// совпадение засчитается дважды.
    AwaitAny(&'static str, u64),
    /// Дождаться числа за подстрокой и сверить его с часами **хоста**.
    ///
    /// Число — секунды эпохи Unix; допуск задаётся в секундах. Существует
    /// потому, что «система напечатала дату» и «система знает, который час» —
    /// разные утверждения: напечатать правдоподобную дату можно и не зная
    /// ничего, а совпасть с часами хоста с точностью до минуты, не прочитав их
    /// у прошивки, нельзя.
    ///
    /// Заодно это единственная проверка часового пояса, которая ловит ошибку в
    /// знаке: сдвиг на целый час здесь не проходит.
    Clock(&'static str, u64, u64),
    /// Дождаться числа за подстрокой и проверить, что оно не больше предела.
    ///
    /// Отличается от [`Step::Clock`] тем, что не спрашивает часы хоста, а
    /// значит проверяет систему саму по себе: «файл помечен тем временем,
    /// которое система считает текущим» — утверждение о её внутренней
    /// согласованности, и внешние часы для него не нужны.
    AtMost(&'static str, u64, u64),
    /// Дождаться числа за подстрокой и проверить, что оно не меньше предела.
    ///
    /// Нужно там, где проверяется, что что-то вообще происходит: счётчик
    /// прерываний, оставшийся нулём, — это работающая система, которая делает
    /// то же самое другим способом, и никакая проверка «работает ли» её не
    /// отличит.
    AtLeast(&'static str, u64, u64),
    /// Проверить, что подстрока встречалась хоть раз за прогон.
    Expect(&'static str),
    /// Проверить, что подстроки не было ни разу.
    Absent(&'static str),
    /// Нажать клавишу на **настоящей** клавиатуре (имя в терминах QEMU).
    Key(&'static str),
    /// Нажать клавишу несколько раз.
    Repeat(&'static str, u32),
    /// Набрать текст на клавиатуре.
    Type(&'static str),
    /// Отправить строку в серийную линию, завершив переводом строки.
    Line(&'static str),
    /// Отправить в серийную линию байты как есть.
    Raw(&'static [u8]),
    /// Сдвинуть мышь на приращение. Абсолютных координат у мыши нет.
    Move(i32, i32),
    /// Навести мышь на то, что гость сам описал в журнале.
    Aim(aim::Aim),
    /// Нажать левую кнопку и отпустить её там же.
    Click,
    /// Щелчок правой кнопкой — меню рабочего стола.
    RightClick,
    /// Нажать левую кнопку и **не** отпускать — начало перетаскивания.
    Press,
    /// Навести на первую точку, нажать и провести через остальные, **не**
    /// отпуская (отпускает `Release`). Жест пальцем по экранной клавиатуре.
    ///
    /// Отдельным шагом, а не `Press` и цепочкой `Aim`, потому что `Aim` перед
    /// движением ждёт конца строки в линии до секунды (`finish_line`), а
    /// приглашение `freeos> ` перевода строки не несёт. Палец между буквами
    /// «стоял» до секунды, и клавиатура честно видела долгое нажатие (400 мс):
    /// сценарий `mobile` падал на жесте примерно раз в несколько прогонов.
    Stroke(&'static [(i32, i32)]),
    /// Отпустить все кнопки.
    Release,
    /// Подключить устройство к работающей машине (аргументы `device_add`).
    Plug(&'static str),
    /// Выдернуть устройство по его идентификатору.
    Unplug(&'static str),
    /// Подождать (мс).
    Wait(u64),
    /// Снять экран.
    Shot(&'static str),
    /// Проверить размер кадра, который показывает устройство, — по монитору
    /// QEMU, а не по словам гостя (фаза С6a).
    Screen(u32, u32),
    /// Сбросить машину, ничего не спрашивая у гостя (`system_reset` монитора).
    ///
    /// Это и есть «выдернули шнур»: система не получает ни уведомления, ни
    /// возможности что-нибудь дописать. Единственный способ проверить, что
    /// брошенный том действительно объявляется грязным при следующей загрузке —
    /// корректное выключение проверяет ровно противоположное утверждение.
    Reset,
    /// Нажать кнопку питания (`system_powerdown` монитора).
    ///
    /// Не то же самое, что команда `shutdown`: команду выполняет оболочка, а
    /// кнопку — обработчик прерывания и служебная задача, и путь от чипсета до
    /// сброса тома на диск проверяется только так.
    PowerButton,
    /// Подключиться к гостю **с хоста** по проброшенному порту, отправить текст
    /// и убедиться, что он вернулся тем же.
    ///
    /// Это единственная проверка стенда, которая ничего не читает в серийной
    /// линии, и в этом весь смысл: сюда стучится обычный клиент TCP из обычной
    /// стандартной библиотеки — то есть чужая реализация протокола, ничего не
    /// знающая о том, как он написан у нас. Сойдётся окно, номера, контрольные
    /// суммы и закрытие — обмен состоится; ошибёмся в любом из них — соединение
    /// либо не установится, либо повиснет.
    TcpEcho(&'static str, u64),
    /// То же, но большим потоком: сколько килобайт отправить туда и обратно.
    ///
    /// Отдельным шагом, потому что проверяет другое. Короткая строка проходит
    /// одним сегментом, и на ней не видно ни окна, ни продолжения потока;
    /// килобайты идут несколькими сегментами, требуют подтверждений по ходу и
    /// упираются в буфер приёма на той стороне.
    TcpBulk(usize, u64),
    /// Позвать **настоящий** `ssh` с хоста и проверить, что он сказал.
    ///
    /// Самая честная проверка SSH, какая существует: клиент OpenSSH придирчив к
    /// деталям формата, и всё, в чём мы ошиблись, он назовёт вслух и оборвёт
    /// соединение. Никакой наш код в этой проверке не участвует.
    Ssh(SshRun),
    /// Позвать **настоящий** `sftp` или `scp` с хоста (фаза 38c).
    ///
    /// Как и [`Step::Ssh`], в проверке не участвует ни строчки нашего кода на
    /// стороне клиента: OpenSSH придирчив к формату SFTP и назовёт вслух всё,
    /// в чём мы ошиблись.
    Sftp(SftpRun),
    /// Дождаться, пока **процесс QEMU завершится сам** (мс).
    ///
    /// Самая сильная проверка выключения из возможных: до этой фазы стенд
    /// всегда снимал QEMU принудительно, потому что гость не умел гаснуть. Если
    /// процесс ушёл сам — значит гость действительно выполнил команду ACPI (или
    /// PSCI), а не просто напечатал, что собирается.
    Exits(u64),
    /// Прочитать том btrfs на диске сценария **с хоста** — после
    /// [`Step::Exits`], когда гость погас и дописывать ему нечего.
    ///
    /// Доказывает то, чего не докажет ни одна строка гостя: записанное дошло до
    /// диска. Читает наш же крейт, поэтому это согласованность, а не формат, —
    /// подробности в [`super::datavol`].
    DataVolume(VolumeCheck),
}

/// Что обязано найтись на томе после сценария.
pub struct VolumeCheck {
    /// Пути внутри тома и содержимое файлов целиком.
    pub files: &'static [(&'static str, Content)],
    /// Чего на томе быть не должно.
    pub gone: &'static [&'static str],
}

/// Содержимое файла: текстом или повторённым байтом — три тысячи одинаковых
/// букв строкой в сценарии не прочёл бы никто.
pub enum Content {
    Text(&'static str),
    Repeat(u8, usize),
}

/// С каким ключом стенд стучится к гостю.
///
/// Ключи делает `ssh-keygen` на машине разработчика при первом прогоне, а
/// открытая половина `Authorized` попадает в `authorized_keys` гостя — см.
/// [`super::sshkeys`].
#[derive(Clone, Copy, PartialEq, Eq)]
pub enum Identity {
    /// Ключа нет вовсе: клиент дойдёт до отказа во входе и там остановится.
    None,
    /// Ключ, записанный в `authorized_keys` гостя.
    Authorized,
    /// Ключ, которого гость не знает.
    ///
    /// Без этой проверки все остальные ничего не доказывают: сервер, пускающий
    /// по любому ключу, проходит проверку «вошли с правильным ключом» ровно так
    /// же успешно, как правильный.
    Stranger,
}

/// Один запуск настоящего `ssh` с хоста.
///
/// Полями, а не позициями: их шесть, и строка вида `Ssh(&[...], "", "", &[], 60_000)`
/// читалась бы гаданием.
pub struct SshRun {
    pub identity: Identity,
    /// Команда для удалённой стороны. Пусто — сеанс без команды, то есть
    /// оболочка.
    pub command: &'static str,
    /// Что подать клиенту на вход. Построчный сеанс читает это как команды —
    /// так проверяется оболочка, которой некому набирать.
    pub stdin: &'static str,
    /// Что обязано найтись в выводе `ssh` (он пишет диагностику в поток ошибок,
    /// а вывод удалённой команды — в обычный; проверяются оба).
    pub expect: &'static [&'static str],
    /// Чего в выводе быть не должно.
    ///
    /// Проверка отказа состоит из двух половин, и вторая здесь: мало увидеть
    /// слово «отказано» — надо убедиться, что содержимого файла в ответе нет.
    pub absent: &'static [&'static str],
    pub timeout_ms: u64,
}

/// Что везти по SFTP и чем (фаза 38c).
pub enum Transfer {
    /// `sftp -b`: строки пакета. Локальные имена — в рабочем каталоге шага, где
    /// лежат файлы из [`SftpRun::stage`].
    Batch(&'static str),
    /// `scp <локальный> roman@...:<удалённый>`.
    ScpPut(&'static str, &'static str),
    /// `scp roman@...:<удалённый> <локальный>`.
    ScpGet(&'static str, &'static str),
}

/// Откуда взять локальный файл перед передачей.
pub enum Source {
    /// Столько псевдослучайных байт. Не узор: узор с периодом, кратным куску
    /// SFTP, совпал бы сам с собой при перепутанном порядке кусков, а
    /// последовательность xorshift за мегабайты не повторяется.
    Random(usize),
    /// Файл из репозитория — например готовая сборка .NET из `initrd/`.
    Repo(&'static str),
}

/// Один запуск настоящего `sftp` или `scp` с хоста.
///
/// Сверка байт идёт **на хосте**: файл уехал в гостя и вернулся, и совпасть с
/// оригиналом до последнего байта он может, только если сервер, окно канала и
/// TCP ни разу не ошиблись.
pub struct SftpRun {
    pub transfer: Transfer,
    /// Локальные файлы, которые надо положить в рабочий каталог до запуска.
    /// Файлы с тем же именем от прошлого шага не трогаются, если здесь их нет,
    /// — так один шаг кладёт, а следующий забирает и сверяет.
    pub stage: &'static [(&'static str, Source)],
    /// Удалить эти локальные файлы до запуска: скачанное прошлым прогоном
    /// сошлось бы с оригиналом и без всякой передачи.
    pub fresh: &'static [&'static str],
    /// Сверить два локальных файла побайтно после передачи.
    pub compare: Option<(&'static str, &'static str)>,
    /// Файл, по размеру которого считается скорость передачи.
    pub measure: Option<&'static str>,
    pub expect: &'static [&'static str],
    pub absent: &'static [&'static str],
    pub timeout_ms: u64,
}

pub struct Scenario {
    pub name: &'static str,
    pub about: &'static str,
    pub target: Target,
    pub steps: &'static [Step],
    /// Дополнительные аргументы QEMU.
    pub extra: &'static [&'static str],
    /// Ввод должен идти именно через USB.
    ///
    /// На x86-64 к машине подключены и PS/2, и USB, а `sendkey` доходит ровно
    /// до одной клавиатуры — QEMU выбирает PS/2. Без отключения i8042 сценарий
    /// «проверяем USB HID» молча проверял бы совсем другой драйвер.
    pub usb_only: bool,
    /// Вместо мыши к машине подключён планшет.
    ///
    /// Это другое устройство, а не та же мышь другими словами: планшет не
    /// объявляет boot-протокола и сообщает координаты вместо приращений. Ровно
    /// такой манипулятор VirtualBox предлагает по умолчанию, поэтому путь через
    /// разбор дескриптора отчётов проверяется здесь, а не на чужой машине.
    ///
    /// Заодно меняется способ управления: абсолютное событие посылается через
    /// QMP, потому что в HMP такой команды нет (см. [`super::qmp`]).
    pub tablet: bool,
    /// Устройства ввода подключены через OHCI, а не через xHCI.
    ///
    /// Это другой драйвер в ядре, а не другая настройка эмулятора. Сценарий
    /// существует потому, что у VirtualBox по умолчанию включён только OHCI:
    /// без него единственной проверкой драйвера была бы чужая машина, на
    /// которой нет журнала.
    pub ohci: bool,
    /// Клавиатура и указатель — на EHCI вместо xHCI. Появился вместе с драйвером:
    /// у ноутбука на Intel Cougar Point другого пути к клавиатуре нет.
    pub ehci: bool,
    /// Чем подключён диск сценария.
    ///
    /// Проверяется этим не эмулятор, а **другой драйвер в ядре**. Пока в
    /// FreeOS был один дисковый драйвер, вся цепочка «система находит свой
    /// корень» проверялась исключительно на virtio-blk — то есть на
    /// контроллере, которого нет ни в VirtualBox по умолчанию, ни в настоящем
    /// компьютере. Сценарий с другой шиной берёт тот же самый установленный
    /// диск и подключает его по SATA или по NVMe: образ не меняется ни на байт,
    /// меняется только то, чем ядро до него добирается.
    pub disk_bus: DiskBus,
    /// Подключить к машине сетевую карту в пользовательскую сеть QEMU.
    ///
    /// По умолчанию карты нет, и это не экономия: машина без сети — это ещё и
    /// проверка того, что система без карты поднимается молча и без жалоб. Сеть
    /// включается там, где её проверяют.
    pub network: bool,
    /// Подключить к машине сетевую карту как **Intel e1000**, а не virtio-net.
    ///
    /// Это другой драйвер в ядре, а не другая настройка эмулятора. Смысл тот
    /// же, что у [`Scenario::ohci`] и [`Scenario::ehci`]: virtio-net не
    /// существует вне виртуальной машины, и до этого сценария весь сетевой
    /// стек проверялся ровно на той карте, которой ни в одном настоящем
    /// компьютере нет.
    pub e1000: bool,
    /// Порт гостя, который стенд пробрасывает с хоста. Ноль — не пробрасывать.
    ///
    /// Нужен там, где проверяется **входящее** соединение. Через SLIRP иначе
    /// никак: сеть за трансляцией адресов, и снаружи внутрь не проходит ничего,
    /// о чём не попросили заранее.
    pub guest_port: u16,
    /// Поднять на хосте эхо-сервер, к которому подключается **гость**.
    ///
    /// Обратное направление к [`Scenario::guest_port`], и оно проверяет другую
    /// половину автомата: активное открытие, где `SYN` посылаем мы. Гость видит
    /// хост как `10.0.2.2` — так устроена пользовательская сеть QEMU, а номер
    /// порта пишется в команде как `{echo}`: у каждого воркера параллельного
    /// прогона своя четвёрка портов, и число, набранное здесь буквой, увело бы
    /// гостя к серверу соседнего прогона.
    pub host_echo: bool,
    /// Поднять на хосте **сервер обновлений** и подготовить гостю его адрес.
    ///
    /// Три вещи разом, и порознь они бессмысленны: собрать каталог репозитория
    /// (образы, индекс, подпись), раздавать его по HTTP на порту воркера и
    /// положить гостю `/etc/update.cfg`, где этот адрес назван. Последнее — на
    /// раздел состояния и до запуска, тем же приёмом, что [`Scenario::ssh_key`]:
    /// набирать адрес по серийной линии значило бы проверять, как гость
    /// принимает длинную строку, а не как он обновляется.
    ///
    /// Рядом с годным репозиторием кладётся подкаталог `x/` — тот же образ,
    /// подписанный чужим ключом, с правильно подписанным индексом. Без него
    /// проверка «обновление поставилось» ничего не доказывает.
    pub host_repo: bool,
    /// Архитектуры, на которых сценарий имеет смысл. Пусто — все.
    pub arches: &'static [Arch],
    /// Сценарий **намеренно** перезагружает машину.
    ///
    /// Обычно стенд запускает QEMU с `-no-reboot`, и это правильно: перезагрузка
    /// в сценарии, который её не заказывал, означает тройную ошибку, а без флага
    /// она превратилась бы в бесконечный цикл загрузок вместо внятного отказа.
    /// Ровно один сценарий проверяет саму перезагрузку — ему флаг мешает, потому
    /// что с ним QEMU завершается там, где машина должна подняться заново.
    pub reboots: bool,
    /// Положить в образ установленной системы контейнеры обновления.
    ///
    /// Стенд делает это **до** запуска и на хосте: открывает корневой раздел
    /// того же образа, на который ставил установщик, и кладёт в `/media` два
    /// файла — годное обновление и заведомо неисправное. Ровно то же сделал бы
    /// человек, принёсший обновление на флешке.
    ///
    /// Через установочный носитель это стоило бы двух лишних переносов
    /// шестидесяти мегабайт на каждой установке — ради файла, который нужен
    /// двум сценариям из тридцати.
    pub updates: bool,
    /// Положить в образ установленной системы `/media/big.dat` — файл, который
    /// заведомо не помещается под предел резидентных страниц отображения.
    ///
    /// Тем же приёмом и по той же причине, что [`Scenario::updates`]: файл
    /// пишется на корневой раздел уже установленного образа, до запуска и с
    /// хоста. Другого пути к нему нет вовсе — двести мебибайт нельзя ни набрать
    /// по серийной линии, ни положить в initrd, который целиком меньше.
    ///
    /// Содержимое **вычисляется**, а не берётся из репозитория, и это тоже не
    /// экономия места: узор зависит и от номера страницы, и от смещения внутри
    /// неё, поэтому ошибка отображения меняет ответ (см.
    /// [`crate::package::BIG_FILE_CHECKSUM`]). Файл в репозитории пришлось бы
    /// либо хранить, либо генерировать — то есть делать ровно то же самое, но
    /// в двух местах.
    ///
    /// Файл переживает прогон вместе с диском: во второй раз он не строится, а
    /// опознаётся на месте — иначе цепочка платила бы за него десятками секунд
    /// на каждом прогоне.
    pub big_file: bool,
    /// Положить открытый ключ стенда в `authorized_keys` гостя.
    ///
    /// Тем же приёмом и по той же причине, что [`Scenario::updates`]: файл
    /// пишется на **раздел состояния** уже установленного образа, а не в
    /// initrd. Ключ, попавший в initrd, уехал бы в выпущенный ISO — и всякий,
    /// кто его скачал, получил бы систему, пускающую владельца ключа из
    /// `build/test/`. Подробности — в [`super::sshkeys`].
    pub ssh_key: bool,
    /// Сколько памяти дать машине (аргумент `-m` QEMU). Пусто — оставить
    /// умолчание стенда, то есть 512 МиБ.
    ///
    /// Просят больше **не ради скорости**. Крупная страница — это не «две тысячи
    /// обычных подряд по номеру», а непрерывный и выровненный на два мебибайта
    /// отрезок **физической** памяти; ядро ищет его в пуле кадров, и на машине с
    /// 512 МиБ, из которых своё уже взяли ядро, initrd и композитор, такого
    /// отрезка может не найтись ни одного.
    ///
    /// Плохо здесь не то, что не найдётся, а то, чем это кончится. Отказа не
    /// будет: ядру велено разложить просьбу обычными страницами по 4 КиБ и
    /// вернуть тот же адрес — честное поведение, названное в договоре у
    /// `MAP_HUGE`. Значит, сценарий про крупные страницы прошёл бы целиком, ни
    /// одной крупной страницы не увидев, и молча проверил бы ровно то же, что
    /// проверяет сценарий `mmap`. Проверка, которая ничего не проверяет и не
    /// говорит об этом вслух, хуже упавшей.
    ///
    /// Поэтому память называется там, где от неё зависит **смысл** проверки, а
    /// не везде подряд: лишний гигабайт — это лишний гигабайт у каждого воркера
    /// параллельного прогона, а их на машине столько, сколько ядер.
    pub memory: &'static str,
}

impl Scenario {
    pub fn runs_on(&self, arch: Arch) -> bool {
        self.arches.is_empty() || self.arches.contains(&arch)
    }

    /// Аргументы QEMU, зависящие от архитектуры.
    pub fn qemu_args(&self, arch: Arch) -> &'static [&'static str] {
        match (self.usb_only, arch) {
            // Опции машины накапливаются: `-machine q35` уже задан, здесь
            // добавляется свойство. На `virt` PS/2 не существует вовсе.
            (true, Arch::X86_64) => &["-machine", "i8042=off"],
            _ => &[],
        }
    }
}

/// Сколько ждать первых слов прошивки.
///
/// Общий предел на обе архитектуры, и он велик не зря: edk2 для `virt`
/// добирается до приложения заметно дольше OVMF, а в release-сборке к этому
/// добавляется время на разметку носителя.
const BOOT: u64 = 120_000;

/// Экран, в котором идёт сценарий, если не тот, что даёт прошивка.
///
/// Функцией, а не полем [`Scenario`]: поле пришлось бы вписать в каждый из
/// восьми десятков сценариев ради одного.
#[must_use]
pub fn screen_for(name: &str) -> Option<&'static str> {
    match name {
        // Экран телефона: форма машины и масштаб стола считаются от него.
        "mobile" => Some("720x1600"),
        _ => None,
    }
}

/// Шаги сценариев о нескольких процессорах — общие для GICv2 и GICv3.
///
/// # Что именно доказывает одновременность
///
/// Не время. Две программы, исполнившиеся за то же время, что одна, — довод
/// слабый: под эмулятором время зависит от загрузки хоста, и стенд в три потока
/// сделал бы из него лотерею. Довод здесь — строка `tasks`: ядро печатает, что
/// исполняет **каждый** процессор в этот миг, и если обе программы названы
/// исполняющимися, они исполняются на разных процессорах — на одном двум
/// задачам сразу быть негде. Оболочка, печатающая список, занимает третий.
///
/// Рядом — счётчики переключений на каждом процессоре (строка `cpu 3`), и
/// строка самой программы о том, на каких процессорах она побывала.
const SMP_STEPS: &[Step] = &[
    Step::Await("smp         : 4 of 4 processors online", BOOT),
    Step::Await("freeos> ", BOOT),
    Step::Line("run -b /bin/spin 6000"),
    Step::Capture("started as #", 15_000),
    Step::Line("run -b /bin/spin 6000"),
    Step::Capture2("started as #", 15_000),
    // Программе нужно время дойти до третьего кольца: загрузка образа — работа
    // ядра, и в ней задача тоже «исполняется», но проверять хочется жгущий цикл.
    Step::Wait(2_000),
    Step::Line("tasks"),
    Step::AwaitAny(": running #{} (program)", 15_000),
    Step::AwaitAny(": running #{2} (program)", 15_000),
    // Где угодно в выводе, а не «после»: программа, найденная шагом выше, может
    // исполняться как раз на третьем процессоре, и тогда её строка **и есть**
    // строка `cpu 3` — курсор уже за ней. Так упал первый прогон на AArch64.
    Step::AwaitAny("cpu 3      :", 15_000),
    Step::AwaitAny("spin {}: burned", 60_000),
    Step::AwaitAny("spin {2}: burned", 60_000),
    Step::Line("echo still-alive"),
    Step::Await("  still-alive", 15_000),
    Step::Line("exit"),
    Step::Await("finishing the session", 15_000),
    // Выход из сеанса останавливает машину, и остальные процессоры обязаны
    // получить об этом просьбу, а не продолжать служебные задачи в пустоте.
    Step::Await("other processor(s) asked to stop", 30_000),
    Step::Absent("KERNEL PANIC"),
    Step::Absent("CPU FAULT"),
    Step::Absent("STACK OVERFLOW"),
    Step::Absent("did not report"),
];

pub const ALL: &[Scenario] = &[
    Scenario {
        name: "boot",
        about: "Ядро грузится, композитор поднимается, оболочка отвечает на команды.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await(crate::version::KERNEL_BANNER, BOOT),
            Step::Await("FreeOS shell.", 60_000),
            Step::Await("freeos> ", 10_000),
            Step::Line("uptime"),
            Step::Await("timer ticks", 10_000),
            Step::Line("mem"),
            Step::Await("frames", 10_000),
            // Композитор обязан быть поднят: строка «windows,» бывает только у
            // работающего окна, а не у оболочки в серийной консоли.
            Step::Line("ui"),
            Step::Await("windows,", 10_000),
            Step::Line("echo harness-ok"),
            Step::Await("harness-ok", 10_000),
            Step::Shot("desktop"),
            Step::Line("exit"),
            Step::Await("finishing the session", 10_000),
            // Паника в любой момент прогона обесценивает всё остальное.
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "keyboard",
        about: "Клавиши доходят до ядра через xHCI и USB HID, а не через PS/2.",
        target: Target::Live,
        usb_only: true,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Ядро перечисляет только поднявшиеся источники. PS/2 на этой машине
            // выключен, значит клавиатура здесь может быть только USB.
            Step::Expect("Input: keyboard"),
            // Клавиатура тоже читается по дескриптору отчётов: модификаторы и
            // список клавиш найдены в нём, а не взяты из boot-протокола. Это
            // ровно тот разбор, которым будет читаться клавиатура на железе,
            // где boot-протокола нет.
            Step::Expect("keyboard, modifiers, 6-key array"),
            Step::Type("echo usb-ok"),
            Step::Key("ret"),
            Step::Await("usb-ok", 15_000),
            // И то, ради чего фаза: клавиша доехала **прерыванием**. Нажатия
            // выше доказывают, что клавиатура работает, но точно так же она
            // работала и на опросе — отличает одно от другого только этот
            // счётчик. Ноль здесь означает, что MSI-X настроены, а события
            // по-прежнему забирает таймер.
            Step::Line("usb"),
            Step::AtLeast("  irqs     ", 1, 15_000),
            // И обратная сторона того же: драйвер просыпается на события, а не
            // по часам. К этому месту сценария система живёт секунд двадцать —
            // при опросе с периодом 10 мс здесь стояло бы за две тысячи, и
            // предел в триста отличает одно от другого без всякой
            // двусмысленности.
            Step::AtMost("  wakeups  ", 300, 15_000),
            // Alt+Tab поднимает нижнее окно наверх и **передаёт ему ввод**: с
            // этой фазы фокус — это фокус, а не только порядок по глубине.
            // Поэтому второй Alt+Tab обязателен: без него «exit» ушёл бы в окно
            // состояния, а не в оболочку. (До фазы N7h окна переключал голый
            // Tab; теперь он достаётся окну.)
            Step::Key("alt-tab"),
            Step::Wait(500),
            Step::Shot("raised"),
            Step::Key("alt-tab"),
            Step::Wait(500),
            Step::Type("exit"),
            Step::Key("ret"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "ohci",
        about: "Клавиатура и указатель работают через OHCI — контроллер, который VirtualBox даёт по умолчанию.",
        target: Target::Live,
        usb_only: true,
        tablet: false,
        ohci: true,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // xHCI на этой машине нет вовсе, и PS/2 выключён: всё, что ниже,
            // может работать только через OHCI. Без этой строки сценарий
            // проверял бы «ввод работает», а не «работает второй драйвер».
            Step::Expect("xhci        : unavailable"),
            Step::Expect("ohci        : root hub with"),
            // Оба устройства подняты и опознаны каждое своим: «клавиатура
            // разобрана указателем» — самый частый отказ этого пути, и он
            // выглядит как работающий ввод.
            Step::Expect("port(s) brought up, 1 keyboard(s), 1 pointer(s)"),
            Step::Expect("Input: keyboard"),
            // Нажатия идут в USB-клавиатуру (i8042 выключён) и доезжают до
            // оболочки — то есть отчёты действительно разбираются, а не просто
            // приходят.
            Step::Type("echo ohci-ok"),
            Step::Key("ret"),
            Step::Await("ohci-ok", 15_000),
            Step::Line("usb"),
            Step::AtLeast("  reports  ", 1, 15_000),
            Step::Type("exit"),
            Step::Key("ret"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "ehci",
        about: "Клавиатура и указатель работают через EHCI — контроллер USB 2.0, за которым у Intel висит встроенный хаб.",
        target: Target::Live,
        usb_only: true,
        tablet: false,
        ohci: false,
        ehci: true,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // xHCI и OHCI на этой машине нет, PS/2 выключен: всё, что ниже,
            // может работать только через EHCI.
            Step::Expect("xhci        : unavailable"),
            Step::Expect("ehci        : root port 1: high speed"),
            // Оба устройства опознаны каждое своим — самый частый отказ пути,
            // «клавиатура разобрана указателем», выглядит как работающий ввод.
            Step::Expect("0 hub(s), 1 keyboard(s), 1 pointer(s)"),
            Step::Expect("Input: keyboard"),
            // Нажатия идут в USB-клавиатуру и доезжают до оболочки: отчёты не
            // только приходят по периодическому списку, но и разбираются.
            Step::Type("echo ehci-ok"),
            Step::Key("ret"),
            Step::Await("ehci-ok", 15_000),
            Step::Line("usb"),
            Step::AtLeast("  reports  ", 1, 15_000),
            // Журнал ядра с клавиатуры: строка о контроллере EHCI, напечатанная
            // при загрузке, достаётся из кольца (`dmesg`), а не из линии.
            Step::Type("dmesg 400"),
            Step::Key("ret"),
            Step::Await("keyboard(s), 1 pointer(s)", 15_000),
            // Остановок точки прерываний на исправном устройстве нет.
            Step::Absent("the report endpoint halted"),
            Step::Type("exit"),
            Step::Key("ret"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("stopped while"),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "e1000",
        about: "Сеть работает на настоящей карте Intel 8254x, а не только на virtio-net.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: true,
        e1000: true,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Карта найдена перебором семейств и поднята: адрес прочитан из
            // приёмного фильтра или из EEPROM — то есть у самой карты, а не
            // придуман нами.
            Step::Await("network     : e1000, hardware address", BOOT),
            Step::Absent("no network card attached"),
            Step::Await("freeos> ", BOOT),

            // Перепись шины называет карту и её драйвер. Это же проверка новой
            // команды `pci`: строка на стенде обязана совпадать с той, которую
            // человек фотографирует на чужой машине.
            Step::Line("pci"),
            Step::Await("8086:100e", 15_000),
            Step::Await("e1000, working", 15_000),

            // Адрес задаётся руками — служба DHCP здесь ни при чём, проверяется
            // карта. Раскладка сети задана QEMU: гость `10.0.2.15`, шлюз
            // `10.0.2.2`.
            Step::Line("ip 10.0.2.15/24 10.0.2.2"),
            Step::Await("address  10.0.2.15/24", 15_000),

            // И вот это — настоящая проверка драйвера: кадр ушёл через кольцо
            // передачи, ответ пришёл через кольцо приёма, и оба разобраны. До
            // этого шага «карта поднялась» означало только «регистры
            // записались».
            Step::Line("ping 10.0.2.2 1"),
            Step::Await("reply from 10.0.2.2, seq 1", 30_000),

            // Оболочка называет карту её собственным именем, а не зашитым в
            // текст: до этой фазы там стояло слово «virtio-net» буквами.
            Step::Line("ip"),
            Step::Await("  link     e1000 ", 15_000),

            Step::Type("exit"),
            Step::Key("ret"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "pipe",
        about: "Канал: вывод одной программы считает другая, и обе видят конец файла.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Главное утверждение: `wc` считает **чужой** вывод и доходит до
            // конца файла. Не дойти до него — самый вероятный отказ этой фазы:
            // забытый где-нибудь конец канала оставляет `wc` ждать вечно, и
            // выглядит это как зависшая команда, а не как ошибка.
            Step::Line("run /bin/hello | /bin/wc"),
            Step::Await("wc: ", 60_000),
            // Ждём, а не проверяем по буферу: числа печатаются шестью
            // системными вызовами, и хвост строки приходит позже её начала.
            Step::Await("lines,", 30_000),
            Step::Await("bytes in standard input", 30_000),
            // И обратная сторона: программа, чей вывод никто не читает, об этом
            // узнаёт. `run` без канала печатает в окно — значит счётчик строк
            // получил ровно то, что напечатал бы человек.
            Step::Line("run /bin/hello"),
            Step::Await("hello from userspace", 30_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "mobile",
        about: "Мобильный вид стола на экране телефона 720x1600: снимки для сверки с макетом.",
        target: Target::Live,
        usb_only: false,
        tablet: true,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("display     : 720x1600 set at start", BOOT),
            Step::Await("keyboard    : shown", BOOT),
            Step::Await("freeos> ", BOOT),
            Step::Wait(1500),
            Step::Shot("01-home"),
            // Экранная клавиатура печатает в оболочку: быстрая команда `free`,
            // затем ↵. Точки — середины кнопок на экране 720x1600 (снимок
            // `01-home`); команда доходит до оболочки тем же путём, что и с
            // USB-клавиатуры, и оболочка её выполняет.
            Step::Aim(Aim::Point(300, 1133)),
            Step::Click,
            Step::Await("keyboard    : typed 'free'", 15_000),
            Step::Aim(Aim::Point(613, 1509)),
            Step::Click,
            Step::Await(" MiB free", 15_000),
            Step::Wait(1000),
            Step::Shot("02-typed"),
            // Слово жестом (эскиз `swype`): палец ведут по буквам, не отрывая.
            // Точки — центры клавиш на экране 720x1600: ряды на y 1240, 1330,
            // 1420, пробел 1510. «echo» и «hello» — английской раскладкой.
            Step::Stroke(&[(199, 1240), (294, 1420), (423, 1330), (583, 1240)]),
            Step::Release,
            Step::Await("keyboard    : swipe of ", 15_000),
            Step::Expect("-> [\"echo\""),
            Step::Stroke(&[(423, 1330), (199, 1240), (615, 1330), (583, 1240)]),
            // Снимок посреди жеста — на удачу: след живёт 450 мс и в отладочном
            // QEMU обычно гаснет раньше снимка (проверен он был с TRAIL_MS=5000).
            // Пауза даёт столу дорисовать кадр; долгого нажатия она не вызовет —
            // палец уже сдвинулся дальше полуклавиши.
            Step::Wait(500),
            Step::Shot("02b-swiping"),
            Step::Release,
            Step::Await("-> [\"hello\"", 15_000),
            Step::Wait(800),
            Step::Shot("02c-suggestions"),
            // Протяжка по пробелу — смена языка; подписи клавиш — кириллица.
            Step::Stroke(&[(270, 1510), (350, 1510), (430, 1510)]),
            Step::Release,
            Step::Await("keyboard    : layout RU (ru)", 15_000),
            // «привет»: п р и в е т — ряды ЙЦУКЕН по одиннадцать и девять клавиш.
            Step::Stroke(&[(300, 1330), (358, 1330), (357, 1420), (184, 1330), (300, 1240), (406, 1420)]),
            Step::Release,
            Step::Await("-> [\"привет\"", 15_000),
            Step::Wait(800),
            Step::Shot("02d-russian"),
            Step::Aim(Aim::Point(613, 1509)),
            Step::Click,
            Step::Await("hello привет", 15_000),
            // Долгое нажатие: над «е» встают «е ё 5», палец доводят до «5».
            // Ячейка варианта — 68 точек, первая стоит над самой клавишей.
            Step::Aim(Aim::Point(300, 1240)),
            Step::Press,
            Step::Wait(1200),
            Step::Await("keyboard    : alternates ['е', 'ё', '5']", 15_000),
            Step::Aim(Aim::Point(436, 1240)),
            Step::Wait(300),
            Step::Shot("02e-long-press"),
            Step::Release,
            Step::Await("keyboard    : long press '5'", 15_000),
            // «ь» держат и отпускают на месте — выбран второй вариант, «ъ».
            Step::Aim(Aim::Point(455, 1420)),
            Step::Press,
            Step::Wait(1200),
            Step::Await("keyboard    : alternates ['ь', 'ъ']", 15_000),
            Step::Release,
            Step::Await("keyboard    : long press 'ъ'", 15_000),
            // ⌫ сразу после слова жестом стирает слово целиком.
            Step::Stroke(&[(300, 1330), (358, 1330), (357, 1420), (184, 1330), (300, 1240), (406, 1420)]),
            Step::Release,
            Step::Await("keyboard    : swipe of ", 15_000),
            Step::Aim(Aim::Point(632, 1420)),
            Step::Click,
            Step::Await("keyboard    : erased the swiped word", 15_000),
            // Удержание ⌫ стирает «5ъ» автоповтором. Проверить стёртое по
            // журналу нельзя — эхо набранного уже в нём; видно по снимку.
            Step::Aim(Aim::Point(632, 1420)),
            Step::Press,
            Step::Wait(1500),
            Step::Release,
            Step::Wait(500),
            Step::Shot("02f-erased"),
            // Быстрая команда латиницей в русской раскладке: стол посылает
            // Alt+Shift до и после, и оболочка получает `free`, а не `акуу`.
            Step::Aim(Aim::Point(300, 1133)),
            Step::Click,
            Step::Await("keyboard    : typed 'free'", 15_000),
            Step::Await("keyboard    : layout RU (ru)", 15_000),
            Step::Aim(Aim::Point(613, 1509)),
            Step::Click,
            Step::Await(" MiB free", 15_000),
            // Обратно на английский: дальше сценарий набирает латиницей.
            Step::Stroke(&[(270, 1510), (430, 1510)]),
            Step::Release,
            Step::Await("keyboard    : layout EN (us)", 15_000),
            Step::Wait(1000),
            // Сворачивание: окно улетает в стопку, клавиатура уходит, док
            // возвращается уже со стопкой. Снимок в полёте — на удачу: снимок
            // экрана монитором занимает время, сравнимое с самим полётом.
            // Кнопка «свернуть» телефона крупнее настольной и стоит первой слева
            // от закрытия; прицел `Minimize` считает по раскладке ПК.
            Step::Aim(Aim::Point(570, 164)),
            Step::Click,
            Step::Await("desktop     : minimized 'Terminal'", 15_000),
            Step::Wait(60),
            Step::Shot("03-flying"),
            // Клавиатура уходит в первом же кадре, полёт кончается позже —
            // строки ждутся в том порядке, в каком их печатает стол.
            Step::Await("keyboard    : hidden", 15_000),
            Step::Await("desktop     : minimize flight of 'Terminal': ", 15_000),
            Step::Wait(1500),
            Step::Shot("04-stack"),
            // Шторка — нажатием на строку состояния, закрывается нажатием мимо.
            Step::Aim(Aim::Point(360, 40)),
            Step::Click,
            Step::Await("desktop     : shade opened", 15_000),
            Step::Wait(1000),
            Step::Shot("04b-shade"),
            Step::Aim(Aim::Point(360, 1300)),
            Step::Click,
            Step::Await("desktop     : shade closed", 15_000),
            Step::Wait(500),
            // Нажатие на стопку открывает лист «Свёрнутые программы»; строка
            // терминала разворачивает его обратно полётом из стопки.
            Step::Aim(Aim::Point(368, 1495)),
            Step::Click,
            Step::Await("desktop     : minimized list opened", 15_000),
            Step::Wait(1000),
            Step::Shot("05-minimized-list"),
            Step::Aim(Aim::Point(300, 1130)),
            Step::Click,
            Step::Await("desktop     : restored 'Terminal'", 15_000),
            Step::Await("desktop     : restore flight of 'Terminal': ", 15_000),
            Step::Await("keyboard    : shown", 15_000),
            Step::Wait(1000),
            Step::Shot("06-restored"),
            // «Пуск» с поиском: свернуть терминал, нажать «Поиск» (со стопкой в
            // доке это плитка с лупой), набрать «pk» клавишами экранной
            // клавиатуры и ↵ — открывается первое найденное.
            Step::Aim(Aim::Point(570, 164)),
            Step::Click,
            Step::Await("desktop     : minimize flight of 'Terminal': ", 15_000),
            Step::Wait(1000),
            Step::Aim(Aim::Point(508, 1495)),
            Step::Click,
            Step::Await("start       : opened for search", 15_000),
            Step::Await("keyboard    : shown", 15_000),
            Step::Wait(1000),
            Step::Aim(Aim::Point(647, 1240)),
            Step::Click,
            Step::Await("start       : query 'p'", 15_000),
            Step::Aim(Aim::Point(551, 1330)),
            Step::Click,
            Step::Await("start       : query 'pk'", 15_000),
            Step::Wait(1000),
            Step::Shot("07-search"),
            Step::Aim(Aim::Point(613, 1509)),
            Step::Click,
            // Первым «pk» находит «Пакеты» (пояснение «установка .fpk») — они
            // открывают «Настройки».
            Step::Await("desktop     : opened 'Settings'", 15_000),
            // Окно вырастает из плитки (у «Пакетов» своей нет — «Параметры»
            // растут из «Настроек») и закрывается уходом в неё же. Середина
            // полёта снимается на удачу, как `03-flying`; строка журнала о
            // конце полёта — не на удачу.
            Step::Await("desktop     : open zoom of 'Settings': ", 15_000),
            Step::Wait(1000),
            Step::Shot("08-settings"),
            // Настройки на телефоне (макет, экраны 04 и 05): список разделов;
            // «Экран» открывает раздел с заголовком, стрелка ведёт назад.
            Step::Aim(Aim::Point(360, 560)),
            Step::Click,
            Step::Await("settings    : opened 'Экран'", 15_000),
            Step::Wait(1000),
            Step::Shot("08c-settings-display"),
            Step::Aim(Aim::Point(80, 264)),
            Step::Click,
            Step::Await("settings    : back to the list", 15_000),
            Step::Aim(Aim::Point(642, 164)),
            Step::Click,
            Step::Await("desktop     : closed 'Settings'", 15_000),
            Step::Wait(40),
            Step::Shot("08b-closing"),
            Step::Await("desktop     : close zoom of 'Settings': ", 15_000),
            Step::Wait(1000),
            Step::Shot("09-closed"),
            // Питание (макет, экран 08): «Пуск» -> кнопка питания открывает
            // лист над доком. Нажатие мимо закрывает его, ничего не выключая.
            Step::Aim(Aim::Point(211, 1495)),
            Step::Click,
            Step::Await("start       : opened", 15_000),
            Step::Wait(800),
            Step::Aim(Aim::Point(632, 1320)),
            Step::Click,
            Step::Await("desktop     : power sheet opened", 15_000),
            Step::Wait(1000),
            Step::Shot("10-power"),
            Step::Aim(Aim::Point(360, 300)),
            Step::Click,
            Step::Await("desktop     : power sheet closed", 15_000),
            // Витрина образцов .NET — девятая плитка, первая в третьем ряду.
            Step::Wait(800),
            Step::Aim(Aim::Point(111, 941)),
            Step::Click,
            Step::Await("desktop     : started '/bin/dotnet /usr/share/dotnet/samples/gallery.dll'", 15_000),
            Step::Wait(30_000),
            Step::Shot("11-gallery"),
        ],
    },
    Scenario {
        name: "desktop",
        about: "Меню запуска открывает программу, окно двигается и закрывается, фокус возвращается.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Сколько программ меню показало. Порогом, а не числом: помещается
            // на разных машинах разное — на AArch64 влезают все двадцать шесть,
            // на x86-64 последняя уже нет.
            //
            // Проверка стоит **до** ожидания приглашения, и это не придирка к
            // порядку: рабочий стол поднимается при загрузке, то есть строку он
            // печатает раньше, чем оболочка здоровается. Ожидание приглашения
            // уводит курсор за неё, и всё, что ищет вперёд, её уже не находит.
            Step::AtLeast("desktop     : start menu lists ", 7, BOOT),
            Step::Await("freeos> ", BOOT),
            // Стол не печатает ничего сам по себе, поэтому все проверки здесь
            // опираются на строки, которые оконный менеджер пишет в журнал.
            // Иначе фазу мог бы проверить только человек, глядящий на экран.
            Step::Expect("window      : 'Terminal'"),
            // Второе окно стола — монитор системы, и с фазы 47b это программа.
            // Запускается она **явно**, а не службой, и это решение, а не
            // удобство сценария: открытие окна держит стол сотни миллисекунд, и
            // служба делала бы этот момент непредсказуемым — он приходился бы
            // то на набор команды, то на движение мыши, и тогда они пропадали.
            //
            // Дождаться окна обязательно: пока оно не открылось, порядок окон по
            // глубине не определён, а от него зависит всё, что ниже делает Tab.
            Step::Line("run -b /bin/sysmon"),
            Step::Await("window      : 'System' at", 30_000),
            Step::Key("f1"),
            Step::Await("desktop     : menu opened", 15_000),
            Step::Wait(400),
            Step::Shot("01-menu"),
            // F1 закрывает меню тем же нажатием, что открыло: имя клавиши
            // `escape` у `sendkey` своё (`esc`), и полагаться на него незачем.
            Step::Key("f1"),
            Step::Await("desktop     : menu closed", 15_000),
            // Вернуть фокус оболочке **обязательно**, и это не про удобство.
            // Серийная линия — тоже ввод, и достаётся она активному окну: пока
            // в фокусе окно монитора, набранная команда уезжает к нему
            // клавишами и пропадает. Прогон при этом падает не здесь, а через
            // двадцать секунд, по пределу простоя оболочки, — и выглядит как
            // «система не приняла команду».
            Step::Key("alt-tab"),
            Step::Await("desktop     : focus 'Terminal'", 15_000),
            // Третье окно — файловый менеджер, и с фазы 47c это **программа**.
            // Запускается так же, как монитор, и по той же причине: открытие
            // окна держит стол сотни миллисекунд, а из меню этот момент
            // приходился бы то на набор команды, то на движение мыши.
            //
            // Через меню он тоже открывается — правым столбцом, где лежат
            // программы из `/bin`, — и это проверяет сценарий `mouse` значком
            // на столе. Здесь важен порядок окон по глубине, а он определён
            // только после того, как окно открылось.
            Step::Line("run -b /bin/files"),
            // «(focused)» в конце строки — и есть доказательство фокуса:
            // отдельной строки `focus` при открытии окна программы нет, её
            // печатает только переключение между уже открытыми.
            Step::Await("window      : 'Files' at", 30_000),
            // Строка самой программы: каталог прочитан, столько-то записей,
            // такая-то выбрана. Снимок экрана доказательством не считается, а
            // эта строка отличает «список показан» от «окно нарисовано пустым».
            // И она же доказывает, что `SYS_READDIR` доехал из третьего кольца
            // до ext2: до 47c этот список читало ядро прямым вызовом.
            Step::Await("files: / has ", 15_000),
            // Пауза щедрая не от неуверенности: снимок показывает последний
            // нарисованный кадр, а отладочная сборка перерисовывает окно в
            // эмуляторе заметно дольше, чем стенд успевает нажать следующую
            // клавишу. Слишком ранний снимок показал бы стол на шаг позади.
            Step::Wait(2_500),
            Step::Shot("02-files"),
            // Ввод уходит в активное окно, а не в оболочку: стрелки листают
            // список, Enter открывает. И теперь это ввод **в программу**: до
            // фазы 47c клавиша без символа события не давала вовсе, а список
            // листают именно стрелками.
            //
            // Выделение сдвигается на две строки, и программа говорит, на
            // какую. Имя в строке — то, чего не даст ни снимок, ни счётчик
            // нажатий: оно доказывает, что стрелка дошла до программы и что та
            // поняла её как «вниз», а не как что-нибудь ещё.
            Step::Repeat("down", 2),
            Step::Await("files: / has ", 15_000),
            // Home возвращает выделение на первую строку. Она заведомо каталог:
            // список сортируется каталогами вверх, а в корне каталог есть
            // всегда. Так следующий Enter проверяет именно переход, не завися
            // от того, чем собран образ.
            Step::Key("home"),
            Step::Await("files: / has ", 15_000),
            Step::Key("ret"),
            // Каталог сменился. Отдельная строка, а не «список перечитан»:
            // «выделение переехало» и «мы вошли внутрь» иначе выглядят
            // одинаково — обе печатают путь и число строк.
            Step::Await("files: entered '/", 15_000),
            Step::Wait(2_500),
            Step::Shot("03-files-open"),
            Step::Repeat("ctrl-right", 2),
            Step::Key("ctrl-down"),
            Step::Wait(400),
            Step::Shot("04-moved"),
            // Обойти окна по кругу и убедиться, что оболочка по-прежнему
            // принимает команды: сочетания оконного менеджера не должны
            // оставлять после себя зажатых модификаторов.
            //
            // Куда попадёт Alt+Tab — следствие порядка окон по глубине, и фаза 47c
            // его снова изменила. Раньше файловый менеджер заводило ядро сразу
            // из меню; теперь он программа, и перед его запуском приходится
            // поднять оболочку (иначе команда уедет в чужое окно). Поднятая
            // оболочка встаёт над монитором, и стопка сверху вниз получается
            // «файлы, оболочка, монитор» — а обход идёт по ней вниз с
            // переносом, то есть от файлов к монитору.
            //
            // Поэтому шаги названы поимённо, а не сосчитаны числом: счёт молча
            // ломается при следующей же перестановке, а имя окна в ожидании
            // показывает, куда ввод ушёл на самом деле.
            Step::Key("alt-tab"),
            Step::Await("desktop     : focus 'System'", 5_000),
            Step::Key("alt-tab"),
            Step::Await("desktop     : focus 'Terminal'", 5_000),
            Step::Line("echo after-move"),
            Step::Await("after-move", 10_000),
            Step::Key("alt-tab"),
            Step::Await("desktop     : focus 'Files'", 5_000),
            // Ctrl+W теперь **просьба**, а не приказ: окно закрывает сама
            // программа, и ядру остаётся снять его за ней. Три строки, а не
            // одна, и каждая своя: система попросила, программа согласилась,
            // окно снято. Пропади средняя — и «программа послушалась» стало бы
            // неотличимо от «ядро закрыло окно само».
            Step::Key("ctrl-w"),
            Step::Await("desktop     : close requested of 'Files'", 15_000),
            Step::Await("files: closing on request", 15_000),
            Step::Await("desktop     : closed the window of ", 15_000),
            // Закрытие возвращает фокус верхнему из оставшихся окон, а им
            // оказывается оболочка: её подняли перед запуском менеджера, и она
            // так и осталась над монитором. Ждать этого обязательно — команда
            // ниже набирается с клавиатуры и должна попасть в терминал, а не в
            // чужое окно.
            Step::Await("desktop     : focus 'Terminal'", 5_000),
            // Дальше набираем с клавиатуры, а не в серийную линию, и это не
            // придирка к стилю. Клавиатура USB опрашивается задачей, а UART
            // приходит прерыванием; пока отладочная сборка перерисовывает
            // экран, отпускание Ctrl ждёт следующего опроса, а байты из линии
            // успевают приехать раньше — и команда попадает в систему как
            // Ctrl+U, Ctrl+I. С одного устройства порядок событий сохраняется
            // всегда, поэтому именно так это и проверяется.
            Step::Type("ui"),
            Step::Key("ret"),
            Step::Await("windows,", 30_000),
            Step::Shot("05-desktop"),
            // «О системе» — окно с кнопкой, а не текстовая сетка (фаза С1). Меню
            // открывается с выбранной первой строкой, «О системе» — третья.
            // Закрывается оно Enter: так проверяется, что клавиша нажимает
            // выбранную кнопку, а не уходит в никуда. Снимок — единственное
            // доказательство, что русский текст больше не знаки вопроса:
            // строки журнала у окна те же, что были у сетки.
            Step::Key("f1"),
            Step::Await("desktop     : menu opened", 15_000),
            Step::Repeat("down", 2),
            Step::Key("ret"),
            Step::Await("desktop     : opened 'About'", 15_000),
            Step::Wait(1_500),
            Step::Shot("05-about"),
            Step::Key("ret"),
            Step::Await("desktop     : closed 'About'", 15_000),
            Step::Await("desktop     : focus 'Terminal'", 15_000),
            // «Пуск» с фазы С3 — только то, у чего есть окно: консольные
            // программы из `/bin` запускаются в терминале по имени, и их список
            // печатает `help`. Число программ в `/bin` — точным числом: оно от
            // экрана не зависит и меняется только вместе с `USER_PROGRAMS` в
            // `build.rs`. Тридцать две с фазы 46; `zdemo` собирается только там,
            // где выполнена `cargo xtask thirdparty`, и без неё здесь будет 31.
            Step::Expect("/bin holds 37 programs"),
            // «Файлы» — четвёртая строка: «Терминал», «Параметры» и «О системе»
            // стоят первыми и в прежнем порядке, на них рассчитаны другие
            // сценарии. Программа из меню открывает своё окно и не поднимает
            // терминал — в этом разница между приложением и командой.
            Step::Key("f1"),
            Step::Await("desktop     : menu opened", 15_000),
            Step::Wait(1_000),
            Step::Shot("06-menu-programs"),
            Step::Repeat("down", 3),
            Step::Key("ret"),
            Step::Await("desktop     : started '/bin/files'", 15_000),
            Step::Await("window      : 'Files' at", 30_000),
            Step::Wait(2_000),
            Step::Shot("07-program-ran"),
            Step::Key("ctrl-w"),
            Step::Await("desktop     : close requested of 'Files'", 15_000),
            Step::Await("desktop     : closed the window of ", 15_000),
            Step::Await("desktop     : focus 'Terminal'", 15_000),
            // Фаза С2: раскладка. Alt+Shift переключает на русскую, клавиши
            // `f` и `d` приезжают в оболочку как «ав», и она отвечает набранным
            // — журнал несёт UTF-8, и стенд сверяет его как текст. Снимок
            // ловит трей с «RU» и кириллицу в терминале.
            Step::Key("alt-shift"),
            Step::Await("keyboard    : layout RU (ru)", 15_000),
            Step::Key("f"),
            Step::Key("d"),
            Step::Wait(600),
            Step::Shot("09-tray-ru"),
            Step::Key("ret"),
            Step::Await("unknown command 'ав'", 15_000),
            // Win+Пробел — обратно. Клавиша с логотипом при этом меню не
            // открывает: его открывает отпускание, и только если за нажатием не
            // последовало другой клавиши.
            Step::Key("meta_l-spc"),
            Step::Await("keyboard    : layout EN (us)", 15_000),
            Step::Key("meta_l"),
            Step::Await("desktop     : menu opened", 15_000),
            Step::Key("esc"),
            Step::Await("desktop     : menu closed", 15_000),
            // Меню трея: правая кнопка по часам, первый пункт — раскладка.
            Step::Aim(aim::Aim::Tray),
            Step::RightClick,
            Step::Await("for tray", 15_000),
            Step::Wait(600),
            Step::Shot("10-tray-menu"),
            Step::Key("ret"),
            Step::Await("keyboard    : layout RU (ru)", 15_000),
            Step::Key("alt-shift"),
            Step::Await("keyboard    : layout EN (us)", 15_000),
            // Меню умеет не только запускать. «Shut down» открывает не
            // выключение, а вопрос: подтверждение сделано обычным окном, потому
            // что человек уже знает, как закрываются окна.
            //
            // Пункт **седьмой**: перед питанием стоят программы с окнами из
            // `/bin` — «Файлы», «Диспетчер задач» и с фазы С7 «Диспетчер
            // устройств».
            Step::Key("f1"),
            Step::Await("desktop     : menu opened", 15_000),
            Step::Repeat("down", 6),
            Step::Key("ret"),
            Step::Await("desktop     : opened 'Shut down'", 15_000),
            Step::Wait(2_500),
            Step::Shot("08-shutdown"),
            // «Нет» обязано означать «нет»: машина остаётся работать, окно
            // закрывается, и об отказе сказано вслух.
            Step::Key("n"),
            Step::Await("desktop     : 'Shut down' cancelled", 15_000),
            Step::Absent("shutting down"),
            // А «да» гасит машину. Путь тут другой, чем у команды `shutdown`:
            // ответ разбирается под замком рабочего стола, где ни ждать диск,
            // ни ждать прерывание нельзя, поэтому окно только поднимает
            // просьбу — а гасит систему служебная задача.
            //
            // Пункт тот же седьмой, что и выше.
            Step::Key("f1"),
            Step::Await("desktop     : menu opened", 15_000),
            Step::Repeat("down", 6),
            Step::Key("ret"),
            Step::Await("desktop     : opened 'Shut down'", 15_000),
            Step::Key("y"),
            Step::Await("power       : confirmed in the start menu", 15_000),
            Step::Await("shutting down", 15_000),
            Step::Exits(30_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "display",
        about: "Разрешение меняется из «Параметров» без перезагрузки, и мышь попадает в новый экран.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            Step::Key("f1"),
            Step::Await("desktop     : menu opened", 15_000),
            Step::Key("down"),
            Step::Key("ret"),
            Step::Await("desktop     : opened 'Settings'", 15_000),

            // Окно открывается на «Экране»; Enter переводит клавиши к пунктам.
            // Режимы выбраны так, чтобы **каждая** смена меняла размер на обеих
            // архитектурах. Загрузчик ставит 1280×720 на x86 и 1024×768 на
            // AArch64 — первая версия сценария начинала с 1024×768 и на AArch64
            // переключала экран в тот же размер, то есть проверка размера кадра
            // там ничего не доказывала. Поэтому сначала 1280×800 (третий пункт)
            // — больше обоих, потом 1024×768 (первый) — сжатие окон на обеих.
            Step::Key("ret"),
            Step::Repeat("down", 2),
            Step::Key("ret"),
            Step::Await("display     : 1280x800 set now", 30_000),
            Step::Await("desktop     : 1280x800, ui scale", 30_000),
            // Главное доказательство — от QEMU: кадр, который отдаёт устройство,
            // стал этого размера. Строки гостя выше говорят только о том, что
            // гость так думает.
            Step::Screen(1280, 800),
            Step::Wait(3_000),
            Step::Shot("01-1280x800"),

            // Вторая смена проверяет то, чего не проверяет первая: адаптер
            // переключается из режима, который задали мы, а не прошивка, и на
            // AArch64 прежний буфер — уже наш и возвращается в пул.
            Step::Repeat("up", 2),
            Step::Key("ret"),
            Step::Await("display     : 1024x768 set now", 30_000),
            Step::Await("desktop     : 1024x768, ui scale", 30_000),
            Step::Screen(1024, 768),
            Step::Wait(3_000),
            Step::Shot("02-1024x768"),

            // Мышь на новом экране. Указатель после смены встаёт в середину,
            // поэтому сначала — в угол, до упора; дальше стенд целится по
            // последней строке размера стола. Кнопка меню промахнулась бы, если
            // бы панель осталась на высоте прежнего режима.
            Step::Aim(Aim::Corner),
            Step::Aim(Aim::MenuButton),
            Step::Click,
            Step::Await("desktop     : menu opened", 15_000),
            Step::Wait(1_500),
            Step::Shot("03-menu"),
            Step::Click,
            Step::Await("desktop     : menu closed", 15_000),
        ],
    },
    Scenario {
        name: "mouse",
        about: "Курсор ездит, щелчок поднимает окно, окно тащится за заголовок и закрывается кнопкой.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Мышь — второе устройство на контроллере. Строка ниже доказывает,
            // что перечисление портов дошло до неё, а не остановилось на
            // клавиатуре.
            Step::Expect("slot 2 is a mouse"),
            // И то, что она читается по собственному дескриптору, а не по
            // boot-протоколу: boot остался запасным путём, и ходить по нему
            // здесь нечего — иначе он проверялся бы, а основной нет.
            Step::Expect("pointer, relative"),
            Step::Expect("Mouse: click to focus"),
            // Доехать до угла можно только упёршись в край: если ограничение
            // не работает, курсор уедет за экран и следующие наводки промажут.
            Step::Aim(Aim::Corner),
            Step::Line("ui"),
            Step::Await("pointer  0,0 visible", 15_000),
            // Кнопка меню — в левом нижнем углу, где панель начинается на любом
            // экране.
            Step::Aim(Aim::MenuButton),
            Step::Click,
            Step::Await("desktop     : menu opened", 15_000),
            Step::Wait(1_500),
            Step::Shot("01-menu"),
            Step::Click,
            Step::Await("desktop     : menu closed", 15_000),
            // Монитор системы — программа (фаза 47b), и запускается он явно.
            //
            // Стоит это **здесь**, а не в начале сценария, и место выбрано не
            // случайно: открытие окна держит рабочий стол сотни миллисекунд, и
            // наведение указателя, попавшее на этот момент, не доезжает. Пусть
            // окно появится тогда, когда мышь всё равно стоит на месте, — а
            // целиться в него будут уже следующим шагом.
            Step::Line("run -b /bin/sysmon"),
            Step::Await("window      : 'System' at", 30_000),
            // Щелчок по заголовку нижнего окна поднимает его и передаёт ему
            // ввод: до этой фазы того и другого можно было добиться только
            // клавишей Tab.
            Step::Aim(Aim::Title("System")),
            Step::Click,
            Step::Await("desktop     : focus 'System'", 15_000),
            // Перетаскивание: нажать, провезти, отпустить. Куда окно приехало,
            // говорит сам гость — снимок экрана этого не доказывает.
            Step::Press,
            Step::Await("desktop     : drag 'System'", 15_000),
            Step::Move(-160, 120),
            Step::Release,
            Step::Await("desktop     : moved 'System' to ", 15_000),
            Step::Wait(1_500),
            Step::Shot("02-dragged"),
            // Кнопка закрытия — у правого края заголовка. Прицел считается от
            // нового положения окна: строка о перетаскивании его и сообщила.
            //
            // Крестик у окна программы **просит** закрыться, а не закрывает:
            // решает программа. Поэтому строк здесь три, и порядок у них не
            // случайный — просьба, согласие программы, снятое окно.
            Step::Aim(Aim::Close("System")),
            Step::Click,
            Step::Await("desktop     : close requested of 'System'", 15_000),
            Step::Await("sysmon: closing on request", 15_000),
            Step::Await("desktop     : closed the window of ", 15_000),
            Step::Aim(Aim::Middle("Terminal")),
            Step::Click,
            Step::Await("desktop     : focus 'Terminal'", 15_000),
            Step::Wait(1_500),
            Step::Shot("03-desktop"),
            // Кнопки заголовка: свернуть — и окна на экране нет, зато видны
            // значки стола; кнопка в панели задач возвращает его обратно.
            Step::Aim(Aim::Minimize("Terminal")),
            Step::Click,
            Step::Await("desktop     : minimized 'Terminal'", 15_000),
            Step::Wait(1_500),
            Step::Shot("04-icons"),
            // Двойной щелчок по значку **запускает программу**. Первый значок
            // сверху — файловый менеджер, и с фазы 47c это `/bin/files`, а не
            // окно ядра. Путь запуска новый: до неё значок умел открывать
            // только окна ядра, а программы запускались единственно из меню.
            Step::Aim(Aim::Icon(0)),
            Step::Click,
            Step::Click,
            Step::Await("desktop     : started '/bin/files'", 15_000),
            // Три строки, и каждая проверяет своё звено: ядро завело задачу,
            // программа попросила окно, программа прочитала каталог. Оборвись
            // цепочка посередине — видно, где именно.
            Step::Await("desktop     : opened 'Files' for ", 30_000),
            Step::Await("files: / has ", 30_000),
            Step::Wait(1_500),
            Step::Shot("05-icon-opened"),
            // Крестиком окно **просят** закрыться, а закрывает его программа.
            // До 47c это окно снимало ядро само; теперь снаружи видно обе
            // половины разговора, и средняя строка — согласие программы.
            Step::Aim(Aim::Close("Files")),
            Step::Click,
            Step::Await("desktop     : close requested of 'Files'", 15_000),
            Step::Await("files: closing on request", 15_000),
            Step::Await("desktop     : closed the window of ", 15_000),
            // Свёрнутый терминал возвращается тем же двойным щелчком по своему
            // значку: «открыть» уже открытое окно — это показать его снова.
            Step::Aim(Aim::Icon(1)),
            Step::Click,
            Step::Click,
            Step::Await("desktop     : restored 'Terminal'", 15_000),
            Step::Wait(1_000),
            // Развернуть на весь стол и вернуть обратно — оба перехода
            // печатаются, потому что оба меняют размер окна.
            //
            // Проверяется это на терминале, а не на файловом менеджере, и
            // причина названа в договоре: окно программы размера **не меняет**.
            // События «размер изменился» в договоре нет, и перевыделить
            // поверхность под работающей программой значило бы поменять память
            // под чужой рукой. Менеджер с 47c — программа, поэтому проверку
            // переносить пришлось, а не выбрасывать.
            Step::Aim(Aim::Maximize("Terminal")),
            Step::Click,
            Step::Await("desktop     : resized 'Terminal'", 15_000),
            Step::Wait(1_500),
            Step::Shot("06-maximized"),
            Step::Aim(Aim::Maximize("Terminal")),
            Step::Click,
            Step::Await("desktop     : resized 'Terminal'", 15_000),
            Step::Wait(1_000),
            // Смена темы. Проверяется отсюда, а не из окна параметров, по двум
            // причинам: пункт меню стола стоит на известном месте, а перекраска
            // из него задевает **всё** — обои, значки, панель, оба открытых
            // окна, — то есть ровно то, что ломается, если перекрасить не всех.
            //
            // Пункт третий сверху, и над ним одна разделительная черта: пункты
            // «создать» отделены от «тема / настройки / обновить».
            Step::Aim(Aim::EmptyRight),
            Step::RightClick,
            Step::Await("desktop     : context menu at", 15_000),
            Step::Aim(Aim::ContextItem(2, 1)),
            Step::Click,
            Step::Await("desktop     : theme light", 15_000),
            Step::Wait(2_000),
            Step::Shot("12-light"),
            // И обратно: тёмная тема обязана вернуться такой же, какой была, а
            // не «почти такой». Снимок до и снимок после сравнивает человек.
            Step::Aim(Aim::EmptyRight),
            Step::RightClick,
            Step::Await("desktop     : context menu at", 15_000),
            Step::Aim(Aim::ContextItem(2, 1)),
            Step::Click,
            Step::Await("desktop     : theme dark", 15_000),
            Step::Wait(2_000),
            Step::Shot("13-dark-again"),
            Step::Shot("07-restored"),
            // «Параметры»: значок третий сверху. Терминал при этом сворачивается
            // — значки лежат на столе, то есть под окнами, и щелчок по значку
            // из-под окна означал бы щелчок по окну.
            Step::Aim(Aim::Minimize("Terminal")),
            Step::Click,
            Step::Await("desktop     : minimized 'Terminal'", 15_000),
            Step::Aim(Aim::Icon(2)),
            Step::Click,
            Step::Click,
            Step::Await("desktop     : opened 'Settings'", 15_000),
            Step::Wait(2_000),
            Step::Shot("08-settings"),
            // «Экран» — первый раздел в левой колонке, и Enter переводит
            // разбор клавиш из списка разделов в сами настройки. Снимок после
            // этого показывает то, ради чего раздел и открывают: список
            // режимов и два образца темы.
            Step::Key("ret"),
            Step::Wait(1_500),
            Step::Shot("09-settings-display"),
            // Убрать за собой: окно параметров закрывается, терминал
            // возвращается со стола — иначе набранное ниже ушло бы не туда.
            Step::Aim(Aim::Close("Settings")),
            Step::Click,
            Step::Await("desktop     : closed 'Settings'", 15_000),
            // Меню стола: правый щелчок по пустому месту, создание папки,
            // закрытие щелчком мимо. Проверяется строками гостя — снимок
            // показывает последний нарисованный кадр, а не то, что случилось.
            Step::Aim(Aim::Empty),
            Step::RightClick,
            Step::Await("desktop     : context menu at", 15_000),
            Step::Wait(1_500),
            Step::Shot("10-context-menu"),
            // Живой носитель ничего не хранит: корень — образ initrd в FAT, где
            // каталогов не заводят. Меню обязано сказать это словами, а не
            // «operation not supported»; создание же проверяется на
            // установленной системе, где корень ext2 (сценарий `installed`).
            Step::Aim(Aim::ContextItem(0, 0)),
            Step::Click,
            Step::Await("cannot create: the live system cannot store files", 15_000),
            Step::Wait(1_500),
            Step::Shot("11-context-refused"),
            // Закрывается щелчком мимо — и «мимо» здесь ниже самого меню:
            // точка, из которой его открыли, теперь занята им самим.
            Step::Aim(Aim::EmptyBelow),
            Step::Click,
            Step::Await("desktop     : context menu closed", 15_000),
            Step::Aim(Aim::Icon(1)),
            Step::Click,
            Step::Click,
            Step::Await("desktop     : restored 'Terminal'", 15_000),
            Step::Wait(1_000),
            Step::Type("exit"),
            Step::Key("ret"),
            Step::Await("finishing the session", 30_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "tablet",
        about: "Манипулятор без boot-протокола: планшет разобран по дескриптору отчётов и ставит курсор в точку.",
        target: Target::Live,
        usb_only: false,
        tablet: true,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Планшет не объявляет boot-протокола вовсе, и единственный способ
            // узнать, что в его отчёте, — прочитать дескриптор. Строка ниже и
            // есть доказательство того, что он прочитан и понят: и то, что
            // координаты абсолютные, и границы их диапазона.
            Step::Expect("pointer, absolute 0..32767"),
            Step::Expect("is a mouse"),
            Step::Expect("Mouse: click to focus"),
            // Абсолютный указатель ставится в точку, а не привозится в неё. Если
            // разбор соврал в разрядности поля или в границах, курсор окажется
            // не там, и следующая же наводка промахнётся мимо окна.
            Step::Aim(Aim::Corner),
            Step::Line("ui"),
            Step::Await("pointer  0,0 visible", 15_000),
            Step::Aim(Aim::MenuButton),
            Step::Click,
            Step::Await("desktop     : menu opened", 15_000),
            Step::Wait(1_500),
            Step::Shot("01-menu"),
            Step::Click,
            Step::Await("desktop     : menu closed", 15_000),
            // Монитор — программа; запускаем явно и здесь же, как в сценарии
            // `mouse`: пока указатель стоит, открытие окна никому не мешает.
            Step::Line("run -b /bin/sysmon"),
            Step::Await("window      : 'System' at", 30_000),
            Step::Aim(Aim::Title("System")),
            Step::Click,
            Step::Await("desktop     : focus 'System'", 15_000),
            // Перетаскивание абсолютным указателем: окно обязано ехать за
            // точкой, хотя приращений устройство не сообщает вовсе — их считает
            // ядро, по разнице положений курсора.
            Step::Press,
            Step::Await("desktop     : drag 'System'", 15_000),
            Step::Move(-160, 120),
            Step::Release,
            Step::Await("desktop     : moved 'System' to ", 15_000),
            Step::Wait(1_500),
            Step::Shot("02-dragged"),
            Step::Aim(Aim::Close("System")),
            Step::Click,
            Step::Await("desktop     : close requested of 'System'", 15_000),
            Step::Await("sysmon: closing on request", 15_000),
            Step::Await("desktop     : closed the window of ", 15_000),
            Step::Aim(Aim::Middle("Terminal")),
            Step::Click,
            Step::Await("desktop     : focus 'Terminal'", 15_000),
            Step::Wait(1_500),
            Step::Shot("03-desktop"),
            Step::Type("exit"),
            Step::Key("ret"),
            Step::Await("finishing the session", 30_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "hotplug",
        about: "Устройство, воткнутое на ходу, поднимается само; выдернутое — освобождает слот.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // На старте машина знает про два устройства: клавиатуру и мышь.
            Step::Line("usb"),
            Step::Await("  xhci     2 devices", 15_000),
            // Третье втыкается уже на работающей системе — как это делает
            // человек. До этой фазы ядро перечисляло порты один раз и такое
            // устройство не существовало для него вовсе.
            Step::Plug("usb-tablet,bus=xhci.0,id=hotplugged"),
            Step::Await("usb         : now 3 device(s)", 20_000),
            // И оно именно поднято, а не просто замечено: указатель разобран по
            // своему дескриптору отчётов.
            Step::Expect("pointer, absolute"),
            Step::Line("usb"),
            Step::Await("  xhci     3 devices", 15_000),
            // Обратная сторона: выдернутое устройство обязано вернуть слот.
            // Слотов у контроллера четыре, и не вернуть слот — значит потерять
            // его до перезагрузки.
            Step::Unplug("hotplugged"),
            Step::Await("is gone, freeing slot", 20_000),
            Step::Line("usb"),
            Step::Await("  xhci     2 devices", 15_000),
            // И память вернулась. Проверяется числом, а не словом: до фазы 25
            // окно DMA умело только расти, и десять подключений подряд его
            // исчерпывали. Значение снимается сейчас, а сверяется после ещё
            // трёх кругов — если освобождение не работает, оно вырастет.
            Step::Line("mem"),
            Step::Capture("  dma      ", 15_000),
            Step::Plug("usb-tablet,bus=xhci.0,id=again1"),
            Step::Await("usb         : now 3 device(s)", 20_000),
            Step::Unplug("again1"),
            Step::Await("is gone, freeing slot", 20_000),
            Step::Plug("usb-tablet,bus=xhci.0,id=again2"),
            Step::Await("usb         : now 3 device(s)", 20_000),
            Step::Unplug("again2"),
            Step::Await("is gone, freeing slot", 20_000),
            Step::Plug("usb-tablet,bus=xhci.0,id=again3"),
            Step::Await("usb         : now 3 device(s)", 20_000),
            Step::Unplug("again3"),
            Step::Await("is gone, freeing slot", 20_000),
            Step::Line("mem"),
            Step::Await("  dma      {} of", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "pmtimer",
        about: "Машина, на которой не удалось измерить частоту по PIT: её меряют по таймеру ACPI, и время идёт.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[Arch::X86_64],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        // Свойство накапливается к уже заданной машине `q35`, как и `i8042=off`
        // у `usb_only`. Так воспроизводится VirtualBox: там PIT существует, но
        // измерить по нему частоту локального APIC не удаётся, и система
        // оставалась без таймера — то есть без всего, что зависит от времени.
        extra: &["-machine", "pit=off"],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Первый источник обязан отказать: иначе сценарий проверяет не то,
            // ради чего заведён.
            Step::Expect("calibration against the PIT failed"),
            Step::Expect("measured against the ACPI timer"),
            Step::Absent("timer disabled"),
            // И главное: таймер после этого работает. Тик — это доставленное
            // прерывание, и ненулевое их число означает, что счётчик заряжен
            // осмысленным значением, а вектор доходит.
            Step::Line("uptime"),
            Step::AtLeast(" ms, ", 1, 15_000),
            Step::Line("echo pmtimer-ok"),
            Step::Await("  pmtimer-ok", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "userspace",
        about: "Программа исполняется в своём адресном пространстве, а её отказ снимает её, а не систему.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            Step::Line("ls /bin"),
            Step::Await("hello", 15_000),
            // Первая программа: печатает через системный вызов, спрашивает у
            // ядра время работы и завершается своим кодом.
            Step::Line("run /bin/hello"),
            // Главное утверждение фазы 12b, и проверить его больше нечем:
            // память программы отображена в её собственных таблицах и
            // отсутствует в таблицах ядра. Обе строки печатает ядро, обойдя
            // оба дерева, — то есть это не пересказ намерения, а результат.
            // С фазы 54 точка входа при запуске ещё не отображена — её
            // страница придёт по обращению; ядро подтверждает, что она лежит в
            // исполняемом сегменте из таблицы областей.
            Step::Await("entry is paged on demand from the segment at", 30_000),
            Step::Await("the kernel space maps nothing at", 15_000),
            Step::Await("hello from userspace", 30_000),
            Step::Await("uptime as the kernel sees it:", 15_000),
            // Время суток дошло до третьего кольца — и это настоящее время, а не
            // число, которое программа могла бы напечатать, ничего не зная:
            // сверяется оно с часами хоста, тем же допуском, что и в `clock`.
            Step::Clock("hello: epoch ", 10, 15_000),
            // Окно возвращается в пул целиком: 768 страниц образа плюс 16
            // страниц стека и четыре таблицы, включая корень. Точное число
            // здесь намеренно — «часть окна вернулась» выглядело бы так же,
            // как «всё», — и оно связывает сценарий с двумя константами ядра
            // (`user::IMAGE_PAGES` и `user::STACK_PAGES`). Менять его вместе с
            // ними правильно: «сколько памяти держит программа» обязано
            // меняться заметно, а не молча. Число 136 стояло здесь с тех пор,
            // когда окно было в три раза меньше, и сценарий с ним не проходил.
            // Строка приходит раньше отчёта оболочки: пространство разбирается
            // внутри `run`, а не после него.
            Step::AtLeast("space released, ", 17, 15_000),
            Step::Await("exited with code 0", 15_000),
            // Программа, которой сказали, что делать: путь приходит аргументом,
            // а не зашит в неё. Числа точные и это намеренно — «что-то
            // посчиталось» выглядело бы так же, как «посчиталось не то», а файл
            // лежит в этом же репозитории и меняется вместе со сценарием.
            Step::Line("run /bin/wc /README.TXT"),
            Step::Await("wc: 18 lines, 143 words, 853 bytes in /README.TXT", 30_000),
            // И вторая половина: размер, взятый `seek`-ом к концу, совпал с
            // числом прочитанных байт. Два числа получены разными путями, и
            // сходятся они только если `seek` действительно двигает позицию.
            Step::Await("wc: size from seek matches the bytes read", 15_000),
            // Без аргумента программа считает стандартный ввод — с фазы 38b
            // это не заглушка «usage», а вторая половина её смысла. Здесь она
            // получает его из канала: «сколько строк напечатала другая
            // программа» — вопрос, на который до каналов ответить было нечем.
            Step::Line("run /bin/hello | /bin/wc"),
            Step::Await("bytes in standard input", 30_000),
            // Перечисление каталога — тоже из третьего кольца. До Phase 20
            // `open` на каталоге отказывал, и `ls` мог существовать только
            // внутри ядра; теперь это программа, видящая ровно то, что
            // разрешено её учётной записи.
            Step::Line("run /bin/ls /bin"),
            Step::Await("hello", 30_000),
            // Каталог `/` содержит и файлы, и подкаталоги — на нём видно, что
            // тип записи доезжает до программы, а не додумывается ею по имени.
            Step::Line("run /bin/ls /"),
            Step::Await("bin/", 30_000),
            Step::Await("ls: ", 15_000),
            // Приглашения ждём отдельно, а не шлём команду сразу за последней
            // строкой программы: пока та не закончилась, набранное достаётся
            // **ей**, и разбирать его будет уже оболочка — правильно, но на
            // такт позже. Ожидание убирает лишний источник неопределённости из
            // сценария, который проверяет не это.
            Step::Await("freeos> ", 15_000),
            // Вторая ломается нарочно. До Phase 12a обращение по нулевому
            // адресу означало панику ядра и остановку машины.
            Step::Line("run /bin/crash"),
            Step::Await("user        : killed by", 30_000),
            // Снятая отказом программа возвращается в ядро не оттуда, откуда
            // уходила, и уборку на этом пути легко потерять — поэтому та же
            // строка проверяется и здесь.
            Step::AtLeast("space released, ", 17, 15_000),
            Step::Await("killed by the kernel", 15_000),
            // Третья читает память ядра по адресу, который в её собственных
            // таблицах есть: корень программы — копия ядерного. Отказ здесь
            // означает не «страницы нет», а «страница ядра из третьего кольца
            // не отдаётся», и проверяется этим ровно то, что копирование
            // корня не раздало прав.
            Step::Line("run /bin/peek"),
            Step::Await("about to read kernel memory", 30_000),
            Step::Await("user        : killed by", 15_000),
            Step::Await("killed by the kernel", 15_000),
            // Две программы сразу. Обе запускаются в фоне, то есть оболочка не
            // ждёт ни одну, — и это первая проверка: приглашение возвращается
            // немедленно. Дальше в журнале идут строки обеих программ вперемежку,
            // каждая со своим номером задачи: чередование доказывает, что
            // исполняются они по очереди, а не одна после другой.
            Step::Line("run -b /bin/count"),
            // Номер первой копии запоминается, и её завершение потом ищется по
            // нему. Без этого шаг ждал «какое-нибудь `: done`» — и терялся,
            // когда байты второй команды доходили рвано: первая копия успевала
            // закончиться раньше, чем стартовала вторая, её строка оказывалась
            // позади курсора, и прогон падал на исправной системе.
            Step::Capture("started as #", 15_000),
            Step::Line("run -b /bin/count"),
            Step::Capture2("started as #", 15_000),
            // Оболочка отвечает, пока программы считают.
            Step::Line("echo shell-alive"),
            // Обе досчитали до конца и обе завершились — каждая по своему
            // номеру и где бы её строка ни оказалась. Порядок здесь не
            // утверждается намеренно: какая копия закончит первой, решает
            // планировщик, и под нагрузкой это бывает вторая.
            Step::AwaitAny("count {}: done", 30_000),
            Step::AwaitAny("count {2}: done", 30_000),
            // Ответ оболочки проверяется по всему журналу, а не ожиданием в
            // строку, и это не послабление. С вытеснением очерёдность «ответ
            // оболочки — строки программ» перестала быть определённой: кто
            // первым получит квант, тот первым и напечатает. Ожидание же ищет
            // только вперёд, и сценарий, требующий определённого порядка там,
            // где его нет, падал бы через раз. Две ведущие пробела отличают
            // ответ команды от эха набранной строки.
            Step::Expect("  shell-alive"),
            // На системе, загруженной с носителя, учётных записей нет вовсе:
            // корень — образ initrd. Сеанс идёт от root, и проверки прав никому
            // ни в чём не отказывают. Это состояние обязано быть видно, а не
            // подразумеваться, — иначе «права работают» и «права молчат»
            // выглядят одинаково.
            Step::Line("whoami"),
            Step::Await("root (uid 0 gid 0)", 15_000),
            // И главное: после всего этого система жива и отвечает.
            Step::Line("echo still-alive"),
            Step::Await("still-alive", 15_000),
            Step::Shot("userspace"),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
            // Эти строки печатаются ровно тогда, когда изоляция не сработала:
            // программа дочитала до конца память ядра, либо ядро само нашло
            // окно программы в своих таблицах.
            Step::Absent("the read succeeded"),
            Step::Absent("user        : WARNING"),
        ],
    },
    Scenario {
        name: "mmap",
        about: "Программа просит память, получает её, и после неё свободных кадров столько же.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Прогон на мегабайте — до всякого счёта, и он тут не «на всякий
            // случай». Первый запуск любой программы стоит того, что запуском
            // не является: файл читается с диска впервые, разбирается его ELF,
            // заводится задача. Сняв число кадров **после** такого запуска, а
            // не до, мы сравниваем два одинаковых состояния, и расхождение
            // может означать только одно — двести пятьдесят шесть мегабайт
            // вернулись не полностью. Иначе проверка ловила бы разогрев и
            // называла бы его утечкой.
            Step::Line("run /bin/memtest 1"),
            Step::Await("memtest: the pool refused 500 MiB", 180_000),
            Step::Await("exited with code 0", 15_000),

            // Вот отсюда и считаем.
            Step::Line("mem"),
            Step::Capture("  frames   ", 15_000),

            // Двести пятьдесят шесть мегабайт. Выделяются они жадно, то есть
            // ядро действительно занимает и обнуляет 65 536 кадров, прежде чем
            // вернуть адрес, — отсюда и минута с лишним на ожидание: под QEMU
            // без ускорения это заметная работа, и её видно. Ленивая выдача
            // была бы быстрее, но требует обработчика отказа страницы, а он —
            // фаза 41.
            Step::Line("run /bin/memtest"),
            Step::Await("memtest: mapped 256 MiB at ", 180_000),
            // Узор записан и прочитан обратно. Это и есть «память есть»:
            // страница, которой нет, не пережила бы записи, а страница, ведущая
            // на чужой кадр, не пережила бы сверки.
            Step::Await("memtest: 256 MiB written and read back, no mismatches", 300_000),
            Step::Await("memtest: unmapped", 60_000),
            // Предел на задачу существует и отвечает своим кодом, а не общим
            // «памяти нет»: у машины её в этот момент сколько угодно.
            Step::Await("memtest: the limit refused 64 GiB", 30_000),
            // А теперь отказ по другой причине — и он тут ради того, что
            // случилось до него. Пятьсот мегабайт проходят предел задачи и
            // упираются в пул: ядро успевает занять и разложить почти
            // полгигабайта, прежде чем упереться, и всё это обязано вернуться.
            // Это единственный за прогон случай, когда исполняется откат
            // наполовину сделанного выделения, и ловит его сверка кадров ниже:
            // не вернись оттуда хоть страница — числа разойдутся.
            Step::Await("memtest: the pool refused 500 MiB", 120_000),
            Step::Await("exited with code 0", 30_000),

            // Главное утверждение фазы, и проверить его больше нечем: столько
            // же свободных кадров, сколько было. Число подставляется то самое,
            // что снято выше, — «примерно столько же» выглядело бы точно так
            // же, как утечка в одну страницу на вызов.
            Step::Line("mem"),
            Step::Await("  frames   {} of", 15_000),

            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
            // Ядро печатает это, если таблицы разошлись с учётом областей: при
            // возврате нашлось не столько страниц, сколько выдавалось.
            Step::Absent("user        : WARNING"),
        ],
    },
    Scenario {
        name: "hugepages",
        about: "Двести пятьдесят шесть мегабайт легли ста двадцатью восемью блоками по 2 МиБ, и все вернулись.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        // Обе архитектуры, и это не симметрия ради симметрии. Блочная запись —
        // единственное место фазы, где код у них **разный** по существу: на
        // x86-64 крупная страница это бит PS в записи каталога, на AArch64 —
        // дескриптор блока на втором уровне, с другим форматом и другими
        // правилами обхода. Проверить одну и поверить за вторую здесь нельзя.
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        // Два гигабайта, и вот зачем — подробно сказано у самого поля. Коротко:
        // на 512 МиБ непрерывного и выровненного отрезка в 2 МиБ может не
        // найтись, ядро честно разложит просьбу обычными страницами, и сценарий
        // пройдёт, ничего из того, ради чего заведён, не проверив.
        memory: "2G",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),

            // Счёта свободных кадров целиком здесь нет, и это решение, а не
            // упущение. Сначала он тут стоял — как в сценарии `mmap`: снять
            // число до, сверить после. На живой машине он оказался шумным:
            // службы поднимаются и падают по своему расписанию, а каждая стоит
            // ровно 789 кадров — 784 страницы окна и 5 таблиц. Первый прогон на
            // эти самые 789 и разошёлся: между двумя замерами успел завершиться
            // `dhcp`. Сравнивались два разных состояния системы, а не «до
            // программы» и «после программы».
            //
            // Заменено на то, что от чужого расписания не зависит вовсе: на
            // числа, которые ядро печатает про **эту** область. Они точные, они
            // про неё одну, и подделать их нечем.

            Step::Line("run /bin/memtest 256 huge"),

            // Центральное утверждение фазы, и другого способа его проверить нет
            // вовсе: со стороны программы крупная страница неотличима от
            // обычной — тот же адрес, та же память, — и говорит о том, что
            // вышло, только ядро. 256 МиБ / 2 МиБ = 128 блоков.
            //
            // Шаг стоит **до** ожиданий самой программы: строку печатает ядро
            // внутри `mmap`, то есть раньше, чем memtest успевает сказать
            // «mapped», а ожидание ищет только вперёд от курсора.
            Step::AtLeast("  user        : mmap: ", 128, 300_000),
            // И ни одной обычной страницы в остатке. Отступление на 4 КиБ —
            // законный исход вообще, но не на простаивающей машине с двумя
            // гигабайтами: здесь оно означало бы, что поиск выровненного
            // непрерывного отрезка не находит того, что в пуле заведомо есть.
            Step::Expect("huge and 0 small pages"),

            // Память при этом настоящая: узор, зависящий от смещения, записан и
            // прочитан обратно. Ошибка в блочной записи — это чаще всего
            // отображение, где соседние блоки ведут на один физический отрезок,
            // и ловится она именно сверкой, а не тем, что запись не упала.
            //
            // Ожидание щедрое по той же причине, что и в сценарии `mmap`:
            // выделение жадное, 256 МиБ действительно занимаются и обнуляются.
            Step::Await("memtest: 256 MiB written and read back, no mismatches", 300_000),
            Step::Await("memtest: unmapped", 60_000),
            // Код выхода проверяется явно, и это не формальность. Программа
            // умеет провалить собственную проверку и сказать об этом строкой,
            // которой сценарий не ждёт: тогда всё ожидаемое сбудется, а провал
            // останется в журнале незамеченным. Один раз в этой фазе так и
            // вышло — проверка «пул отказал в 500 МиБ» на машине с двумя
            // гигабайтами ложна, и сценарий прошёл бы поверх её провала.
            Step::Await("exited with code 0", 60_000),

            // Самый острый край фазы, и проверяется он отдельным запуском.
            //
            // Область возвращается двумя разными путями, и они не заменяют друг
            // друга. `munmap` идёт по записям, зная длину области, — его только
            // что прошли выше. Уборка при выходе не знает ничего: она обходит
            // поддерево таблиц и разбирает всё, что найдёт, обязана узнать блок
            // по одному биту и вернуть за него 512 кадров. До этой фазы она не
            // умела — печатала отказ и шла дальше, теряя два мегабайта
            // навсегда, молча и не роняя ничего.
            //
            // Отсюда `keep`: программа уходит, не прибравшись, и число в строке
            // ядра — единственное доказательство. Восемь мегабайт, а не двести
            // пятьдесят шесть, потому что проверяется не масштаб, а
            // арифметика: 4 блока по 512 кадров плюс окно программы (768
            // страниц образа и 16 стека) — ровно 2832.
            Step::Line("run /bin/memtest 8 huge keep"),
            Step::Await("memtest: leaving the region to the teardown", 60_000),
            Step::Await("  user        : space released, 2832 pages", 60_000),

            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
            // Ядро печатает это, если при возврате нашлось не столько страниц,
            // сколько выдавалось.
            Step::Absent("user        : WARNING"),
            // Собственные отказы разборщика таблиц. Каждый из них означает, что
            // до блока добрались кодом, который блоков не понимает: первый —
            // при возврате пространства целиком, второй — при `munmap`.
            //
            // Формулировок у первого две, по одной на архитектуру, и это не
            // небрежность, а то же самое, ради чего сценарий гоняется на обеих:
            // на x86-64 это «huge page», на AArch64 — «block mapping», потому
            // что там это и называется блоком. Проверять надо обе: сценарий с
            // одной строкой на второй архитектуре молчал бы всегда.
            Step::Absent("refusing to free a huge page"),
            Step::Absent("refusing to free a block mapping"),
            Step::Absent("refusing to unmap part of a huge page"),
        ],
    },
    Scenario {
        name: "filemap",
        about: "Файл отображён в память и читается по обращению, а не целиком.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Пока это проверка механизма, а не масштаба: файл берётся тот, что
            // и так лежит в образе. Настоящая проверка фазы — файл, который в
            // память не помещается, — стоит отдельно и на установленном диске,
            // потому что для него нужен раздел, куда хост может этот файл положить.
            Step::Line("run /bin/filemap /bin/sysupdate"),
            Step::Await("filemap: /bin/sysupdate is ", 30_000),
            // Сумма по каждому сотому байту. Она ничего не доказывает сама по
            // себе — доказывает то, что программа дошла до конца файла, ни разу
            // не получив отказ, которого не поправила подкачка.
            Step::Await("filemap: checksum over every 100th byte is ", 60_000),
            // А вот это и есть утверждение фазы: страницы читались по одной, по
            // мере обращения. Ноль здесь означал бы, что отображение наполнили
            // заранее, то есть что подкачки не было.
            //
            // Проверка стоит **до** строки программы, а не после, и это не
            // вкусовщина: печатает её `munmap` в ядре, то есть она приходит
            // раньше, чем программа успевает сказать «unmapped», а ожидание
            // ищет только вперёд от курсора.
            Step::AtLeast("  user        : file mapping released, ", 1, 15_000),
            Step::Await("filemap: unmapped", 15_000),
            Step::Await("exited with code 0", 15_000),
            // Фаза 54: образ программы читается по обращению. `big` несёт шесть
            // мегабайт данных — вдвое больше прежнего окна в три, — и может
            // запуститься только если ядро не читает файл заранее. Свёртка
            // посчитана на хосте (см. `big.rs`); строку с числом прочитанных
            // страниц печатает ядро при выходе, и их не меньше, чем страниц
            // данных: обход трогает каждую, а держит ядро только четыре
            // мегабайта из шести, так что часть страниц читается дважды.
            Step::Line("run /bin/big"),
            Step::Await("big: data is 6291456 bytes in the image", 60_000),
            Step::Await("big: checksum over every 1000th word is 13091774967880311519", 300_000),
            Step::Await("big: spot checks passed", 30_000),
            Step::AtLeast("  user        : program image released, ", 1536, 30_000),
            Step::Await("exited with code 0", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
            Step::Absent("user        : WARNING"),
        ],
    },
    Scenario {
        name: "stdin",
        about: "Программа читает набранное, управляет терминалом и снимается по Ctrl+C.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Набор **во время загрузки** — то, на чём приёмник ломался, и
            // единственный способ создать этот промежуток надёжно, а не когда
            // повезёт.
            //
            // Якорь — строка о поднятом приёме: раньше неё вычитывать некому по
            // построению. После неё и до появления стола ядро печатает два
            // десятка строк, каждая из которых прокручивает экранную консоль, а
            // прокрутка — это до тысячи перерисованных ячеек с запрещёнными
            // прерываниями: замерено 663 мс на одну строку в отладочной сборке.
            // Двадцать два байта команды уходят в линию за сорок миллисекунд и
            // приезжают целиком внутрь такого промежутка. Приёмное FIFO — 16
            // байт у 16550, 32 у PL011; без вычитывания изнутри печати половина
            // команды терялась бы гарантированно.
            //
            // Строка выбрана общей для обеих архитектур: за ней стоит COM1 на
            // x86-64 и PL011 на AArch64.
            Step::Await("serial in   : ", BOOT),
            Step::Raw(b"echo typed-during-boot\n"),
            Step::Await("freeos> ", BOOT),
            // Команда, набранная в глухой промежуток, дошла целиком и
            // исполнилась. Это и есть утверждение: не «счётчик красив», а
            // «система приняла ввод тогда, когда раньше не принимала».
            Step::Await("  typed-during-boot", 30_000),
            Step::Line("run /bin/ask"),
            // Размер окна пришёл из ядра, а не выдуман программой: она не знает
            // ни размера экрана, ни размера глифа, ни того, есть ли графика.
            Step::Await("ask: window is ", 30_000),
            // Ядро назвало разобранные последовательности. Без этих строк
            // «терминал выполнил команду» и «терминал напечатал её текстом»
            // выглядели бы одинаково — а снимок экрана доказательством не
            // является (правило дома).
            Step::Await("term        : CSI 2J", 15_000),
            Step::Await("term        : CSI 1;1H", 15_000),
            Step::Await("ask: name?", 15_000),
            // Вот оно, главное утверждение фазы: набранная строка ушла не в
            // редактор оболочки, а **в программу**, и вернулась её ответом.
            Step::Line("roman"),
            Step::Await("ask: hello, roman", 15_000),
            Step::Await("ask: say something?", 15_000),
            // Второе чтение из того же потока: позиция в нём не теряется между
            // вызовами, и число байт точное — «что-то прочиталось» выглядело бы
            // так же, как «прочиталось не то».
            Step::Line("still here"),
            Step::Await("ask: heard 10 bytes: still here", 15_000),
            Step::Await("ask: done", 15_000),
            Step::Await("exited with code 0", 15_000),
            // Ввод вернулся оболочке: программа закончилась, и терминал больше
            // никому не принадлежит.
            Step::Await("tty         : foreground released", 15_000),
            Step::Line("echo shell-has-input"),
            Step::Await("  shell-has-input", 15_000),
            // Ввод в разгар печати — то, на чём приёмник и ломался. `help`
            // печатает три десятка строк подряд, а следующая команда шлётся,
            // **не дожидаясь приглашения**: её байты приходят ровно тогда,
            // когда ядро занято выводом и держит прерывания запрещёнными. Без
            // спасения приёмника отсюда и брались полкоманды: FIFO у 16550
            // шестнадцать байт, и вычитывать его в этот промежуток некому.
            //
            // Нарушение правила «команду не слать, не дождавшись приглашения»
            // здесь намеренное и единственное на весь стенд: оно и есть
            // проверка. Набранное не теряется, потому что события копятся в
            // очереди ввода (128 штук — вчетверо больше, чем нужно этой
            // строке), и оболочка разбирает их, когда допечатает.
            Step::Line("help"),
            Step::Line("echo flood-survived"),
            Step::Await("  flood-survived", 30_000),
            // Счётчик прочитанного программами — не украшение: он отличает
            // «программа получила байты» от «программа напечатала то, что и так
            // знала».
            Step::Line("input"),
            // Ни одного потерянного байта на входе. Утверждение сильное и
            // проверяет ровно то, чего до сих пор не видел никто: приёмник
            // UART теряет байт, если тот пришёл в полное FIFO, — а ядро в этот
            // момент печатало строку с запрещёнными прерываниями. Снаружи
            // потеря выглядит как «система не приняла команду»: эхо обрывается
            // на середине, ответа нет, и отличить это от ошибки в разборе или
            // в оболочке нечем. Сценарий `kill` падал так трижды, прежде чем
            // ядро научилось про это говорить.
            Step::AtMost("  serial   ", 0, 15_000),
            // А это — что ноль выше получен не даром. Хотя бы один байт вынут
            // из приёмника изнутри печати: без этой строки «дефекта больше нет»
            // и «в этот раз повезло» выглядят одинаково.
            // А это — что ноль выше получен не даром: байты действительно
            // вынуты из приёмника изнутри печати. Без этой строки «дефекта
            // больше нет» и «в этот раз повезло» выглядели бы одинаково.
            Step::AtLeast("  rescued  ", 1, 15_000),
            // И спасённое не потеряно уже нами: кольцо не переполнялось.
            Step::AtMost("  spilled  ", 0, 15_000),
            // Семнадцать байт — «roman\n» и «still here\n»; предел ниже с
            // запасом, потому что проверяется не арифметика, а то, что счётчик
            // вообще двигается.
            Step::AtLeast("  terminal line mode, ", 15, 15_000),
            // Ctrl+C снимает программу переднего плана. `/bin/forever` не делает
            // ни одного системного вызова после первой строки, то есть ждать
            // его добровольного возврата бессмысленно.
            Step::Line("run /bin/forever"),
            Step::Await("this program never ends on its own", 30_000),
            Step::Raw(&[3]),
            Step::Await("killed by request", 30_000),
            // И система жива: снятие программы переднего плана вернуло оболочке
            // и ввод, и приглашение.
            Step::Line("echo still-alive"),
            Step::Await("still-alive", 15_000),
            Step::Shot("stdin"),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "vector",
        about: "Векторные регистры принадлежат задаче: две программы не видят чисел друг друга.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        // `-cpu max` — не украшение. С процессором по умолчанию QEMU не
        // объявляет `XSAVE`, и ядро уходит на путь `FXSAVE`: проверялась бы
        // ровно та половина, которой на современной машине не бывает. Здесь
        // объявлено всё, что эмулятор умеет, — то есть тот путь, по которому
        // пойдёт настоящее железо.
        extra: &["-cpu", "max"],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Ядро обязано сказать, чем именно оно сохраняет состояние: на
            // машине без XSAVE это FXSAVE, и размер области там другой.
            // Строка заодно доказывает, что фаза вообще включена.
            Step::Expect("  fpu         : "),
            // Две копии одной программы с разными константами. Проверка внутри
            // каждой: записать восемь векторных регистров, уступить процессор,
            // сверить. До этой фазы такой тест красный — переключение задач
            // сохраняло целочисленный контекст и не сохраняло ничего больше.
            Step::Line("run -b /bin/vec 1111111111"),
            Step::Capture("started as #", 15_000),
            Step::Line("run -b /bin/vec 2222222222"),
            Step::Await("started as #", 15_000),
            // Обе строки ищутся **где угодно** в выводе, и вторая тоже. Порядок,
            // в котором финишируют две равноправные задачи, ничем не закреплён:
            // он зависит от того, куда лягут тики планировщика, а те — от
            // скорости всего остального, вплоть до того, как отображён кадровый
            // буфер. Ровно это и произошло, когда буфер стал некэшируемым вместо
            // Device: вывод ускорился, задачи поменялись местами, и сценарий
            // упал, хотя обе программы прошли все две тысячи проверок и ни одна
            // не напечатала MISMATCH. Проверяется здесь принадлежность
            // векторных регистров задаче, а не очерёдность финиша.
            Step::AwaitAny("vec 1111111111: 2000 checks passed", 120_000),
            Step::AwaitAny("vec 2222222222: 2000 checks passed", 120_000),
            Step::Line("echo still-alive"),
            Step::Await("still-alive", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
            // Единственная строка, которую программа печатает при расхождении.
            // Её отсутствие и есть результат фазы.
            Step::Absent("MISMATCH"),
        ],
    },
    Scenario {
        name: "preempt",
        about: "Программа без единого системного вызова больше не владеет машиной.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Вытеснение обязано быть включено, а не собрано: строку печатает
            // сам планировщик, читая свой флаг.
            Step::Line("tasks"),
            Step::Await("preemption : on,", 15_000),
            // Программа уходит в фон и в цикл, из которого не выходит наружу
            // ни разу: ни `yield`, ни `write`, ни `uptime`.
            Step::Line("run -b /bin/spin"),
            Step::Await("started as #", 15_000),
            Step::Await("no system calls from here on", 30_000),
            // Вот и всё доказательство. Строка ниже появляется в журнале между
            // двумя строками программы, а между ними программа не отдавала
            // процессор — значит его отобрали. При кооперативном планировщике
            // ответа не было бы вовсе: оболочка не получила бы ни такта, пока
            // счёт не кончится.
            Step::Line("echo preempted-ok"),
            // Два ведущих пробела — это ответ команды, а не эхо набранной
            // строки: доказательством должно быть, что оболочка команду
            // **выполнила**.
            Step::Await("  preempted-ok", 30_000),
            // Дальше ждём конца счёта, и только потом набираем ещё что-нибудь:
            // шаги идут в порядке **строк журнала**, а не событий. Команда,
            // отправленная сюда, ответила бы уже после «done», и поиск ушёл бы
            // за нужную строку — то есть искал бы её второе появление.
            Step::Await("never yielded once", 120_000),
            Step::Line("echo still-alive"),
            Step::Await("still-alive", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "smp",
        about: "Четыре процессора: две жгущие программы исполняются одновременно, а не по очереди.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &["-smp", "4"],
        steps: SMP_STEPS,
    },
    Scenario {
        name: "smp-gicv3",
        about: "Четыре процессора на GICv3: у каждого свой redistributor, найденный по сродству.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        // Только AArch64: на x86-64 GIC не существует.
        arches: &[Arch::Aarch64],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        // Прошивка QEMU описывает redistributor'ы одним диапазоном и не говорит,
        // какой кому принадлежит: ядро обязано найти их по `GICR_TYPER`. Без
        // этого сценария путь проверялся бы только на GICv2, где redistributor'ов
        // нет вовсе.
        extra: &["-smp", "4", "-machine", "gic-version=3"],
        steps: SMP_STEPS,
    },
    Scenario {
        name: "kill",
        about: "Программу, которая сама не кончится, снимают командой — и её память возвращается в пул.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            Step::Line("run -b /bin/forever"),
            // Номер задачи не выдумывается сценарием, а берётся из строки,
            // которую напечатала система. Константа здесь уже подводила: одна
            // служебная задача, заведённая в ядре, сдвинула нумерацию, и
            // сценарий стал бы снимать не ту задачу.
            Step::Capture("started as #", 15_000),
            Step::Await("this program never ends on its own", 30_000),
            // Система жива, пока программа крутится: это уже доказано сценарием
            // `preempt`, но здесь важно сравнение с тем, что будет после kill.
            Step::Line("echo alive-before-kill"),
            Step::Await("  alive-before-kill", 30_000),
            Step::Line("kill {}"),
            // Оболочка отвечает только «просьба принята». О самом снятии
            // говорит ядро, и говорит из другой задачи — той, которую снимают.
            // Без префикса `  kill: ` намеренно. Строку печатает оболочка, а
            // между её началом и концом в ту же линию успевает написать ядро —
            // «killed by request» приходит из **другой** задачи и замка вывода
            // оболочки не берёт. Строка от этого рвётся пополам, и ожидание
            // целого куска превращается в лотерею по таймингам. Проверяется
            // здесь то же самое: команда сказала, что просит задачу
            // остановиться.
            Step::Await("#{} asked to stop", 15_000),
            Step::Await("user        : killed by request, task #{}", 15_000),
            // Память вернулась в пул целиком, тем же путём, что и после отказа:
            // 128 страниц образа, 8 стека и четыре таблицы.
            Step::AtLeast("space released, ", 17, 15_000),
            Step::Await("#{} /bin/forever: killed by request", 15_000),
            // Снять её второй раз нельзя, и отказ объясняет почему.
            Step::Line("kill {}"),
            Step::Await("kill: task #{} has already finished", 15_000),
            // Оболочка — задача, но не программа: возвращаться ядру некуда, и
            // делать вид, что можно, было бы хуже отказа. Номер её здесь не
            // нужен: любой живой не-программы достаточно, а холостая задача
            // с номером 0 есть всегда.
            Step::Line("kill 0"),
            Step::Await("kill: task #0 is not running a program", 15_000),
            Step::Line("kill 99"),
            Step::Await("kill: task #99 does not exist", 15_000),
            Step::Line("echo still-alive"),
            Step::Await("  still-alive", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "sleep",
        about: "Ждущая программа выходит из очереди на исполнение, и машине становится нечего делать.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            Step::Line("run -b /bin/nap"),
            Step::Await("started as #", 15_000),
            // Поиск по всему выводу, а не вперёд от курсора: программа запущена
            // фоном, и печатает она **сама**, не спросив оболочку. Чья строка
            // ляжет раньше — «started as #» оболочки или «sleeping…»
            // программы, — решает планировщик. Под нагрузкой это оказалась
            // программа, её строка ушла за курсор, и сценарий упал, требуя
            // порядка, которого система не обещает.
            Step::AwaitAny("asking for nothing", 30_000),
            // Пауза обязательна: строку программа печатает **до** того, как
            // уснуть, и между этими двумя событиями её успевает вытеснить
            // разбуженная вводом оболочка. Без паузы `tasks` заставал программу
            // ещё готовой к исполнению — и сценарий падал, доказывая ровно
            // обратное тому, что проверяет. Полсекунды из трёх, которые она
            // спит, ничего не стоят.
            Step::Wait(500),
            // Вот и вся фаза одной строкой: спящая программа не `ready`, а
            // `blocked`. До Phase 13d ждущая программа была неотличима от
            // считающей — она и была считающей, просто уступала процессор.
            Step::Line("tasks"),
            Step::Await("program  blocked", 15_000),
            Step::Await("woke after", 30_000),
            // И вторая половина того же утверждения: пока все ждут, процессор
            // стоит. Строку печатает планировщик, считая тики, заставшие
            // холостую задачу.
            Step::Line("tasks"),
            Step::Await("idle       :", 15_000),
            Step::Line("echo still-alive"),
            Step::Await("  still-alive", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            // Система по-прежнему останавливается по `exit`: задача опроса USB
            // не заканчивается никогда, и не будь она объявлена служебной, это
            // ожидание не наступило бы.
            Step::Await("All tasks finished", 15_000),
            Step::Absent("KERNEL PANIC"),
            // Ноль процентов простоя означал бы, что кто-то по-прежнему крутится
            // вхолостую, — то есть что фаза не сделала ничего.
            Step::Absent("idle       : 0% of"),
        ],
    },
    Scenario {
        name: "clock",
        about: "Система знает время суток: часы прошивки доезжают до ядра и идут дальше сами.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Время системы считает счётчик, который идёт сам, а не тики
            // прерываний. Строка про него — первое, что обязано быть в журнале:
            // без неё все проверки ниже проходили бы и на прежних, отстающих
            // часах.
            Step::Await("  monotonic   : ", BOOT),
            // Первое утверждение фазы: время, с которым загрузилась система, —
            // настоящее. Сверяется с часами хоста, потому что все остальные
            // проверки прошли бы и над выдуманной датой.
            //
            // Допуск в десять секунд — не осторожность, а рабочий предел.
            // Пока время считалось доставленными тиками, здесь набегало −23 с
            // на отладочной сборке x86-64, и такой допуск ловит возврат к тому
            // поведению. Меньше ставить нельзя: часы прошивки округлены до
            // секунды, и пара секунд разницы законна.
            Step::Clock("clock       : ", 10, BOOT),
            Step::Await("freeos> ", BOOT),
            // Второе утверждение: часы идут. `date` печатает не то число,
            // которое пришло от прошивки, а сумму точки отсчёта и тиков
            // таймера, и она обязана сойтись с хостом так же.
            Step::Line("date"),
            Step::Await("  local  ", 15_000),
            Step::Clock("epoch  ", 10, 15_000),
            // И третье: часы идут **в ногу** с настоящими. Десять секунд
            // бездействия, и второй замер обязан сойтись с хостом так же, как
            // первый. Проверка не лишняя: часы, которые идут вдвое медленнее,
            // проходят любую однократную сверку — расхождение накапливается,
            // и на первой же минуте его ещё не видно.
            Step::Wait(10_000),
            Step::Line("date"),
            Step::Clock("epoch  ", 10, 15_000),
            // Корень здесь — образ initrd на FAT, и время файла на нём
            // неизвестно. Прочерк вместо даты — это утверждение, а не пропуск:
            // формат хранит дату, но читатель ядра её не разбирает, и показать
            // эпоху FAT, выдав её за метку файла, было бы хуже.
            Step::Line("stat /bin/hello"),
            Step::Await("  mtime  unknown", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "iso",
        about: "Система грузится с загрузочного ISO — того же файла, что отдают человеку.",
        target: Target::Iso,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Это утверждение фазы целиком: прошивка нашла на носителе
            // загрузочную запись El Torito и пошла по ней. Проверить его иначе
            // нечем — ISO, который читается, но не грузится, выглядит точно так
            // же, как рабочий, вплоть до этой строки. Ровно так и выяснилось,
            // что двух записей в каталоге мало: носитель виделся как `FS0:
            // CDROM`, а прошивка уходила в UEFI Shell.
            Step::Await("FreeOS bootloader", BOOT),
            Step::Await(crate::version::KERNEL_BANNER, 60_000),
            Step::Await("freeos> ", 60_000),
            // Система работает целиком, а не «дошла до приглашения»: initrd
            // смонтирован, файлы читаются. Диска у неё при этом нет вовсе —
            // носитель только для чтения, и это тот самый режим, в котором её
            // будут пробовать.
            Step::Line("ls /bin"),
            Step::Await("hello", 15_000),
            Step::Line("run /bin/hello"),
            Step::Await("hello from userspace", 30_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "gicv3",
        about: "Прерывания работают и на GICv3: адреса берутся из MADT, а не из констант.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        // Только AArch64: на x86-64 GIC не существует.
        arches: &[Arch::Aarch64],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        // Свойство накапливается к уже заданной машине `virt`. Версия по
        // умолчанию — та, что выбрал QEMU; здесь она задана явно, потому что
        // проверяется именно другая ветка кода.
        extra: &["-machine", "gic-version=3"],
        steps: &[
            // Первое: версия опознана и распечатана. До Phase 22 здесь стояло
            // «unsupported interrupt controller (Unknown)» — ровно то, что
            // увидел человек, запустивший систему в VirtualBox на Apple Silicon.
            Step::Await("interrupts  : GICv3", BOOT),
            // Второе, и главное: прерывания доходят. Тик таймера — единственное,
            // чем это проверяется: без него планировщик стоит, а оболочка не
            // отвечает вовсе.
            Step::Await("freeos> ", BOOT),
            Step::Line("uptime"),
            Step::Await("timer ticks", 15_000),
            // Ввод по UART тоже приходит прерыванием, и то, что оболочка
            // ответила на команду выше, это и доказывает.
            Step::Line("echo gicv3-ok"),
            Step::Await("gicv3-ok", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
            Step::Absent("NO TICKS"),
        ],
    },
    Scenario {
        name: "serial-cr",
        about: "Терминал, присылающий один возврат каретки, работает как Enter.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Именно этот путь прежний стенд проверить не мог: канал Windows
            // съедал 0x0D, и до гостя не доезжало ничего.
            Step::Raw(b"echo cr-ok\r"),
            Step::Await("cr-ok", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "image",
        about: "Система грузится с диска, размеченного нашим же кодом: GPT и FAT32.",
        target: Target::Image,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // На VVFAT таблицы разделов не существует вовсе — QEMU синтезирует
            // её на лету. Значит, разметку проверяет только этот сценарий.
            Step::Await(crate::version::KERNEL_BANNER, BOOT),
            Step::Await("freeos> ", 60_000),
            Step::Line("echo image-ok"),
            Step::Await("image-ok", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "install",
        about: "Установщик проходит все экраны, пишет систему на чистый диск и заводит загрузочную запись.",
        target: Target::Installer,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        // Сценарий **намеренно** перезагружает машину: проверяется, куда она
        // пойдёт после установки, а с `-no-reboot` она вместо этого погасла бы.
        reboots: true,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("FreeOS installer", BOOT),
            Step::Await("[disk]", 30_000),
            // Список дисков отсортирован так, что установочный носитель уходит
            // вниз: под курсором по умолчанию — цель установки.
            Step::Wait(1_500),
            Step::Shot("01-language"),
            Step::Key("ret"),
            Step::Wait(700),
            Step::Shot("02-welcome"),
            Step::Key("ret"),
            Step::Wait(700),
            Step::Shot("03-disk"),
            Step::Key("ret"),
            Step::Wait(700),
            Step::Shot("04-account"),
            Step::Type("roman"),
            Step::Key("tab"),
            Step::Type("freeos"),
            Step::Key("tab"),
            Step::Type("freeos"),
            Step::Shot("05-account"),
            Step::Key("ret"),
            Step::Wait(700),
            Step::Shot("06-keyboard"),
            Step::Key("ret"),
            Step::Wait(700),
            // Пролистать список, а не согласиться с первым пунктом: выбор,
            // который никто не двигал, не доказывает, что он двигается.
            Step::Repeat("down", 3),
            Step::Shot("07-timezone"),
            Step::Key("ret"),
            Step::Wait(700),
            Step::Shot("08-confirm"),
            // Курсор стоит на «нет»: подтверждение стирания диска не должно
            // происходить от случайного Enter.
            Step::Key("down"),
            Step::Key("ret"),
            // Загрузочная запись заводится в самом конце, до строки «finished»:
            // порядок шагов повторяет порядок строк, а не порядок событий.
            Step::Await("[install] boot entry Boot", 240_000),
            Step::Await("[install] BootOrder: FreeOS first", 15_000),
            Step::Await("[install] finished", 30_000),
            Step::Expect("[install] state: /etc/passwd"),
            Step::Wait(1_000),
            Step::Shot("09-done"),

            // И главное утверждение про запись: машина, перезагруженная с
            // **подключённым** установочным носителем, поднимается в
            // установленную систему, а не в установщик снова. Без записи
            // прошивка нашла бы носитель первым — ровно это и случилось в
            // релизе v0.1.57 на VirtualBox.
            Step::Key("ret"),
            Step::Await(crate::version::KERNEL_BANNER, BOOT),
            // Строка, которой у живой системы не бывает: корень на ext2 есть
            // только у установленной.
            Step::Await("root        : ext2 at LBA", 90_000),
            Step::Await("account     : /etc/passwd", 60_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "installed",
        about: "Установленная система находит свой диск, монтирует ext2, читает /etc и проверяет права.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("root        : ext2 at LBA", BOOT),
            // Первая загрузка после установки обязана застать том закрытым:
            // установщик помечает его используемым на время работы и чистым в
            // конце. Строка здесь проверяет вторую половину — а первую проверять
            // нечем, кроме прерванной установки, которой стенд не умеет.
            Step::Await("root        : volume was unmounted cleanly", 15_000),
            // Это и есть доказательство цепочки virtio-blk → GPT → ext2 → VFS:
            // ядро читает файл, который записал установщик.
            Step::Await("account     : /etc/passwd", 30_000),
            // Часовой пояс — из того же файла настроек, и он не нулевой: на
            // экране установщика список пролистан на три пункта вниз. Это
            // единственная проверка, которая связывает выбор человека при
            // установке с тем, что система показывает потом.
            Step::Await("timezone    : UTC+03:00 from /etc/system.cfg", 30_000),
            Step::Await("freeos> ", 30_000),
            // Меню рабочего стола на установленной системе: здесь корень ext2,
            // и «создать папку» действительно создаёт её — в отличие от живого
            // носителя, где хранить нечего. Каталог стола заводится по дороге,
            // вместе с домашним.
            // Стол начинается пустым, и это проверяется вслух. Прогон,
            // оборванный посередине, оставил бы на нём свою папку, и все
            // проверки ниже считали бы значки на один больше — а выглядело бы
            // это как поломка стола. Убрать её можно так:
            // `rm /home/roman/Desktop/<имя>` в гостевой оболочке либо новым
            // прогоном сценария `install`, который пересоздаёт диск.
            // Строка печатается при запуске стола, то есть **до** приглашения,
            // которого сценарий дождался выше, — поэтому проверка по всему
            // журналу, а не ожидание вперёд от курсора.
            Step::Expect("desktop     : icons 4, 0 from /home/roman/Desktop"),
            // Монитор системы запускается **вручную**, и это не забывчивость
            // сценария. С фазы 47b он программа (`/bin/sysmon`), а службой быть
            // не должен: открытие окна держит стол сотни миллисекунд, и всё,
            // что придёт в этот момент, рискует пропасть — так и написано в
            // `services`. До 47b окно заводило ядро само, и сценарий его просто
            // находил; с тех пор он **искал окно, которого никто не открывал**,
            // и был красным. Нашлось это в 48a, вместе с двумя такими же.
            Step::Line("run -b /bin/sysmon"),
            Step::Await("window      : 'System' at", 30_000),
            Step::Wait(1_500),
            // Окна убираются с дороги: меню стола открывается только там, где
            // стол виден, а щелчок по окну — это щелчок по окну. Монитор
            // сворачивается первым: он только что открылся и лежит поверх
            // оболочки, то есть её кнопка сейчас под ним.
            Step::Aim(Aim::Minimize("System")),
            Step::Click,
            Step::Await("desktop     : minimized 'System'", 15_000),
            Step::Aim(Aim::Minimize("Terminal")),
            Step::Click,
            Step::Await("desktop     : minimized 'Terminal'", 15_000),
            Step::Aim(Aim::Empty),
            Step::RightClick,
            Step::Await("desktop     : context menu at", 15_000),
            Step::Aim(Aim::ContextItem(0, 0)),
            Step::Click,
            Step::Await("desktop     : created 'New folder' in /home/roman/Desktop", 15_000),
            // Созданное обязано появиться **на столе**, а не только на диске:
            // четыре системных значка плюс один из каталога.
            Step::Await("desktop     : icons 5, 1 from /home/roman/Desktop", 15_000),
            Step::Aim(Aim::EmptyBelow),
            Step::Click,
            Step::Await("desktop     : context menu closed", 15_000),
            Step::Wait(2_000),
            Step::Shot("01-desktop-folder"),
            // Меню на самом значке: пункты у него другие — открыть,
            // переименовать, удалить. Значок пятый сверху, то есть первый после
            // системных.
            Step::Aim(Aim::Icon(4)),
            Step::RightClick,
            Step::Await("desktop     : context menu at", 15_000),
            Step::Expect("for entry"),
            Step::Wait(1_500),
            Step::Shot("02-entry-menu"),
            // «Переименовать» — второй пункт. Строка приходит заполненной
            // прежним именем; Ctrl+U стирает её целиком, дальше набирается
            // новое имя на настоящей клавиатуре.
            Step::Aim(Aim::ContextItem(1, 0)),
            Step::Click,
            Step::Wait(1_500),
            Step::Shot("03-rename"),
            Step::Key("ctrl-u"),
            Step::Type("Reports"),
            Step::Key("ret"),
            Step::Await(
                "desktop     : renamed '/home/roman/Desktop/New folder' to 'Reports'",
                15_000,
            ),
            Step::Await("desktop     : icons 5, 1 from /home/roman/Desktop", 15_000),
            // Меню закрывается **до** команды оболочке, и это не уборка за
            // собой: пока оно открыто, оно забирает весь ввод — в том числе
            // перевод строки, приехавший по серийной линии. Первый прогон
            // упал ровно на этом: `Enter` от команды `ls` попал в меню и
            // запустил «переименовать» второй раз.
            Step::Aim(Aim::EmptyBelow),
            Step::Click,
            Step::Await("desktop     : context menu closed", 15_000),
            Step::Line("ls /home/roman/Desktop"),
            Step::Await("Reports", 15_000),
            // Удаление спрашивает подтверждения: отменить его нечем, корзины в
            // системе нет. Отвечает на вопрос клавиша, а не щелчок — пунктов в
            // меню в этот момент не нарисовано.
            Step::Aim(Aim::Icon(4)),
            Step::RightClick,
            Step::Await("desktop     : context menu at", 15_000),
            Step::Aim(Aim::ContextItem(2, 0)),
            Step::Click,
            Step::Wait(1_500),
            Step::Shot("04-delete-confirm"),
            Step::Key("y"),
            Step::Await("desktop     : deleted '/home/roman/Desktop/Reports'", 15_000),
            Step::Await("desktop     : icons 4, 0 from /home/roman/Desktop", 15_000),
            Step::Aim(Aim::EmptyBelow),
            Step::Click,
            Step::Await("desktop     : context menu closed", 15_000),
            // Каталог стола остаётся пустым, и это условие повторяемости:
            // созданное предыдущим прогоном заняло бы имя «New folder», а
            // проверки выше считают значки поимённо. Сам каталог остаётся —
            // его завело создание папки, и это тоже проверка: на живом носителе
            // он не заводится вовсе.
            Step::Line("ls /"),
            Step::Await("etc/", 15_000),
            Step::Line("cat /etc/system.cfg"),
            Step::Await("language=", 15_000),
            // Тот же пояс, но уже там, где его видит человек: местное время
            // отличается от UTC ровно на выбранное смещение.
            Step::Line("date"),
            Step::Await("UTC+03:00", 15_000),
            // Личность сеанса пришла из /etc/passwd, который написал
            // установщик: это первый раз, когда система работает не от root.
            Step::Line("whoami"),
            Step::Await("roman (uid 1000 gid 1000)", 15_000),
            // И то, ради чего фаза: программа, запущенная от имени этого
            // пользователя, получает разные ответы на разные файлы — и каждый
            // ответ приходит по своей причине.
            Step::Line("run /bin/perms"),
            Step::Await("perms: uid 1000 gid 1000", 30_000),
            // Открыто всем — читается.
            Step::Await("/etc/system.cfg: mode 0644 owner 0:0 -> read", 15_000),
            // Права 0640 у root: «остальным» не разрешено ничего.
            Step::Await("/etc/passwd: mode 0640 owner 0:0 -> permission denied", 15_000),
            // Свой файл — по классу владельца. Путь программа взяла из
            // /etc/system.cfg, то есть добралась до него открытием и чтением.
            Step::Await("/home/roman/notes.txt: mode 0600 owner 1000:1000 -> read", 15_000),
            // Главный случай: сам файл открыт всем, а каталог вокруг — нет.
            // Проверка, которая смотрит только на файл, здесь ответила бы «да».
            Step::Await("/root/notes.txt: permission denied", 15_000),
            Step::Await("perms: done", 15_000),
            // Перечисление каталога проверяется правами так же, как чтение
            // файла. `/root` закрыт от всех, кроме root, а сеанс идёт от
            // `roman` — программа получает отказ на открытии, а не пустой
            // список: пустой список означал бы «каталог пуст», то есть сообщал
            // бы о содержимом, которое видеть не дали.
            Step::Line("run /bin/ls /root"),
            Step::Await("ls: cannot open /root", 30_000),
            // А свой домашний каталог перечисляется: те же права, другой ответ.
            Step::Line("run /bin/ls /home/roman"),
            Step::Await("notes.txt", 30_000),
            Step::Shot("installed"),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "write",
        about: "Система пишет в корневой раздел: оболочка из кольца ноль, программа — из третьего.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Сценарий начинается с уборки, и это не аккуратность: диск
            // переживает прогон, а записанное — переживает диск. Без уборки
            // повторный запуск этого сценария падал бы на «имя уже занято», то
            // есть ровно из-за того, что фаза и доказывает. Отказы уборки не
            // проверяются: на свежеустановленной системе удалять нечего.
            //
            // Между строками стоит ожидание приглашения, и это обязательно, а
            // не для порядка: две команды подряд не помещаются в приёмный FIFO
            // гостя (у PL011 он 32 байта), пока тот занят перерисовкой окна с
            // запрещёнными прерываниями. Хвост строки теряется, эхо обрывается
            // на середине, и сеанс встаёт навсегда — оболочка ждёт конца
            // строки, стенд ждёт ответа. Ровно так это и выглядело на обеих
            // сборках AArch64, прежде чем сюда добавились эти два шага.
            Step::Line("rm /home/roman/notes/first.txt"),
            Step::Await("freeos> ", 15_000),
            Step::Line("rm /home/roman/notes"),
            Step::Await("freeos> ", 15_000),
            // Каталог, файл и его содержимое — всё создаётся здесь и сейчас, на
            // том самом разделе, который записал установщик.
            Step::Line("mkdir /home/roman/notes"),
            Step::Await("created /home/roman/notes", 15_000),
            Step::Line("echo persisted-by-the-shell > /home/roman/notes/first.txt"),
            // Двадцать три байта — двадцать два текста и перевод строки,
            // который дописывает `echo`. Точное число здесь намеренно: «что-то
            // записалось» выглядело бы так же, как «записалось не то».
            Step::Await("wrote 23 bytes", 15_000),
            Step::Line("cat /home/roman/notes/first.txt"),
            Step::Await("persisted-by-the-shell", 15_000),
            // Файл создан секунду назад, и метка на нём обязана это показывать.
            // До появления часов ядро ставило сюда время из суперблока — момент
            // установки, — и все созданные системой файлы получали одну и ту же
            // дату, навсегда.
            //
            // Проверяется это возрастом, а не сверкой с хостом: между установкой
            // и этой строкой проходит минута-другая, и допуск, достаточно
            // широкий для дрейфа часов, оказался бы шире этого промежутка —
            // старое поведение прошло бы такую проверку. Возраст же отличает их
            // однозначно: у только что созданного файла ноль, у помеченного
            // меткой тома — сотни секунд.
            Step::Line("stat /home/roman/notes/first.txt"),
            Step::AtMost("  age    ", 5, 15_000),
            // И вторая половина утверждения: это не просто «согласованное само с
            // собой» число, а настоящее время — оно сходится с часами хоста.
            Step::Line("stat /home/roman/notes/first.txt"),
            Step::Clock("  mtime  ", 300, 15_000),
            // То же самое, но из третьего кольца: программа открывает файл на
            // запись системным вызовом и читает его обратно.
            Step::Line("run /bin/save"),
            Step::Await("save: wrote 20 bytes", 30_000),
            Step::Await("save: read back: written from ring 3", 15_000),
            Step::Await("save: done", 15_000),
            // Удаление — тоже запись. Файл `/etc/system.cfg` не трогаем: он
            // нужен следующему прогону.
            Step::Line("echo temporary > /home/roman/scratch.txt"),
            Step::Await("wrote 10 bytes", 15_000),
            Step::Line("rm /home/roman/scratch.txt"),
            Step::Await("removed /home/roman/scratch.txt", 15_000),
            Step::Line("cat /home/roman/scratch.txt"),
            Step::Await("no such file or directory", 15_000),
            // Непустой каталог не удаляется, и отказ объясняет причину.
            Step::Line("rm /home/roman/notes"),
            Step::Await("the directory is not empty", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "libc",
        about: "Программа на C: собрана нашим набором, печатает printf'ом и работает с файлами.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Установленная система, а не живая, и это не выбор из удобства:
            // корень живой — образ initrd в FAT, и писать в него нельзя. Файл,
            // который программа создаёт и читает обратно, — половина того, что
            // эта фаза обещала, и проверить её можно только на ext2.
            Step::Line("run /bin/cdemo /home/roman"),
            // Первая же строка доказывает больше, чем кажется: она напечатана
            // `printf`, то есть сквозь всю libc — форматирование, буфер stdio,
            // наш `write`, системный вызов. До этой фазы программа умела
            // печатать только тем, что написали мы сами.
            Step::Await("cdemo: starting, writing under /home/roman", 30_000),
            // Строки и форматирование. Здесь нет ничего от системы: сломайся
            // это, сломана сама библиотека, а не порт.
            Step::Await("cdemo: ok strings and snprintf", 15_000),
            // `qsort` зовёт нашу функцию обратно — то есть проверяет указатель
            // на функцию. Он живёт по адресу 512 ГиБ и не влезает в тридцать
            // два бита; неверно выбранная модель кода ломается ровно здесь.
            Step::Await("cdemo: ok qsort", 15_000),
            // Куча. `malloc` идёт через наш `sbrk`, а тот — через `SYS_MMAP`.
            Step::Await("cdemo: ok malloc", 15_000),
            // И то, ради чего фаза названа так, как названа: файл создан
            // `fopen`, записан `fwrite`, прочитан обратно и совпал побайтно.
            Step::Await("cdemo: ok fopen and fwrite", 30_000),
            Step::Await("cdemo: ok stat", 15_000),
            Step::Await("cdemo: ok fread", 15_000),
            // Удаление проверяется отдельным `stat`: `remove`, вернувшая ноль и
            // ничего не удалившая, — ровно та заглушка, против которой написана
            // вся фаза.
            Step::Await("cdemo: ok remove", 15_000),
            // Итог одной строкой. Ноль — и только ноль.
            Step::Await("cdemo: done, 0 check(s) failed", 15_000),
            Step::Await("exited with code 0", 15_000),
            // Отдельным шагом, потому что «строка не напечаталась» и «проверка
            // не прошла» — разные беды, и первую поймали бы ожидания выше.
            Step::Absent("cdemo: FAILED"),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "sdk",
        about: "Чужая библиотека (zlib 1.3.1), собранная нашим набором, работает в системе.",
        // Живая система, а не установленная, и это осознанно: программа не
        // трогает файлов вовсе — только кучу, — а живой прогон вдвое короче.
        // Проверять на ext2 то, чему файловая система не нужна, значит платить
        // за установку ради ничего.
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            Step::Line("run /bin/zdemo"),
            // Версия печатается самой zlib из её же заголовка. Строка
            // доказывает, что слинкована именно чужая библиотека, а не наша
            // заглушка с теми же именами.
            Step::Await("zdemo: starting, zlib 1.3.1", 30_000),
            Step::Await("zdemo: sample is 18000 bytes", 15_000),
            // Чужой CRC-32 против своего, посчитанного побитно в той же
            // программе. Совпадение двух независимых реализаций — проверка;
            // совпадение библиотеки с собой — тавтология.
            Step::Await("zdemo: ok crc32", 15_000),
            // Сжатие обязано **уменьшить**: несжимаемый вход zlib заворачивает
            // в блок «как есть», и проверка прошла бы с неработающим
            // дефлятором.
            Step::Await("zdemo: ok compress2", 30_000),
            // И то, ради чего фаза: данные, прошедшие туда и обратно через
            // чужой код, совпали побайтно.
            Step::Await("zdemo: ok round trip", 30_000),
            Step::Await("zdemo: done, 0 check(s) failed", 15_000),
            Step::Await("exited with code 0", 15_000),
            // Отдельным шагом: «строка не напечаталась» и «проверка не прошла» —
            // разные беды, и первую поймали бы ожидания выше.
            Step::Absent("zdemo: FAILED"),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "mc",
        about: "Двухпанельный менеджер: ходьба по каталогам, копирование, удаление — всё вне ядра.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Уборка перед началом — по той же причине, что и в сценарии
            // `write`: диск переживает прогон, и второй запуск иначе падал бы
            // на «имя уже занято». Между строками ждём приглашения: приёмный
            // FIFO гостя не держит две команды подряд, пока тот перерисовывает
            // окно.
            Step::Line("rm /home/roman/one.txt"),
            Step::Await("freeos> ", 15_000),
            Step::Line("rm /home/roman/mcdir/one.txt"),
            Step::Await("freeos> ", 15_000),
            Step::Line("rm /home/roman/mcdir/two.txt"),
            Step::Await("freeos> ", 15_000),
            Step::Line("rm /home/roman/mcdir/zed.txt"),
            Step::Await("freeos> ", 15_000),
            Step::Line("rm /home/roman/mcdir"),
            Step::Await("freeos> ", 15_000),
            // Каталог с одним предсказуемым файлом. Проверять `mc` на `/bin`
            // было бы нельзя: порядок записей каталога — тот, в котором они
            // лежат на носителе, и «второй сверху» означало бы разное на разных
            // сборках.
            Step::Line("mkdir /home/roman/mcdir"),
            Step::Await("created /home/roman/mcdir", 15_000),
            Step::Line("echo mc-payload > /home/roman/mcdir/one.txt"),
            Step::Await("wrote 11 bytes", 15_000),
            // Второй файл — для правки и просмотра (фаза С9). Имя на «z»: панель
            // сортирует по имени, и «вниз на одну» по-прежнему попадает в
            // `one.txt`.
            Step::Line("echo mc-edit > /home/roman/mcdir/zed.txt"),
            Step::Await("wrote 8 bytes", 15_000),
            // Панели указаны аргументами: программа не знает, где что лежит, и
            // знать не должна.
            // Короткая форма: имя программы вместо `run /bin/...`. Оболочка
            // ищет её в `/bin` — так же, как всякая оболочка ищет по `PATH`.
            Step::Line("mc /home/roman/mcdir /home/roman"),
            Step::Await("mc: started", 30_000),
            // Прямой режим — то, ради чего в фазе 29 появился второй режим
            // терминала: без него стрелки приезжали бы четырьмя видимыми
            // символами, а Enter требовался бы после каждой клавиши. Проверка
            // по всему выводу, а не ожиданием вперёд: строку печатает ядро в
            // ответ на системный вызов, и она оказывается позади курсора.
            Step::Expect("tty         : raw mode"),
            // Вниз на одну строку: первая — «..», вторая — единственный файл.
            // Клавиша идёт по серийной линии той же последовательностью,
            // которую прислал бы настоящий терминал, и возвращается программе
            // ею же.
            Step::Raw(b"\x1b[B"),
            Step::Wait(2000),
            Step::Shot("mc"),
            // Окно терминала разворачивается под работающим `mc` — и `mc` идёт
            // за ним (фаза С9). Три строки по порядку: стол изменил окно, ядро
            // назвало новый размер сетки (до С9 оно помнило размер от загрузки),
            // `mc` его перечитал сам, без нажатия клавиши. Потом окно
            // возвращается, чтобы остальные шаги шли в прежнем размере.
            Step::Aim(Aim::Maximize("Terminal")),
            Step::Click,
            Step::Await("desktop     : resized 'Terminal'", 15_000),
            Step::Await("term        : window is now", 15_000),
            Step::Await("mc: window is", 15_000),
            Step::Wait(2000),
            Step::Shot("mc-maximized"),
            Step::Aim(Aim::Maximize("Terminal")),
            Step::Click,
            Step::Await("desktop     : resized 'Terminal'", 15_000),
            Step::Await("term        : window is now", 15_000),
            Step::Await("mc: window is", 15_000),
            Step::Wait(1000),
            // F5 копирует файл под курсором в каталог другой панели. С фазы С9
            // сначала спрашивает — окном с полем, где уже стоит путь другой
            // панели, — и Enter соглашается. Строка называет обе стороны
            // целиком: «скопировалось» и «скопировалось не то» иначе выглядели
            // бы одинаково.
            Step::Raw(b"\x1b[15~"),
            Step::Wait(2000),
            Step::Shot("mc-copy"),
            Step::Raw(b"\n"),
            Step::Await(
                "mc: copied /home/roman/mcdir/one.txt -> /home/roman/one.txt",
                20_000,
            ),
            // F6 переименовывает — и это не «скопировать и удалить»: содержимое
            // не читается вовсе, меняется запись каталога. В поле уже стоит
            // выделенное имя, и первая набранная буква заменяет его целиком.
            Step::Raw(b"\x1b[17~"),
            Step::Wait(500),
            Step::Raw(b"two.txt\n"),
            Step::Await(
                "mc: renamed /home/roman/mcdir/one.txt -> /home/roman/mcdir/two.txt",
                20_000,
            ),
            // Курсор остался на переименованном (как в Far), поэтому F8 сразу:
            // удаление спрашивает подтверждение, Enter — «Удалить».
            Step::Raw(b"\x1b[19~"),
            Step::Wait(500),
            Step::Raw(b"\n"),
            Step::Await("mc: removed /home/roman/mcdir/two.txt", 20_000),
            // Курсор остаётся на том же месте списка — теперь там `zed.txt`.
            // F4 открывает правку: в конец первой строки дописываются ещё две,
            // F2 сохраняет. Число байт посчитано: «mc-edit\n» (8) + «second
            // line\n» (12) + «third\n» (6) — перевод строки в конце файла был и
            // сохраняется.
            Step::Raw(b"\x1bOS"),
            Step::Await("mc: editing /home/roman/mcdir/zed.txt, 1 line(s)", 20_000),
            Step::Raw(b"\x1b[F"),
            Step::Raw(b"\nsecond line\nthird"),
            Step::Wait(2000),
            Step::Shot("mc-edit"),
            Step::Raw(b"\x1bOQ"),
            Step::Await("mc: saved /home/roman/mcdir/zed.txt, 26 bytes", 20_000),
            Step::Raw(b"\x1b[21~"),
            Step::Await("mc: editor closed", 20_000),
            // F3 — просмотр того же файла: трёх строк, записанных правкой. End
            // уводит в конец, и строка закрытия называет видимый кусок — у
            // файла короче окна это все три строки при любом размере окна.
            Step::Raw(b"\x1bOR"),
            Step::Await("mc: viewed /home/roman/mcdir/zed.txt", 20_000),
            Step::Raw(b"\x1b[F"),
            Step::Wait(2000),
            Step::Shot("mc-view"),
            Step::Raw(b"\x1bOR"),
            Step::Await("mc: viewer closed showing lines 1-3 of 3", 20_000),
            // F10 — выход. Программа возвращает построчный режим и убирает за
            // собой экран.
            Step::Raw(b"\x1b[21~"),
            Step::Await("mc: quit", 20_000),
            Step::Expect("tty         : canonical mode"),
            Step::Await("mc: done", 15_000),
            // Приглашения ждём отдельно, и это не формальность: пока терминал
            // числится за программой, набранное уходит **ей**, а не оболочке.
            // Так же ведёт себя всякий tty, и команда, отправленная в этот
            // промежуток, пропала бы — что и происходило, пока этой строки
            // здесь не было.
            Step::Await("tty         : foreground released", 15_000),
            Step::Await("freeos> ", 15_000),
            // И главное: сделанное программой пережило её саму. Проверяет это
            // оболочка — другой код, другая задача, ядро вместо третьего кольца.
            Step::Line("cat /home/roman/one.txt"),
            Step::Await("mc-payload", 20_000),
            Step::Line("cat /home/roman/mcdir/two.txt"),
            Step::Await("no such file or directory", 15_000),
            // Правка пережила программу: оболочка читает то, что сохранил F2.
            Step::Line("cat /home/roman/mcdir/zed.txt"),
            Step::Await("second line", 20_000),
            Step::Await("third", 15_000),
            // Вывод в окно, пришедший, пока стол рисовал кадр, ждёт в очереди, а
            // не пропадает (фаза С9). Пропасть он может только при переполнении
            // очереди — и тогда ядро это называет.
            Step::Absent("term        : dropped output"),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "dotnet",
        about: "Своя среда .NET: сборки, собранные dotnet build на Windows, выполняются вне ядра без переделки.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Шаблон `dotnet new console` без единой правки. Живая система, а не
            // установленная: сборки только читаются, а initrd везёт их сам.
            Step::Line("dotnet /usr/share/dotnet/samples/hello.dll"),
            Step::Await("dotnet: hello.dll: 4608 bytes, 2 type(s), 2 method(s)", 60_000),
            Step::Await("Hello, World!", 60_000),
            // Строка итога — доказательство, что `Main` дошёл до `ret`, а не что
            // программа успела напечатать и упала.
            Step::Await("dotnet: hello.dll: Main returned 0", 60_000),
            Step::Await("freeos> ", 15_000),
            // Арифметика трёх ширин, рекурсия, циклы, switch, склейка строк и
            // кириллица. Каждая строка — та же, что печатает настоящий dotnet
            // (сверено `cargo xtask clr-check` и тестом `clr-vm`).
            Step::Line("dotnet /usr/share/dotnet/samples/arith.dll"),
            Step::Await("arith: start", 60_000),
            Step::Await("sum 1..10 = 55", 60_000),
            Step::Await("fib(20) = 6765", 120_000),
            Step::Await("fact(20) = 2432902008176640000", 60_000),
            Step::Await("gcd(1071, 462) = 21", 60_000),
            Step::Await("-17 / 5 = -3, -17 % 5 = -2", 60_000),
            Step::Await("uint: 4294967279", 60_000),
            Step::Await("uint >> 28: 15", 60_000),
            Step::Await("1L << 40 = 1099511627776", 60_000),
            Step::Await("-64 >> 3 = -8", 60_000),
            Step::Await("bool: True, char: Z", 60_000),
            Step::Await("switch: zero one", 60_000),
            Step::Await("switch: two many", 60_000),
            Step::Await("Привет из IL", 60_000),
            Step::Await("arith: done", 60_000),
            Step::Await("dotnet: arith.dll: Main returned 0", 60_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N3a: классы, виртуальные вызовы, интерфейсы, статические
            // конструкторы, структуры и упаковка поверх своей базовой библиотеки
            // на C#. Строки выбраны так, что каждая ломается от своего дефекта:
            // таблица виртуальных методов, повторная реализация интерфейса,
            // порядок статического конструктора, копия структуры, упаковка.
            Step::Line("dotnet /usr/share/dotnet/samples/objects.dll"),
            Step::Await("objects: start", 60_000),
            Step::Await("little Bim says yip", 60_000),
            Step::Await("type: Puppy / FreeOs.Samples.Objects.Puppy", 60_000),
            Step::Await("counter at 2", 60_000),
            Step::Await("base next: 3", 60_000),
            Step::Await("before Registry", 60_000),
            Step::Await("Registry: static constructor", 60_000),
            Step::Await("registry: 102", 60_000),
            Step::Await("a = (1, 2), b = (11, 12), sum 23", 60_000),
            Step::Await("points: (0, 0) (5, 5) (7, 0)", 60_000),
            Step::Await("holder: (50, 3) line (3, 3)-(4, 4)", 60_000),
            Step::Await("boxed (1, 2), moved (2, 3), unboxed (1, 2)", 60_000),
            Step::Await("boxed equals: True True False", 60_000),
            Step::Await("primes: 129", 60_000),
            Step::Await("раздватри 3", 60_000),
            Step::Await("objects: done", 60_000),
            // Main возвращает число созданных животных — три.
            Step::Await("dotnet: objects.dll: Main returned 3", 60_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N3b: исключения. Порядок строк и есть проверка: фильтр
            // печатает раньше `finally` внутренних кадров (два прохода, как в
            // CLR), исключение из `finally` заменяет летящее, а исключения самой
            // среды ловятся программой с текстом из базовой библиотеки.
            Step::Line("dotnet /usr/share/dotnet/samples/exceptions.dll"),
            Step::Await("exceptions: start", 60_000),
            Step::Await("filter deep", 60_000),
            Step::Await("unwind 1", 60_000),
            Step::Await("deep: bottom code 7", 60_000),
            Step::Await("fell through to AppError", 60_000),
            Step::Await("nested 110", 60_000),
            Step::Await("null: Object reference not set to an instance of an object.", 60_000),
            Step::Await("divide: Attempted to divide by zero.", 60_000),
            Step::Await("rethrown again", 60_000),
            Step::Await("close file", 60_000),
            Step::Await("got second", 60_000),
            Step::Await("loop 43", 60_000),
            Step::Await("exceptions: done", 60_000),
            Step::Await("dotnet: exceptions.dll: Main returned 43", 60_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N3c: обобщения, специализированные и ссылочными, и значимыми
            // типами, статика у каждого экземпляра обобщённого типа, делегаты,
            // замыкания, события и интерполяция строк.
            Step::Line("dotnet /usr/share/dotnet/samples/generics.dll"),
            Step::Await("generics: start", 60_000),
            Step::Await("stacks: 25 4 два один", 60_000),
            Step::Await("pair (7, семь) swapped (семь, 7)", 60_000),
            Step::Await("Tally<Int32> ready", 60_000),
            Step::Await("Tally<String> ready", 60_000),
            Step::Await("max 9 pear -5", 60_000),
            Step::Await("delegates 5 1005 42 3", 60_000),
            Step::Await("multicast 22", 60_000),
            Step::Await("clicks 1", 60_000),
            Step::Await("interpolated: привет, мир! 3 x (7, семь) = 21", 60_000),
            Step::Await("generics: done", 60_000),
            Step::Await("dotnet: generics.dll: Main returned 22", 60_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N3d: сборщик мусора. Программа выделяет около 70 МиБ при
            // куче `/bin/dotnet` в 16 и проверяет каждое живое значение сама:
            // без сборки она не дожила бы, с пропущенным корнем напечатала бы
            // другие числа.
            Step::Line("dotnet /usr/share/dotnet/samples/gc.dll"),
            Step::Await("gc: start", 60_000),
            Step::Await("checksum 5886420", 300_000),
            Step::Await("chain 6 sum 7500", 60_000),
            Step::Await("cells cell 500 / cell 2500 weight 2500", 60_000),
            Step::Await("closure 2501 local -1", 60_000),
            Step::Await("литерал живёт", 60_000),
            Step::Await("gc: done", 60_000),
            // Что сборок было больше нуля, проверяет тест `clr-vm` на том же
            // файле: подстрока «0 collection(s)» совпала бы и с «10», и с
            // законным нулём у `hello` выше.
            Step::Await("dotnet: gc.dll: Main returned 6", 60_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N4a: строки, форматирование, разбор, StringBuilder, Math.
            Step::Line("dotnet /usr/share/dotnet/samples/text.dll"),
            Step::Await("text: start", 60_000),
            Step::Await("HELLO, FREEOS WORLD hello, freeos world ПРИВЕТ, МИР", 60_000),
            Step::Await("4 [a|b||c] abc", 60_000),
            Step::Await("2 + 3 = 5 [   7|8   ]", 60_000),
            Step::Await("   42|-7  |FF|00ff|003|-0012|-2147483648|18446744073709551615", 60_000),
            Step::Await(">>Y=10,True -3 14 =", 60_000),
            Step::Await("format: The input string 'x' was not in a correct format.", 60_000),
            Step::Await("True True Ж 65 True True", 60_000),
            Step::Await("text: done", 60_000),
            Step::Await("dotnet: text.dll: Main returned 77", 60_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N4b: коллекции, массивы как IList<T>, итераторы.
            Step::Line("dotnet /usr/share/dotnet/samples/collections.dll"),
            Step::Await("collections: start", 60_000),
            Step::Await("sorted 8,7,1 sum 16 array 3", 60_000),
            Step::Await("dict ann=31 dan=19 cid=41 count 3 False True 41", 60_000),
            Step::Await("missing: The given key 'zed' was not present in the dictionary.", 60_000),
            Step::Await("set 16,1,9 count 3 added False has 9 True", 60_000),
            Step::Await("array 1,2,3 sum 6 index 1 ilist 33", 60_000),
            Step::Await("words: finally", 60_000),
            Step::Await("collections: done", 60_000),
            Step::Await("dotnet: collections.dll: Main returned 6", 60_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N4c: дробные числа — печать, форматы, разбор, Math, float.
            Step::Line("dotnet /usr/share/dotnet/samples/floats.dll"),
            Step::Await("floats: start", 60_000),
            Step::Await("0.3333333333333333 0.6666666666666666 1E+21 1E-07 123.456 -0 100", 60_000),
            Step::Await("3.14|2.7183|12,345.68|25.6 %|1.234E+003|(¤42.50)|2|4|0.12", 60_000),
            Step::Await("0.33333334 16777216 16777216 3.4028235E+38 1 True 0.3333333432674408 0.1", 60_000),
            Step::Await("-123450 True Infinity False 0 3.14159 NaN -Infinity", 60_000),
            Step::Await("0.8414709848 0.5000000000 0.5463024898 2.3561944902 2.7182818285 2.3025850930", 60_000),
            Step::Await("1.6439345666815615 3.1406380562059946 3FFA4D8E550A946E", 60_000),
            Step::Await("floats: done", 60_000),
            Step::Await("dotnet: floats.dll: Main returned 41", 60_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N4d: перечисления — имена, флаги, форматы, разбор.
            Step::Line("dotnet /usr/share/dotnet/samples/enums.dll"),
            Step::Await("enums: start", 60_000),
            Step::Await("Read, Execute | ReadWrite | All | None | 8 | 9", 60_000),
            Step::Await("Blue 6 Blue 00000006 Read, Execute 5 C8 FF Bold, Italic", 60_000),
            Step::Await("Requested value 'nope' was not found.", 60_000),
            Step::Await("enums: done", 60_000),
            Step::Await("dotnet: enums.dll: Main returned 6", 60_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N4d: Linq и большие массивы примитивов — четыре мегабайта
            // `byte[]` в куче программы на 16 МиБ.
            Step::Line("dotnet /usr/share/dotnet/samples/linq.dll"),
            Step::Await("linq: start", 60_000),
            Step::Await("Dan Bob Eve Ann Cid | Cid Ann Eve Bob Dan", 60_000),
            Step::Await("Oslo: 2 Ann+Cid avg 31", 60_000),
            Step::Await("0 4 10 True 13", 60_000),
            Step::Await("Sequence contains more than one matching element", 60_000),
            Step::Await("4000000 1000000 8496120 1229 234 3999", 120_000),
            Step::Await("linq: done", 60_000),
            Step::Await("dotnet: linq.dll: Main returned 29", 60_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N5b: время и окружение. Даты от фиксированного мига печатаются
            // как у dotnet; строка `now:` проверяет часы ядра (год не 1970) и
            // пояс из настроек, `slept:` — сон и монотонные часы, `machine:` —
            // число процессоров из SYS_SYSINFO. Код 21 приходит через
            // `Environment.Exit`, мимо `finally`, и журнал говорит об этом сам.
            Step::Line("dotnet /usr/share/dotnet/samples/time.dll alpha beta"),
            Step::Await("time: start", 60_000),
            Step::Await("F: Monday, 14 September 2026 21:05:07", 60_000),
            Step::Await("o: 2026-09-14T21:05:07.1230000", 60_000),
            Step::Await("Mon Sep 14 1200 12 A.D. hey q 21 | 09:05:07 A 02026", 60_000),
            Step::Await("2024-02-29 2025-02-28 2026-12-24T09:05:07.1230000 2026-09-13T17:35:07.3730005", 60_000),
            Step::Await("2026-09-14T07:05:00.0000000 False 0 True Monday, 14 September 2026", 60_000),
            Step::Await("1:2:03:04.567 1:02:03:04.5670000 -1.02:03:04.5670000 1.02:03:04.567 2 -1:2:03:04.567", 60_000),
            Step::Await("now: True Utc Local True True Local True True Utc", 60_000),
            Step::Await("slept: True True True True True False True True True", 60_000),
            Step::Await("args: 2 [alpha|beta]", 60_000),
            Step::Await("command line: 3 time.dll alpha|beta", 60_000),
            Step::Await("machine: True True", 60_000),
            Step::Await("time: done", 60_000),
            Step::Await("dotnet: time.dll: Environment.Exit(21)", 60_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N10: PriorityQueue — файл dotnet/runtime в corelib без правки.
            // Раскладка кучи (`heap`) и порядок равных приоритетов (`ties`)
            // совпадают с dotnet только при том же алгоритме; `copied` и
            // `cleared` идут через Array.Copy и Array.Clear среды, `negative` —
            // через сообщение в две строки, склеенное образцом.
            Step::Line("dotnet /usr/share/dotnet/samples/pqueue.dll"),
            Step::Await("pqueue: start", 60_000),
            Step::Await("heap g:0 b:2 d:4 a:1 c:3 f:6 e:5 count 7 capacity 7", 60_000),
            Step::Await("ties red:3 gray:4 gold:4 pink:4 cyan:4 blue:4 green:5", 60_000),
            Step::Await("max high top huge peek mid:5 rest mid:5 low:1 tiny:0", 60_000),
            Step::Await("jobs removed True y:7 missing False layout urgent:1 later:8 z:7 x:7", 60_000),
            Step::Await("copied (, 0) (r, 1) (q, 3) (s, 2)", 60_000),
            Step::Await("copy: Target array type is not compatible", 60_000),
            Step::Await("cleared 0 capacity 3", 60_000),
            Step::Await("negative: initialCapacity | initialCapacity ('-1') must be a non-negative value.", 60_000),
            Step::Await("modified: Collection was modified after the enumerator was instantiated.", 60_000),
            Step::Await("dijkstra A=0 B=7 C=9 F=11 E=20 D=20", 60_000),
            Step::Await("pqueue: done", 60_000),
            Step::Await("dotnet: pqueue.dll: Main returned 20", 60_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N10b: ещё шесть коллекций из dotnet/runtime. `set ops` идёт
            // через stackalloc (localloc среды), `stack:` — через упаковку int
            // при копировании в object[], `tree copy` — через красно-чёрное
            // дерево SortedSet под SortedDictionary.
            Step::Line("dotnet /usr/share/dotnet/samples/sorted.dll"),
            Step::Await("sorted: start", 60_000),
            Step::Await("linked: a,b,c,d,e,c 6 a c", 60_000),
            Step::Await("stack: 4,3,2,1 4 0,4,3,2,1,0 4,3,2,1 True", 60_000),
            Step::Await("queue: q3,q4,q5 q3 True q3,q4,q5 InvalidOperationException: Queue empty.", 60_000),
            Step::Await("sorted list: [apple, 1],[fig, 7],[kiwi, 2],[pear, 3] 2 -1 1", 60_000),
            Step::Await("sorted dictionary: 10,30,50,60,70,80,90 7 v60 True False", 60_000),
            Step::Await("set: 1,3,5,7,9,11,13 1 13 view 5,7,9,11 4 5 11", 60_000),
            Step::Await("set ops: True True True True", 60_000),
            Step::Await("union: 1,3,5,6,7,8,9,11,13,100 | 5,6,7 | 1,3,9,11,13 | 2,4,5,6,7,9,11,13", 60_000),
            Step::Await("sorted: done", 60_000),
            Step::Await("dotnet: sorted.dll: Main returned 42", 60_000),
            Step::Await("freeos> ", 15_000),
            // Nullable<T>: пустое значение упаковывается в null, полное — в сам T.
            Step::Line("dotnet /usr/share/dotnet/samples/nullable.dll"),
            Step::Await("nullable: start", 60_000),
            Step::Await("boxed: True Int32 5 True True False", 60_000),
            Step::Await("unboxed: 5 False 12 12", 60_000),
            Step::Await("operators: 6 True True False True True -1", 60_000),
            Step::Await("struct: Point (3, 4) 4 True", 60_000),
            Step::Await("list: 2 107 10||-3", 60_000),
            Step::Await("dotnet: nullable.dll: Main returned 11", 60_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N10c: List<T> и сортировка из dotnet/runtime. Строка `large`
            // — порядок равных ключей после неустойчивой сортировки .NET.
            Step::Line("dotnet /usr/share/dotnet/samples/listsort.dll"),
            Step::Await("listsort: start", 60_000),
            Step::Await("large: 0u46,0t45,0n39,0j9,0z25,0w48,1a0,1r43,1m38,1k36,1f31,1d29,1b27,1t19,1q16,1x49,1h7,1p15,2o14,2l37,2p41,2w22,2m12,2n13,2s18,2v47,3s44,3b1,3o40,3e4,3f5,3g6,3c2,3i34,3y24,3i8,3c28,3a26,3r17,3h33,4j35,4e30,4k10,4l11,4q42,4d3,4x23,4v21,4u20,4g32", 120_000),
            Step::Await("doubles: NaN,NaN,-Infinity,-1,0,-0,2,3.5,10000000000", 60_000),
            Step::Await("removed: 3 0,1,2,3,4,5,6,7,8", 60_000),
            Step::Await("read only: 7 5 True NotSupportedException: Collection is read-only.", 60_000),
            Step::Await("set: a,b,d True True False", 60_000),
            Step::Await("dotnet: listsort.dll: Main returned 16", 60_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N10d: Dictionary и HashSet из dotnet/runtime. `order` — порядок
            // обхода после удалений и повторных вставок, `trimmed` — простое
            // число из таблицы HashHelpers, `span` — поиск по срезу знаков.
            Step::Line("dotnet /usr/share/dotnet/samples/dict.dll"),
            Step::Await("dict: start", 60_000),
            Step::Await("order: k0=0,n1=11,k2=2,n3=13,k4=4,k5=5,k6=6,n2=12,k8=8,k9=9,n4=14", 120_000),
            Step::Await("trimmed: 71 50", 60_000),
            Step::Await("span: 2 False True n4 14 True 10 True False True", 60_000),
            Step::Await("union: 31,21,7,11,13,5,17,19 8 23 59", 60_000),
            Step::Await("words: Apple,banana,Cherry True True False True Apple,banana,date True True Apple", 60_000),
            Step::Await("dotnet: dict.dll: Main returned 21", 60_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N11: async/await. `first` — начало метода идёт синхронно, конец
            // продолжением; `all` — три задачи с разными задержками; `cancel` —
            // отмена Task.Delay; `delay` — таймеры очереди идут по часам.
            Step::Line("dotnet /usr/share/dotnet/samples/asyncs.dll"),
            Step::Await("asyncs: start", 60_000),
            Step::Await("first: 20 RanToCompletion True True", 60_000),
            Step::Await("delay: True True True", 60_000),
            Step::Await("all: 1,4,9", 60_000),
            Step::Await("both: InvalidOperationException: one | 2 AggregateException: One or more errors occurred. (one) (two)", 60_000),
            Step::Await("cancel: TaskCanceledException: A task was canceled. | Canceled True True", 60_000),
            Step::Await("continue: 4 42", 60_000),
            Step::Await("dotnet: asyncs.dll: Main returned 30", 60_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N6a: окно WinForms. Форма открывается окном программы,
            // рисует себя в Paint, получает щелчок мышью в своих координатах и
            // закрывается крестиком стола — FormClosing и FormClosed с причиной
            // UserClosing, как под Windows. Пока окно открыто, строк в линию не
            // набирать: фокус у окна, и строка ушла бы в него. После закрытия
            // стол сам отдаёт фокус терминалу (`focus 'Terminal'`).
            Step::Line("dotnet /usr/share/dotnet/samples/form.dll"),
            Step::Await("form: start", 60_000),
            Step::Await("Color [SteelBlue] -12156236 70,130,180 True", 60_000),
            Step::Await("ctor: 480x320 'FreeOS Form' DemoForm False", 60_000),
            Step::Await("load: {Width=480, Height=320}", 60_000),
            Step::Await("dotnet: window 'FreeOS Form' opened, 480x320", 60_000),
            Step::Await("shown: True", 60_000),
            Step::Await("paint 1: {X=0,Y=0,Width=480,Height=320}", 60_000),
            Step::Wait(2_500),
            Step::Shot("form"),
            Step::Aim(Aim::Program("FreeOS Form", 100, 60)),
            Step::Click,
            Step::Await("mouse: Left at 100,60 clicks 1", 30_000),
            Step::Aim(Aim::Close("FreeOS Form")),
            Step::Click,
            Step::Await("closing: UserClosing False", 30_000),
            Step::Await("closed: UserClosing", 30_000),
            Step::Await("dotnet: window 'FreeOS Form' closed after", 30_000),
            Step::Await("form: Run returned, disposed True", 30_000),
            Step::Await("dotnet: form.dll: Main returned 17", 30_000),
            // `focus 'Terminal'` стол печатает раньше, при закрытии окна, — ждать
            // его здесь значит искать позади курсора.
            Step::Await("freeos> ", 15_000),
            // Фаза N6b: шаблон `dotnet new winforms` с кнопкой и надписью из
            // дизайнера, без единой правки сгенерированного кода. Стенд щёлкает
            // по середине кнопки — (12, 12) и 94×29 из дизайнера; масштаба по DPI
            // у среды нет, и координаты те же, что в коде. Между щелчками пауза:
            // два щелчка подряд — это ещё и двойной щелчок.
            Step::Line("dotnet /usr/share/dotnet/samples/winforms.dll"),
            Step::Await("dotnet: window 'Form1' opened, 800x450", 60_000),
            Step::Await("shown: Form1 | Click me | label1 | 2 True 0 True Form1", 60_000),
            Step::Wait(2_500),
            Step::Shot("winforms"),
            Step::Aim(Aim::Program("Form1", 59, 26)),
            Step::Click,
            Step::Await("button1: Clicked 1 time", 30_000),
            Step::Wait(1_200),
            Step::Click,
            Step::Await("button1: Clicked 2 times", 30_000),
            Step::Wait(2_500),
            Step::Shot("winforms-clicked"),
            Step::Aim(Aim::Close("Form1")),
            Step::Click,
            Step::Await("closed: UserClosing Clicked 2 times", 30_000),
            Step::Await("dotnet: window 'Form1' closed after", 30_000),
            Step::Await("dotnet: winforms.dll: Main returned 0", 30_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N7a: клавиатура. Щелчок ставит фокус в поле ввода, клавиатура
            // QEMU набирает `hello`, Backspace стирает букву, Alt+Shift
            // переключает раскладку, и те же клавиши `ghbdtn` приезжают словом
            // «привет» — TextChanged печатает текст и счёт изменений после
            // каждой. Щелчок по флажку — CheckedChanged, затем CheckStateChanged.
            Step::Line("dotnet /usr/share/dotnet/samples/controls.dll"),
            Step::Await("dotnet: window 'Controls' opened, 400x140", 60_000),
            Step::Await("shown: 0 32767 False False False Unchecked False True 0", 60_000),
            Step::Aim(Aim::Program("Controls", 60, 25)),
            Step::Click,
            Step::Wait(1_000),
            Step::Type("hello"),
            Step::Await("text: hello | False Unchecked | 5", 30_000),
            Step::Key("backspace"),
            Step::Await("text: hell | False Unchecked | 6", 30_000),
            Step::Key("alt-shift"),
            Step::Await("keyboard    : layout RU (ru)", 15_000),
            Step::Type("ghbdtn"),
            Step::Await("text: hellпривет | False Unchecked | 12", 30_000),
            Step::Key("alt-shift"),
            Step::Await("keyboard    : layout EN (us)", 15_000),
            Step::Aim(Aim::Program("Controls", 20, 64)),
            Step::Click,
            Step::Await("checked: hellпривет | True Checked | 12", 30_000),
            Step::Await("state: Checked", 30_000),
            Step::Wait(2_500),
            Step::Shot("controls"),
            Step::Aim(Aim::Close("Controls")),
            Step::Click,
            Step::Await("dotnet: window 'Controls' closed after", 30_000),
            Step::Await("dotnet: controls.dll: Main returned 0", 30_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N7b: списки. Щелчок по первой строке списка, дальше стрелки —
            // высота строки у шрифта FreeOS своя, и целиться мышью ниже первой
            // строки значило бы угадывать. Щелчок по выпадающему списку
            // открывает его поверх формы, щелчок по первой строке выбирает,
            // стрелка вниз меняет выбор при закрытом списке.
            Step::Line("dotnet /usr/share/dotnet/samples/lists.dll"),
            Step::Await("dotnet: window 'Lists' opened, 360x140", 60_000),
            Step::Await("shown: 3 -1 One False | 3 -1 DropDownList ''", 60_000),
            Step::Aim(Aim::Program("Lists", 60, 18)),
            Step::Click,
            Step::Await("list: 0 Moscow | Moscow,Oslo,Paris", 30_000),
            Step::Key("down"),
            Step::Await("list: 1 Oslo | Moscow,Oslo,Paris", 30_000),
            Step::Key("down"),
            Step::Await("list: 2 Paris | Moscow,Oslo,Paris", 30_000),
            Step::Aim(Aim::Program("Lists", 255, 26)),
            Step::Click,
            Step::Wait(2_500),
            Step::Shot("lists-dropdown"),
            Step::Aim(Aim::Program("Lists", 230, 46)),
            Step::Click,
            Step::Await("combo: 0 Small 'Small' | Small,Medium,Large", 30_000),
            Step::Key("down"),
            Step::Await("combo: 1 Medium 'Medium' | Small,Medium,Large", 30_000),
            Step::Wait(2_500),
            Step::Shot("lists"),
            Step::Aim(Aim::Close("Lists")),
            Step::Click,
            Step::Await("dotnet: window 'Lists' closed after", 30_000),
            Step::Await("dotnet: lists.dll: Main returned 0", 30_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N7c: таймер и окна сообщений. Таймер тикает трижды и
            // останавливается сам. Кнопка открывает модальное окно «Question»:
            // его кнопки стенд находит от правого нижнего угла, «Yes» — вторая
            // справа. Второе окно без заголовка называется именем программы, и
            // закрывает его Enter — кнопка по умолчанию.
            Step::Line("dotnet /usr/share/dotnet/samples/dialogs.dll"),
            Step::Await("dotnet: window 'Dialogs' opened, 320x100", 60_000),
            Step::Await("started: True", 60_000),
            Step::Await("tick 3 True 50", 60_000),
            Step::Await("stopped: False", 30_000),
            Step::Aim(Aim::Program("Dialogs", 72, 26)),
            Step::Click,
            Step::Await("dotnet: window 'Question' opened", 30_000),
            Step::Wait(2_500),
            Step::Shot("dialogs-question"),
            Step::Aim(Aim::ProgramCorner("Question", 140, 26)),
            Step::Click,
            Step::Await("answer: Yes", 30_000),
            Step::Await("dotnet: window 'dialogs' opened", 30_000),
            Step::Wait(1_500),
            Step::Key("ret"),
            Step::Await("ok: OK", 30_000),
            Step::Wait(2_500),
            Step::Shot("dialogs"),
            Step::Aim(Aim::Close("Dialogs")),
            Step::Click,
            Step::Await("closed: UserClosing 3", 30_000),
            Step::Await("dotnet: dialogs.dll: Main returned 0", 30_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N7d: меню, Dock и Anchor. Пункт «File» открывается щелчком, а
            // строки выбираются стрелками и Enter: высота строки у шрифта FreeOS
            // своя. Стрелка вправо переходит к «View». Кнопка «Grow» прижата к
            // правому нижнему углу — стенд целится в неё от угла, форма растёт,
            // и окно FreeOS растёт вместе с ней. «Exit» — последний пункт, до
            // него доходит стрелка вверх.
            Step::Line("dotnet /usr/share/dotnet/samples/layout.dll"),
            Step::Await("dotnet: window 'Layout' opened, 400x250", 60_000),
            Step::Await("shown: panel1 0 100 True True | panel2 100 300 True", 60_000),
            Step::Aim(Aim::Program("Layout", 20, 12)),
            Step::Click,
            Step::Wait(2_500),
            Step::Shot("layout-menu"),
            Step::Key("down"),
            Step::Key("ret"),
            Step::Await("open: ToolStripMenuItem &Open", 30_000),
            Step::Aim(Aim::Program("Layout", 20, 12)),
            Step::Click,
            Step::Wait(1_000),
            Step::Key("right"),
            Step::Key("down"),
            Step::Key("ret"),
            Step::Await("wrap: True Checked", 30_000),
            Step::Aim(Aim::ProgramCorner("Layout", 62, 50)),
            Step::Click,
            Step::Await("dotnet: window 'Layout' resized to 500x300", 30_000),
            Step::Await("grown: 500x300 panel1 0 100 True True | panel2 100 400 True", 30_000),
            Step::Wait(2_500),
            Step::Shot("layout-grown"),
            Step::Aim(Aim::Program("Layout", 20, 12)),
            Step::Click,
            Step::Wait(1_000),
            Step::Key("up"),
            Step::Key("ret"),
            Step::Await("dotnet: window 'Layout' closed after", 30_000),
            Step::Await("dotnet: layout.dll: Main returned 0", 30_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N7e: переключатели, полоса хода и ползунок. Щелчок по
            // «Medium» снимает отметку с «Small»; щелчок по ползунку ставит
            // значение по месту щелчка — восьмая отметка из десяти, — а
            // стрелка вправо двигает его на шаг. Ползунок тянет за собой полосу
            // хода.
            Step::Line("dotnet /usr/share/dotnet/samples/choices.dll"),
            Step::Await("dotnet: window 'Choices' opened, 380x150", 60_000),
            Step::Await("shown: True False False | tabstop True False False", 60_000),
            Step::Aim(Aim::Program("Choices", 34, 76)),
            Step::Click,
            Step::Await("radio: Medium True | False True False", 30_000),
            Step::Aim(Aim::Program("Choices", 319, 74)),
            Step::Click,
            Step::Await("track: 8", 30_000),
            Step::Key("right"),
            Step::Await("track: 9", 30_000),
            Step::Wait(6_000),
            Step::Shot("choices"),
            Step::Aim(Aim::Close("Choices")),
            Step::Click,
            Step::Await("closed: UserClosing Level 9", 30_000),
            Step::Await("dotnet: choices.dll: Main returned 0", 30_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N7f: вкладки и строка состояния. Щелчок по заголовку первой
            // вкладки даёт ей фокус, стрелка вправо переходит ко второй — ширина
            // заголовков зависит от шрифта. Флажок на второй странице стоит под
            // заголовками: высота их полосы от 24 до 30 точек, и точка 52 от
            // верха формы попадает во флажок при любой из них.
            Step::Line("dotnet /usr/share/dotnet/samples/tabs.dll"),
            Step::Await("dotnet: window 'Tabs' opened, 360x202", 60_000),
            Step::Await("shown: 2 0 General | General:True Options:False", 60_000),
            Step::Aim(Aim::Program("Tabs", 20, 10)),
            Step::Click,
            Step::Wait(1_000),
            Step::Key("right"),
            Step::Await("selected: 2 1 Options | General:False Options:True", 30_000),
            Step::Aim(Aim::Program("Tabs", 24, 52)),
            Step::Click,
            Step::Await("checked: True", 30_000),
            Step::Wait(2_500),
            Step::Shot("tabs"),
            Step::Aim(Aim::Close("Tabs")),
            Step::Click,
            Step::Await("closed: UserClosing Enabled: True", 30_000),
            Step::Await("dotnet: tabs.dll: Main returned 0", 30_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N7g: поле со стрелками и decimal. Щелчок по верхней стрелке
            // первого поля — у правого края, в верхней половине — прибавляет
            // шаг и даёт полю фокус; стрелка вверх на клавиатуре прибавляет ещё.
            // ValueChanged приходит раньше, чем обновится текст поля.
            Step::Line("dotnet /usr/share/dotnet/samples/numbers.dll"),
            Step::Await("dotnet: window 'Numbers' opened, 260x90", 60_000),
            Step::Await("shown: 10 5 50 1 '10' | 1.5 0.25 '1.50'", 60_000),
            Step::Aim(Aim::Program("Numbers", 124, 17)),
            Step::Click,
            Step::Await("value: 11 '10'", 30_000),
            Step::Key("up"),
            Step::Await("value: 12 '11'", 30_000),
            Step::Wait(2_500),
            Step::Shot("numbers"),
            Step::Aim(Aim::Close("Numbers")),
            Step::Click,
            Step::Await("closed: UserClosing 12", 30_000),
            Step::Await("dotnet: numbers.dll: Main returned 0", 30_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N7h: клавиатура и мышь у формы. Клавиши и строки — те же, что
            // у пробы в Windows (`build/n7h/keyprobe`, нажатия keybd_event):
            // число набирается в поле и принимается по Enter; Tab идёт от поля
            // к кнопке и к отмеченному переключателю; стрелка ведёт отметку по
            // группе — соседний теряет её раньше, чем приходит Enter; Shift+Tab
            // назад; число за краем подрезается, когда фокус ушёл, — после
            // Enter у кнопки. Затем сочетания меню (второе — во вложенном
            // подменю), пробел на кнопке, подменю стрелками и подсказка у кнопки
            // наведением мыши. Tab с этой фазы достаётся окну, окна переключает
            // Alt+Tab.
            Step::Line("dotnet /usr/share/dotnet/samples/keys.dll"),
            Step::Await("dotnet: window 'Keys' opened, 320x200", 60_000),
            Step::Await("shown: numericUpDown1 5 '5' O, Control 2 True | True False False", 60_000),
            Step::Key("end"),
            Step::Key("backspace"),
            Step::Type("42"),
            Step::Key("ret"),
            Step::Await("value: 42", 30_000),
            Step::Key("tab"),
            Step::Await("focus: button1", 30_000),
            Step::Key("tab"),
            Step::Await("focus: radioButton1", 30_000),
            Step::Key("down"),
            Step::Await("radio: Red False", 30_000),
            Step::Await("radio: Green True", 30_000),
            Step::Await("focus: radioButton2", 30_000),
            Step::Key("shift-tab"),
            Step::Await("focus: button1", 30_000),
            Step::Key("shift-tab"),
            Step::Await("focus: numericUpDown1", 30_000),
            Step::Key("end"),
            Step::Key("backspace"),
            Step::Key("backspace"),
            Step::Type("250"),
            Step::Key("tab"),
            Step::Await("focus: button1", 30_000),
            Step::Await("value: 100", 30_000),
            Step::Key("ctrl-o"),
            Step::Await("open: &Open", 30_000),
            Step::Key("ctrl-shift-s"),
            Step::Await("recent: second.txt", 30_000),
            Step::Key("spc"),
            Step::Await("hello", 30_000),
            // Меню «File» щелчком, вниз до «Recent», вправо — подменю с уже
            // выбранной первой строкой, вниз — вторая.
            Step::Aim(Aim::Program("Keys", 20, 12)),
            Step::Click,
            Step::Wait(1_000),
            Step::Key("down"),
            Step::Key("down"),
            Step::Key("right"),
            Step::Key("down"),
            // Отладочная сборка перерисовывает эту форму по нескольку секунд на
            // клавишу (замер: 2–8 с от клавиши до строки программы), а клавиш
            // здесь четыре подряд. С паузой 2.5 с снимок показывал форму ещё до
            // щелчка по меню.
            Step::Wait(10_000),
            Step::Shot("keys-submenu"),
            Step::Key("ret"),
            Step::Await("recent: second.txt", 30_000),
            // Середина кнопки «Hello»: 160 + 94/2, 38 + 29/2 от угла содержимого.
            Step::Aim(Aim::Program("Keys", 207, 52)),
            Step::Await("tip: button1 'Says hello'", 30_000),
            // Строка приходит до показа (Popup — событие «перед»), а кадр с
            // подсказкой — через перерисовку формы. Висит она 30 с
            // (AutoPopDelay в образце), так что паузе есть куда уместиться.
            Step::Wait(10_000),
            Step::Shot("keys-tooltip"),
            Step::Aim(Aim::Close("Keys")),
            Step::Click,
            Step::Await("closed: UserClosing 100 False True False", 30_000),
            Step::Await("dotnet: keys.dll: Main returned 0", 30_000),
            Step::Await("freeos> ", 15_000),
            // Фаза N8: «Файлы» запускают .NET-программу. Рядом с `winforms.dll`
            // лежит её `.runtimeconfig.json` — так её оставляет `dotnet build`,
            // — и Enter по ней не открывает просмотр, а запускает `/bin/dotnet`.
            // Строки списка идут по имени: последняя —
            // `winforms.runtimeconfig.json`, над ней сама сборка.
            Step::Line("run -b /bin/files /usr/share/dotnet/samples"),
            Step::Await("window      : 'Files' at", 30_000),
            Step::Await("files: /usr/share/dotnet/samples has ", 30_000),
            Step::Key("end"),
            Step::Await("selected 'winforms.runtimeconfig.json'", 15_000),
            Step::Key("up"),
            Step::Await("selected 'winforms.dll'", 15_000),
            Step::Key("ret"),
            Step::Await("files: started '/bin/dotnet /usr/share/dotnet/samples/winforms.dll' as #", 15_000),
            Step::Await("dotnet: window 'Form1' opened, 800x450", 60_000),
            Step::Wait(2_500),
            Step::Shot("files-winforms"),
            Step::Aim(Aim::Close("Form1")),
            Step::Click,
            Step::Await("dotnet: winforms.dll: Main returned 0", 30_000),
            Step::Aim(Aim::Close("Files")),
            Step::Click,
            Step::Await("files: closing on request", 15_000),
            Step::Absent("finally after Environment.Exit must not run"),
            Step::Absent("dotnet: error"),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "drawing",
        about: "System.Drawing как в GDI+ (фаза N9): Bitmap с чтением точек, перья, кисти, пути, преобразования, отсечение и форма.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Каждая строка — та же, что печатает GDI+ на Windows (сверено
            // `cargo xtask clr-check` и тестом `clr-vm`): точки прочитаны из
            // Bitmap. Строки выбраны так, что каждая ломается от своего дефекта
            // растеризатора: центры точек и правый край заливки, половинное
            // покрытие сглаженного края, оба конца тонкой линии и её ступени,
            // округление смешивания, градиент, увеличенная картинка, поворот,
            // виды точек пути, отсечение эллипсом и область с вычитанием.
            Step::Line("dotnet /usr/share/dotnet/samples/drawing.dll"),
            Step::Await("drawing: start", 60_000),
            Step::Await("fill: 255 255 0 0 0 | 0 0 255 255 | 255 0 | 255 255", 120_000),
            Step::Await("antialias: 255 255 127 0 0 | 0 0 127 255", 60_000),
            Step::Await("smooth ellipse: True 0 255", 60_000),
            Step::Await("line: 255 255 0 0 0 | 0 0 0 255 255 | 2-4 5-8 9-12 13-16 17-20 21-24 25-28 29-30", 60_000),
            Step::Await("wide 5: ..#####.. 255 255 0 0", 60_000),
            Step::Await("joins: #.###..#.", 60_000),
            Step::Await("blend: 128,0,0,255 178,143,0,111 255,127,127,255 255,55,133,33", 60_000),
            Step::Await("gradient: 0 64 128 191 249 | True True", 60_000),
            Step::Await("texture: 255,255,0,0 255,0,255,0 255,0,0,255 255,0,0,0", 60_000),
            Step::Await("scaled: 0 0 127 255 255 255,0,0,255 255,191,0,64 255,255,0,0", 60_000),
            Step::Await("invert: 0.433,-0.25,0.167,0.289,-7.663,-3.274", 60_000),
            Step::Await("rotate transform: 0,1,-1,0,30,0 ##..#..", 60_000),
            Step::Await("arcs: 4 0,3,3,3,1,3,3,3,3,3,3,3,3,3 | 5 0,1,3,3,131", 60_000),
            Step::Await("clip path: #..# 255,0,0,255 255,0,0,0", 60_000),
            Step::Await("fill region: #.#.#", 60_000),
            // Фаза N9c: текст системным шрифтом в кегле программы — вдвое
            // крупнее, повёрнутый, по центру прямоугольника, отсечённый. Шрифт
            // здесь настоящий (в песочнице clr-check — прямоугольники на месте
            // знаков), а строки — те же отношения, что у GDI+.
            Step::Await("measure: True True True True {Width=0, Height=0} True True", 60_000),
            Step::Await("text: True True True", 60_000),
            Step::Await("rotated text: True True True", 60_000),
            Step::Await("centered text: Center Center True True True", 60_000),
            Step::Await("clipped text: True True", 60_000),
            Step::Await("buffer: True True 255,240,244,248 255,255,140,0", 60_000),
            // Форма рисует в окно тем же растеризатором: картинку из Bitmap,
            // повёрнутые полупрозрачные лучи, штриховку, отсечённую эллипсом, и
            // кривую штрих-пунктиром. Доказательство — строка после рисования;
            // снимок смотреть глазами.
            Step::Await("dotnet: window 'Drawing' opened, 360x240", 60_000),
            Step::Await("shown: 360x240", 60_000),
            Step::Await("paint: {X=0,Y=0,Width=360,Height=240} True True", 120_000),
            Step::Wait(5_000),
            Step::Shot("drawing"),
            Step::Aim(Aim::Close("Drawing")),
            Step::Click,
            Step::Await("closed: UserClosing", 60_000),
            Step::Await("drawing: done", 30_000),
            Step::Await("dotnet: drawing.dll: Main returned 9", 30_000),
            Step::Await("freeos> ", 15_000),
            Step::Absent("dotnet: error"),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "persist",
        about: "Записанное переживает выключение: та же машина, тот же диск, новая загрузка.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Это отдельная загрузка того же диска. Всё, что проверяется ниже,
            // записал предыдущий сценарий и с тех пор машина была выключена, —
            // то есть проверяется носитель, а не память.
            Step::Await("root        : ext2 at LBA", BOOT),
            // Между корнем и приглашением лежит вся остальная загрузка:
            // композитор рисует первый кадр 1280x720, супервизор поднимает
            // службы, а `sshd` трижды не встаёт с паузой в полсекунды. В
            // отладочной сборке это **десятки** секунд, и тридцати перестало
            // хватать — сценарий объявлял зависанием обычную медленную
            // загрузку, причём гость в это время благополучно доходил до
            // оболочки (проверено запуском того же образа руками).
            //
            // Промежуточный шаг не украшение: он делит ожидание надвое, и
            // следующий отказ скажет, встал ли стол, — а не только то, что
            // приглашения нет.
            Step::Await("console     : screen handed over", 60_000),
            Step::Await("freeos> ", 60_000),
            Step::Line("cat /home/roman/notes/first.txt"),
            Step::Await("persisted-by-the-shell", 15_000),
            Step::Line("cat /home/roman/from-a-program.txt"),
            Step::Await("written from ring 3", 15_000),
            // А удалённого нет: удаление тоже пережило выключение.
            Step::Line("cat /home/roman/scratch.txt"),
            Step::Await("no such file or directory", 15_000),
            // И то, что записал установщик, на месте: новая запись ничего не
            // испортила в чужих файлах.
            Step::Line("cat /etc/system.cfg"),
            Step::Await("language=", 15_000),
            Step::Line("whoami"),
            Step::Await("roman (uid 1000 gid 1000)", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "dotnet-files",
        about: "Программа на C# пишет, читает, переносит и удаляет файлы на установленном диске, а оболочка видит результат.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Файлам нужен носитель, который пишется: у «живой» загрузки корень —
            // образ в памяти только для чтения. Сеанс здесь — `roman`, и
            // относительные пути программы считаются от `/home/roman` (фаза N5a).
            Step::Await("root        : ext2 at LBA", BOOT),
            Step::Await("console     : screen handed over", 60_000),
            Step::Await("freeos> ", 60_000),
            Step::Line("dotnet /usr/share/dotnet/samples/files.dll"),
            Step::Await("files: start", 60_000),
            Step::Await("31 3 [first line|вторая строка|third] 43 208 вторая 43", 60_000),
            Step::Await("a.md,data.bin,notes.txt | notes.txt | logs | b.txt,notes.txt | app.log,b.txt,old", 60_000),
            Step::Await("read: FileNotFoundException missing.txt", 60_000),
            Step::Await("rmdir: IOException", 60_000),
            Step::Await("False app.log,b.txt,moved.txt", 60_000),
            Step::Await("files: done", 60_000),
            Step::Await("dotnet: files.dll: Main returned 13", 60_000),
            Step::Await("freeos> ", 15_000),
            // Оболочка видит, что программа оставила.
            Step::Line("cat /home/roman/n5-files/from-dotnet.txt"),
            Step::Await("written by a C# program", 15_000),
            Step::Line("ls /home/roman/n5-files/archive"),
            Step::Await("moved.txt", 15_000),
            // Второй запуск начинает с удаления всего дерева — рекурсивное
            // удаление на настоящем ext2, а не в песочнице хоста.
            Step::Line("dotnet /usr/share/dotnet/samples/files.dll"),
            Step::Await("False False", 60_000),
            Step::Await("dotnet: files.dll: Main returned 13", 60_000),
            Step::Absent("dotnet: error"),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "filemap-big",
        about: "Программа обходит файл в 200 МиБ, а ядро держит из него не больше шестнадцати.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[Arch::X86_64],
        reboots: false,
        updates: false,
        big_file: true,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Считаем кадры до, чтобы сверить после.
            //
            // Прогрева, каким начинается сценарий `mmap`, здесь нет, и это
            // осознанный риск, а не забывчивость. Там он нужен был потому, что
            // первый запуск программы стоит того, что запуском не является;
            // здесь то же рассуждение почти не работает: куча ядра — это
            // фиксированный диапазон, отображённый при старте
            // (`HEAP_BASE..HEAP_BASE+HEAP_SIZE`), так что ни узел VFS, ни
            // очередь резидентных страниц кадров не занимают, а образ, стек и
            // таблицы задачи возвращаются целиком (это отдельно проверяет
            // сценарий `userspace`). Останется одно окно неопределённости —
            // окно DMA дискового драйвера, которое умеет расти один раз. К
            // первому `mem` корень уже смонтирован и с него прочитаны и
            // `/etc/passwd`, и образ оболочки, то есть окно своё уже взяло.
            //
            // Дрожащей она оказалась, и не из-за окна DMA. Сеть в этом
            // прогоне выключена, `sshd` не может занять порт и падает, а
            // надзиратель перезапускает его трижды с паузами. Первый `mem`
            // попадал в случайную точку этой пляски: иногда адресное
            // пространство `sshd` — семьсот восемьдесят девять кадров — уже
            // числилось за ним, иногда ещё нет. Второй `mem` брался через пять
            // минут, когда `sshd` давно сдался, и числа расходились ровно на
            // эти семьсот восемьдесят девять.
            //
            // Поэтому счёт начинается там, где система перестала шевелиться
            // сама по себе. Строка приходит после третьего отказа, то есть
            // через несколько секунд после приглашения.
            Step::Await("init: 'sshd' failed 3 time(s) in a row, giving up", 60_000),
            Step::Line("mem"),
            Step::Capture("  frames   ", 15_000),

            Step::Line("run /bin/filemap /media/big.dat"),
            // Размер точный и это намеренно: файл кладёт хост, и «файл на месте»
            // здесь означает ещё и «положен целиком». Двести мебибайт при блоке
            // 4 КиБ — это 51 200 блоков, то есть двойная косвенность в inode;
            // оборвись она на переходе через таблицу, размер вышел бы другим.
            Step::Await(crate::package::BIG_FILE_SIZE_LINE, 60_000),
            // Ответ, посчитанный по каждому сотому байту. Число выписано в
            // `crate::package`, а не здесь, и не для красоты: рядом с ним стоит
            // тест, который пересчитывает его тем же генератором, каким файл
            // наполнялся, — то есть напечатанное гостем сверяется со счётом,
            // который на госте не выполнялся.
            //
            // Десять минут — не запас на всякий случай, а оценка: 51 200
            // страниц, каждая приходит отдельным чтением с виртуального диска,
            // и 47 104 из них вытесняются по дороге. Под QEMU без ускорения это
            // минуты.
            Step::Await(crate::package::BIG_FILE_CHECKSUM_LINE, 900_000),

            // Вот ради этих двух чисел вся фаза, и оба проверяются здесь.
            //
            // Первое: страниц прочитано не меньше, чем их в файле. Обход идёт
            // строго вперёд, шагом меньше страницы, поэтому каждая страница
            // читается ровно один раз — 209 715 200 / 4096 = 51 200. Меньше
            // означало бы, что часть файла программа не увидела вовсе.
            Step::AtLeast("  user        : file mapping released, ", 16_384, 30_000),
            // Второе, и оно и есть утверждение фазы: вытеснения были. Предел
            // резидентных страниц в ядре — 4096, то есть арифметика даёт
            // 51 200 − 4096 = 47 104. Порог взят ниже (40 000) намеренно:
            // сверка с точным 47 104 была бы пересказом константы ядра — измени
            // её кто-нибудь, и сценарий упал бы, ничего не сообщив о подкачке.
            // Сорок тысяч не проходят ни при каком пределе, при котором файл
            // помещается целиком, и проходят при любом разумном.
            //
            // Вместе эти две строки говорят то, чего по отдельности не говорит
            // ни одна: программа прошла двести мебибайт, а ядро не держало из
            // них больше шестнадцати.
            Step::AtLeast(" reads and ", 12_000, 30_000),
            // Обе проверки стоят **до** строки самой программы, и это не
            // вкусовщина: печатает их `munmap` в ядре, то есть они приходят
            // раньше, чем программа успевает сказать «unmapped», а ожидание
            // ищет только вперёд от курсора. Поставь их после — и они искали бы
            // то, что уже проехало.
            Step::Await("filemap: unmapped", 30_000),
            Step::Await("exited with code 0", 30_000),

            // И кадры вернулись все до одного. Число подставляется то самое,
            // что снято выше: отображение файла занимает кадры и отпускает их
            // на вытеснении, то есть путь возврата здесь исполняется сорок семь
            // тысяч раз — потеряй он по странице на тысячу вызовов, разница
            // была бы видна невооружённым глазом.
            Step::Line("mem"),
            Step::Await("  frames   {} of", 30_000),

            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
            // Ядро печатает это, если при возврате области нашлось не столько
            // страниц, сколько за ней числилось.
            Step::Absent("user        : WARNING"),
        ],
    },
    Scenario {
        name: "power",
        about: "Машину можно выключить и перезагрузить, и том закрывается за собой чисто.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        // Единственный сценарий, в котором перезагрузка — цель, а не симптом.
        reboots: true,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Первая загрузка. Каким том был до неё, не утверждаем: предыдущий
            // сценарий закончился снятием QEMU, но порядок сценариев — не то, на
            // чём стоит строить проверку. Обе интересные ситуации сценарий
            // создаёт сам, ниже.
            Step::Await("root        : ext2 at LBA", BOOT),
            Step::Await("freeos> ", 30_000),

            // --- случай первый: машину бросили ---------------------------
            //
            // Запись меняет счётчики свободного, а признак «том используется»
            // стоит на диске с самого монтирования. Дальше машина сбрасывается
            // без предупреждения — это и есть пропавшее питание.
            Step::Line("echo before-the-crash > /home/roman/crash.txt"),
            Step::Await("wrote 17 bytes", 15_000),
            Step::Reset,
            // Вторая загрузка обязана сказать, что том закрыт не был. Строка
            // печатается при монтировании и до всякой оболочки.
            Step::Await("state       : volume was NOT unmounted cleanly", BOOT),
            Step::Await("freeos> ", 30_000),
            // Данные при этом целы: том грязный — это про счётчики, а не про
            // содержимое файлов.
            Step::Line("cat /home/roman/crash.txt"),
            Step::Await("before-the-crash", 15_000),

            // --- случай второй: машину перезагрузили командой -------------
            Step::Line("reboot"),
            Step::Await("restarting", 15_000),
            Step::Await("volume      : / flushed and marked clean", 30_000),
            Step::Await("power       : ", 15_000),
            // Третья загрузка — и вот теперь том обязан быть чистым. Это и есть
            // всё утверждение фазы 27, целиком и в одной строке.
            Step::Await("state       : volume was unmounted cleanly", BOOT),
            Step::Await("freeos> ", 30_000),

            // --- случай третий: машину выключили --------------------------
            //
            // Здесь проверяется то, чего не случалось ни разу за двадцать шесть
            // фаз: процесс QEMU заканчивается сам, потому что гость снял
            // питание.
            Step::Line("shutdown"),
            Step::Await("shutting down", 15_000),
            Step::Await("volume      : / flushed and marked clean", 30_000),
            Step::Exits(30_000),
            Step::Absent("KERNEL PANIC"),
            Step::Absent("the machine refused to go down"),
        ],
    },
    Scenario {
        name: "fsck",
        about: "Том, брошенный без выключения, проверяется и чинится самой системой при загрузке.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        // Сброс посреди работы — это и есть та поломка, ради которой фаза
        // существует; после него машина обязана подняться сама.
        reboots: true,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("root        : ext2 at LBA", BOOT),
            Step::Await("freeos> ", 30_000),
            // Проверка живого тома: она ничего не чинит и на исправной системе
            // обязана молчать. Если бы она находила «поломки» на работающей
            // машине, всё остальное в этом сценарии ничего не значило бы.
            Step::Line("fsck"),
            Step::Await(": the volume is consistent", 60_000),
            // Теперь ломаем: пишем файл и выдёргиваем шнур.
            Step::Line("echo written-before-the-crash > /home/roman/crash.txt"),
            Step::Await("wrote 25 bytes", 15_000),
            Step::Reset,
            // Следующая загрузка обязана заметить и проверить — сама, без
            // единой команды. Порядок строк тоже проверяется: сначала «том
            // закрыт не был», потом проверка, и только потом монтирование.
            // Порядок шагов повторяет порядок **строк**: корень монтируется
            // раньше, чем проверяется том состояния, потому что состояние
            // монтируется вторым. Ожидание ищет только вперёд, и шаг,
            // поставленный по порядку событий в голове, а не в журнале, ждал бы
            // строку, которая уже проехала.
            Step::Await("root        : ext2 at LBA", BOOT),
            Step::Await("fsck        : checking the state volume (it was not closed cleanly)", 60_000),
            Step::Await("fsck        : state: ", 60_000),
            Step::Await("freeos> ", 30_000),
            // Данные на месте: проверка чинит счётчики, а не выбрасывает файлы.
            Step::Line("cat /home/roman/crash.txt"),
            Step::Await("written-before-the-crash", 15_000),
            // И том после починки согласован — это говорит уже живая проверка.
            Step::Line("fsck"),
            Step::Await(": the volume is consistent", 60_000),
            Step::Line("shutdown"),
            Step::Await("volume      : / flushed and marked clean", 30_000),
            Step::Exits(30_000),
            Step::Absent("KERNEL PANIC"),
            // Проверка обязана быть беззвучной там, где она не нужна: на
            // чистом томе её не запускают вовсе.
            Step::Absent("fsck        : the check itself failed"),
        ],
    },
    Scenario {
        name: "recovery",
        about: "Меню загрузчика: безопасный режим с проверкой диска и корнем только на чтение.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Клавиши жмутся **заранее**: прошивка копит нажатия в буфере, а
            // окно ожидания у загрузчика короткое — полсекунды, потому что эти
            // полсекунды платит каждая загрузка. Попасть в него нажатием «в
            // нужный момент» нельзя, и это не недостаток стенда: человек за
            // клавиатурой попадает в меню ровно тем же способом — держит
            // клавишу с самого включения.
            Step::Repeat("s", 4),
            // ...и второй раз — уже после того, как загрузчик заговорил. На
            // x86-64 хватает первых: нажатия копит контроллер клавиатуры. На
            // машине, где клавиатура приходит по USB, до её инициализации
            // прошивкой нажимать некуда, поэтому нужны вторые — их ловит
            // поздний опрос, тот, что идёт перед самой передачей управления.
            Step::Await("FreeOS bootloader", BOOT),
            Step::Repeat("s", 8),
            Step::Await("---- boot menu", 120_000),
            // Четвёртый пункт: безопасный режим **и** проверка тома. Проверяем
            // оба флага разом — иначе пришлось бы грузиться дважды ради двух
            // битов в одном поле.
            Step::Key("4"),
            Step::Await("  safe mode with a disk check", 15_000),
            // Ядро обязано сказать, каким режимом грузится: пропавший рабочий
            // стол иначе выглядит поломкой.
            Step::Await("boot        : safe mode, no desktop, root read-only", BOOT),
            Step::Await("boot        : the root volume will be checked before mounting", 15_000),
            // Проверка идёт даже на чистом томе — об этом и просили.
            Step::Await("fsck        : checking the root volume (asked for", 60_000),
            Step::Await("root        : mounted read-only", 60_000),
            Step::Await("compositor  : not started, safe mode", 30_000),
            Step::Await("freeos> ", 30_000),
            // Читать можно всё.
            Step::Line("cat /etc/system.cfg"),
            Step::Await("language=", 15_000),
            // А писать — нельзя, и отказ называет причину, а не притворяется
            // нехваткой прав или поломкой носителя.
            Step::Line("echo no > /home/roman/x.txt"),
            Step::Await("the volume is mounted read-only", 15_000),
            // Проверка тома работает и здесь: это и есть консоль
            // восстановления — оболочка и `fsck` на месте, том не тронут.
            Step::Line("fsck"),
            Step::Await(": the volume is consistent", 60_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "display-keep",
        about: "Меню загрузчика, пятый пункт: режим экрана прошивки не меняется, список режимов напечатан, стол встаёт.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // В меню попадаем так же, как в `recovery`: клавиши жмутся заранее
            // и ещё раз, когда загрузчик заговорил.
            Step::Repeat("s", 4),
            Step::Await("FreeOS bootloader", BOOT),
            Step::Repeat("s", 8),
            Step::Await("---- boot menu", 120_000),
            // Пункт для машины, у которой экран гаснет от смены режима (ASUS
            // K53SD). Проверяем то, что увидит человек на фотографии: режим
            // прошивки и список режимов — до того, как что-то поменялось.
            Step::Key("5"),
            Step::Await("  keeping the firmware's display mode", 15_000),
            Step::Await("GraphicsOutput handle(s); firmware mode ", 15_000),
            Step::Await("  [gop] modes 0:", 15_000),
            Step::Await("  press any key to boot", 15_000),
            Step::Key("ret"),
            Step::Await("  [gop] keeping the firmware's ", 15_000),
            // Стол обязан встать в любом режиме, который дала прошивка.
            Step::Await("desktop     : ", BOOT),
            Step::Absent("[gop] switched to"),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "one-cpu",
        about: "Меню загрузчика, шестой пункт: из четырёх процессоров будится один, система встаёт.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &["-smp", "4"],
        steps: &[
            // В меню попадаем так же, как в `recovery`.
            Step::Repeat("s", 4),
            Step::Await("FreeOS bootloader", BOOT),
            Step::Repeat("s", 8),
            Step::Await("---- boot menu", 120_000),
            // Пункт для машины, которая встаёт при запуске остальных
            // процессоров (ASUS K53SD). Процессоров у машины четыре — и ядро
            // обязано это сказать, а будить не обязано.
            Step::Key("6"),
            Step::Await("  one processor: the others stay asleep", 15_000),
            Step::Await("smp         : 1 of 4 processors online, the others stay asleep", BOOT),
            Step::Await("desktop     : ", BOOT),
            Step::Absent("cpu 1 online"),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "powerbtn",
        about: "Кнопка питания: событие ACPI доходит до системы, и она гаснет по-настоящему.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        // Только x86-64, и это не упущение. В QEMU `virt` кнопку приносит ACPI
        // GED, описанный в AML: без интерпретатора событие не разобрать, и ядро
        // говорит об этом вслух при загрузке. Проверяем то, что работает.
        arches: &[Arch::X86_64],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Кнопка заведена при загрузке — и сказано, каким вектором и с
            // какого входа. Без этой строки «нажали и выключилось» не отличить
            // от «нажали, а выключил кто-то другой».
            Step::Await("power       : power button on vector", BOOT),
            Step::Await("freeos> ", 30_000),
            Step::PowerButton,
            // Нажатие видит обработчик прерывания, а печатает задача: строка
            // приходит уже из неё.
            Step::Await("power       : power button pressed", 30_000),
            Step::Await("volume      : / flushed and marked clean", 30_000),
            Step::Exits(30_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "ahci",
        about: "Тот же установленный диск, подключённый по SATA: корень находится через AHCI.",
        target: Target::LiveAndDisk,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Ahci,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Сначала — что найден именно контроллер AHCI, а не что-то, что
            // сработало по случайности. Версия и маска портов приходят из его
            // собственных регистров: прочитать их и не иметь контроллера нельзя.
            Step::Await("ahci        : version", BOOT),
            Step::Await("ahci        : port 0:", 30_000),
            // Диск найден слоем блочных устройств и назван своим именем. До
            // Phase 26a эта строка была бы «virtio-blk» при любом контроллере,
            // потому что другого драйвера не существовало.
            Step::Await("disk        : ahci #0", 30_000),
            // И главное: корень найден **на нём**. Раздел ищется по типу GUID
            // среди всех носителей, поэтому строка называет, на каком именно он
            // нашёлся, — иначе проверка не отличила бы AHCI от virtio.
            Step::Await("on ahci #0 at LBA", 30_000),
            Step::Await("root        : ext2 at LBA", 30_000),
            // Дальше — обычная жизнь системы, но каждое обращение к диску идёт
            // через новый драйвер: чтение файла установщика...
            Step::Await("account     : /etc/passwd", 30_000),
            Step::Await("freeos> ", 30_000),
            Step::Line("cat /etc/system.cfg"),
            Step::Await("language=", 15_000),
            // ...и запись, которой у драйвера свой путь. Файл читается обратно
            // в этой же загрузке: чтение после записи проверяет, что записано
            // именно то и именно туда.
            Step::Line("echo written-over-sata > /home/roman/sata.txt"),
            Step::Await("wrote 18 bytes", 15_000),
            Step::Line("cat /home/roman/sata.txt"),
            Step::Await("written-over-sata", 15_000),
            Step::Line("whoami"),
            Step::Await("roman (uid 1000 gid 1000)", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "nvme",
        about: "Тот же диск на шине PCI Express: очереди, звонок и бит фазы вместо портов.",
        target: Target::LiveAndDisk,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Nvme,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Версия и шаг звонков читаются из регистров контроллера: получить
            // их и не иметь контроллера нельзя.
            Step::Await("nvme        : version", BOOT),
            // Ёмкость пришла из Identify Namespace, то есть административная
            // очередь работает: команда дошла, ответ разобран по биту фазы.
            Step::Await("nvme        : ", 30_000),
            Step::Await("disk        : nvme #0", 30_000),
            // Корень найден именно на нём. Диска два — VVFAT, с которого
            // грузилась прошивка, и этот; строка называет, где нашёлся раздел.
            Step::Await("on nvme #0 at LBA", 30_000),
            Step::Await("root        : ext2 at LBA", 30_000),
            Step::Await("account     : /etc/passwd", 30_000),
            Step::Await("freeos> ", 30_000),
            // Чтение через очередь ввода-вывода — она отдельная от
            // административной, и до этой строки ни одной команды в ней не было.
            Step::Line("cat /etc/system.cfg"),
            Step::Await("language=", 15_000),
            Step::Line("echo written-over-nvme > /home/roman/nvme.txt"),
            Step::Await("wrote 18 bytes", 15_000),
            Step::Line("cat /home/roman/nvme.txt"),
            Step::Await("written-over-nvme", 15_000),
            Step::Line("whoami"),
            Step::Await("roman (uid 1000 gid 1000)", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "pkg",
        about: "Пакет ставится с носителя, запускается из /opt, проверяется и сносится без следа.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Уборка перед началом — по той же причине, что в сценарии `write`:
            // диск переживает прогон, и второй запуск иначе падал бы на «уже
            // установлен». Отказы уборки не проверяются: на свежей системе
            // сносить нечего.
            Step::Line("run /bin/pkg remove extra"),
            Step::Await("freeos> ", 30_000),
            Step::Line("run /bin/pkg remove hello"),
            Step::Await("freeos> ", 30_000),

            // Зависимость проверяется **до** первой записи: пакет, которому
            // нужен `hello`, обязан не встать, пока того нет.
            Step::Line("run /bin/pkg install /media/extra-1.0.fpk"),
            Step::Await("requires hello, which is not installed", 30_000),

            // Теперь сам пакет. Число файлов в строке точное: «что-то
            // поставилось» выглядело бы так же, как «поставилось не то».
            Step::Line("run /bin/pkg install /media/hello-1.0.fpk"),
            Step::Await("pkg: installed hello 1.0, 2 file(s)", 60_000),

            // Главное утверждение фазы: программа, которой в системе не было,
            // приехала пакетом и работает. `/bin/greet` не существует — она
            // попадает в систему единственным способом.
            Step::Line("run /opt/hello/bin/greet"),
            Step::Await("greet: installed from a package", 30_000),
            // И файл рядом с ней распаковался целиком, а не «появился».
            Step::Line("cat /opt/hello/share/readme.txt"),
            Step::Await("unpacked by pkg install", 15_000),

            // Проверка на нетронутом пакете обязана молчать.
            Step::Line("run /bin/pkg verify hello"),
            Step::Await("2 file(s) intact, 0 changed", 30_000),

            // Зависимый пакет теперь ставится.
            Step::Line("run /bin/pkg install /media/extra-1.0.fpk"),
            Step::Await("pkg: installed extra 1.0", 60_000),
            Step::Line("run /bin/pkg list"),
            Step::Await("pkg: 2 package(s) installed", 30_000),

            // Порча файла обязана быть замечена. Меняется именно программа:
            // подменённый исполняемый файл — то, ради чего проверка и нужна.
            // Пауза, а не ожидание приглашения: приглашение к этому моменту уже
            // напечатано, и ждать второго — значит ждать вечно. Пауза же
            // отвечает на настоящий вопрос: гость перерисовывает окно с
            // запрещёнными прерываниями и не разбирает линию, а приёмный FIFO
            // у PL011 — 32 байта, ровно на них строка и обрывалась.
            Step::Wait(2_000),
            Step::Line("echo x > /opt/hello/bin/greet"),
            Step::Await("wrote 2 bytes", 15_000),
            Step::Line("run /bin/pkg verify hello"),
            Step::Await("greet: size differs", 30_000),
            Step::Await("1 file(s) intact, 1 changed", 15_000),

            // Удаление убирает ровно то, что положило.
            Step::Line("run /bin/pkg remove hello"),
            Step::Await("pkg: removed hello, 2 file(s)", 30_000),
            Step::Line("cat /opt/hello/share/readme.txt"),
            Step::Await("no such file or directory", 15_000),
            // Чужого не тронуто: одноимённая программа из `/bin` на месте.
            Step::Line("run /bin/hello"),
            Step::Await("hello from userspace", 30_000),
            // И соседний пакет тоже цел.
            Step::Line("run /bin/pkg verify extra"),
            Step::Await("1 file(s) intact, 0 changed", 30_000),

            // Вторая половина проверки, и она важнее первой: подмена **той же
            // длины**. Размер такую не ловит вовсе, и проверка, состоящая из
            // одного размера, пропустила бы подменённую программу.
            // Пауза, а не ожидание приглашения: приглашение к этому моменту уже
            // напечатано, и ждать второго — значит ждать вечно. Пауза же
            // отвечает на настоящий вопрос: гость перерисовывает окно с
            // запрещёнными прерываниями и не разбирает линию, а приёмный FIFO
            // у PL011 — 32 байта, ровно на них строка и обрывалась.
            Step::Wait(2_000),
            Step::Line("echo tampered-11 > /opt/extra/x.txt"),
            Step::Await("wrote 12 bytes", 15_000),
            Step::Line("run /bin/pkg verify extra"),
            Step::Await("contents changed", 30_000),
            Step::Await("0 file(s) intact, 1 changed", 15_000),

            Step::Line("run /bin/pkg remove extra"),
            Step::Await("pkg: removed extra", 30_000),

            // Фаза N8: программа WinForms пакетом. В её манифесте `start=` — и
            // стол ставит её в «Пуск» при следующем открытии меню, без
            // перезагрузки. Строка пакета стоит прямо над строками питания, и
            // меню ходит по кругу: от первой строки до неё три шага вверх,
            // сколько бы программ ни лежало в `/bin`.
            Step::Line("run /bin/pkg install /media/winforms-1.0.fpk"),
            Step::Await("pkg: installed winforms 1.0, 2 file(s)", 60_000),
            Step::Wait(1_000),
            Step::Key("f1"),
            Step::Await("desktop     : menu opened", 15_000),
            Step::Await("desktop     : start menu offers packages: winforms", 15_000),
            Step::Repeat("up", 3),
            Step::Wait(1_500),
            Step::Shot("winforms-menu"),
            Step::Key("ret"),
            Step::Await("desktop     : started '/bin/dotnet /opt/winforms/winforms.dll' as ", 15_000),
            Step::Await("dotnet: window 'Form1' opened, 800x450", 60_000),
            Step::Wait(2_500),
            Step::Shot("winforms-window"),
            Step::Aim(Aim::Close("Form1")),
            Step::Click,
            // Строку фокуса стол печатает, закрывая окно, — раньше последней
            // строки самой программы.
            Step::Await("desktop     : focus 'Terminal'", 30_000),
            Step::Await("dotnet: winforms.dll: Main returned 0", 30_000),
            Step::Wait(1_000),
            Step::Line("run /bin/pkg remove winforms"),
            Step::Await("pkg: removed winforms, 2 file(s)", 30_000),

            // Вторая половина фазы: то же самое кнопками, без единой набранной
            // команды. Проверяется не `pkg` — он проверен выше, — а то, что
            // окно «Параметры» действительно его запускает: строка
            // `settings : started ...` печатается только этим путём.
            //
            // Всё клавиатурой: попадание мышью в строку списка зависит от
            // масштаба шрифта, а он на двух архитектурах разный.
            Step::Wait(2_000),
            Step::Key("f1"),
            Step::Await("desktop     : menu opened", 15_000),
            // Один шаг вниз, а не два. Было два, и это сломалось молча в фазе
            // 47c: оттуда `App::Files` из меню исчез (файловый менеджер стал
            // программой), «Параметры» переехали с третьей строки на вторую, и
            // сценарий стал открывать «О системе». Нашлось это только в 48-й, и
            // ровно потому, что стенд считал пункты **числом** — тот самый
            // способ, про который в памяти проекта записано «называть поимённо».
            // Назвать здесь нечем: строку меню сценарий выбирает клавишами.
            Step::Key("down"),
            Step::Key("ret"),
            Step::Await("desktop     : opened 'Settings'", 15_000),
            // «Пакеты» — пятый раздел слева: с фазы 48 перед ним встали «Дата и
            // время», «Сеть», «Диски» и «Пользователи».
            Step::Repeat("down", 5),
            Step::Key("ret"),
            Step::Wait(1_500),
            Step::Shot("01-programs"),
            // Единственный пункт пустого списка — «поставить пакет». За ним
            // список файлов: контейнеры обновления системы в него не попадают,
            // хотя лежат в том же `/media` и с тем же расширением.
            Step::Key("ret"),
            Step::Wait(1_500),
            Step::Shot("02-choose"),
            // Первым идёт `extra`, вторым `hello`; ставим второй — первый
            // требует его и не встал бы.
            Step::Key("down"),
            Step::Key("ret"),
            Step::Await("settings    : started '/bin/pkg install /media/hello-1.0.fpk'", 15_000),
            Step::Await("pkg: installed hello 1.0, 2 file(s)", 60_000),
            // Перечитать список: окно рисует то, что видит, а увидеть новое
            // оно может только заново пройдясь по реестру.
            Step::Key("left"),
            Step::Key("right"),
            Step::Wait(1_500),
            Step::Shot("03-installed"),
            // Теперь удаление — оно спрашивает подтверждения, и это отдельное
            // нажатие: снести пакет промахом мимо строки нельзя.
            Step::Key("ret"),
            Step::Wait(1_500),
            Step::Shot("04-remove-confirm"),
            Step::Key("ret"),
            Step::Await("settings    : started '/bin/pkg remove hello'", 15_000),
            Step::Await("pkg: removed hello, 2 file(s)", 60_000),

            // Окно закрывается **до** команды оболочке, и это не уборка: ввод
            // достаётся активному окну, а активно сейчас окно «Параметры».
            // Строка `exit`, приехавшая по серийной линии, ушла бы в него и
            // пропала — первый прогон именно так и встал.
            Step::Key("ctrl-w"),
            Step::Await("desktop     : closed 'Settings'", 15_000),
            Step::Wait(2_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "services",
        about: "Убитая служба поднимается сама; падающая по кругу — останавливается, а система жива.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Супервизор поднимается **при загрузке**, без единой команды: это
            // и есть разница между «есть программа init» и «в системе есть
            // службы».
            Step::Await("services    : /bin/init started", BOOT),
            Step::Capture("init: started 'logger' as #", 60_000),
            Step::Await("freeos> ", 60_000),
            // `AwaitAny`, а не `Await` и не `Expect`. Строку печатает **служба**,
            // приглашение — оболочка, и порядок между двумя задачами не
            // определён: на AArch64 он оказался обратным, и ожидание приглашения
            // ушло искать второе, которого не бывает. `Expect` тут тоже не
            // годится — он смотрит на буфер сейчас, а служба может ещё
            // загружаться. Нужно и подождать, и посмотреть назад.
            Step::AwaitAny("svclog: up as #", 30_000),

            // Убиваем службу. Номер её задачи взят из журнала, а не написан
            // константой: одна служебная задача, заведённая в ядре, сдвинула бы
            // нумерацию и превратила бы проверку в лотерею.
            Step::Line("kill {}"),
            Step::Await("killed by request", 30_000),
            // Супервизор обязан заметить, сказать и поднять заново.
            Step::Await("init: 'logger' ended with code", 30_000),
            Step::Await("init: restarting 'logger' in 500 ms", 15_000),
            Step::Await("init: started 'logger' as #", 30_000),
            // И поднятая служба снова отзывается — то есть работает, а не
            // «числится запущенной».
            Step::Await("svclog: up as #", 30_000),

            // Вторая половина: служба, падающая при каждом старте. Описание
            // пишется прямо здесь, командой оболочки, — так проверяется и то,
            // что супервизор читает файл, а не зашитый список.
            // Пауза, а не ожидание приглашения: приглашение к этому моменту уже
            // напечатано, и ждать второго — значит ждать вечно. Пауза же
            // отвечает на настоящий вопрос: гость перерисовывает окно с
            // запрещёнными прерываниями и не разбирает линию, а приёмный FIFO
            // у PL011 — 32 байта, ровно на них строка и обрывалась.
            Step::Wait(2_000),
            Step::Line("echo bad /bin/svcbad > /home/roman/s"),
            Step::Await("wrote 16 bytes", 15_000),
            Step::Line("run -b /bin/init /home/roman/s"),
            Step::Await("init: supervising 1 service(s)", 30_000),
            Step::Await("svcbad: failing with code 3", 30_000),
            // Три попытки подряд — и остановка с записью в журнал. Именно это
            // отличает устойчивость от сокрытия поломки.
            Step::Await("init: 'bad' failed 3 time(s) in a row, giving up", 60_000),
            Step::Await("init: nothing left to supervise", 30_000),

            // Система при этом жива и отвечает — ради этого всё и затевалось.
            Step::Line("uptime"),
            Step::Await("timer ticks", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "ssh-shell",
        about: "Вход по ключу: чужой ssh выполняет команду в установленной системе и видит ровно то, что видит этот человек.",
        // Установленная система, а не живая, и это главное решение сценария.
        // Вход по ключу требует учётной записи, домашнего каталога и настоящих
        // прав на файлах — всего того, чего на образе initrd нет и быть не
        // должно. На FAT, где всё `root:root 0755`, проверка прав ничего не
        // проверяла бы.
        //
        // Отсюда и место в списке: после `install`, который делает этот диск, и
        // до `install4k`, который пересоздаёт его с сектором 4096.
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: true,
        e1000: false,
        guest_port: 22,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: true,
        memory: "",
        extra: &[],
        steps: &[
            Step::AwaitAny("account     : /etc/passwd", BOOT),
            Step::AwaitAny("dhcp: lease 10.0.2.15/24", 90_000),
            Step::AwaitAny("sshd: listening on port 22", 90_000),
            // Учётные записи у сервера есть — на живой системе этой строки не
            // бывает, и разница между двумя системами видна сразу.
            Step::AwaitAny("sshd: accounts come from /etc/passwd", 15_000),
            Step::Wait(1_000),

            // 1. Чужой ключ. Проверка, без которой все остальные ничего не
            // значат: сервер, пускающий кого угодно, прошёл бы их все.
            Step::Ssh(SshRun {
                identity: Identity::Stranger,
                command: "uptime",
                stdin: "",
                expect: &["Permission denied"],
                // И ответа команды при этом нет: отказ настоящий, а не «пустили
                // и ничего не дали».
                absent: &["up "],
                timeout_ms: 60_000,
            }),
            Step::AwaitAny("sshd: this key is not in /home/roman/.ssh/authorized_keys", 30_000),

            // 2. Свой ключ — вход и команда. Подпись клиента привязана к
            // идентификатору сеанса, то есть годится ровно для этого
            // соединения; проверяет её чужая библиотека по чужому же формату.
            Step::Ssh(SshRun {
                identity: Identity::Authorized,
                command: "uptime",
                stdin: "",
                expect: &["up ", " ms"],
                absent: &["Permission denied"],
                timeout_ms: 60_000,
            }),
            Step::AwaitAny(
                "sshd: roman authenticated with a key from /home/roman/.ssh/authorized_keys",
                30_000,
            ),
            Step::AwaitAny("sshd: exec 'uptime' for roman", 15_000),
            // Код завершения доехал: `ssh` в чужом скрипте проверяет именно его,
            // и команда, отработавшая с ошибкой, не должна выглядеть удачной.
            Step::AwaitAny("sshd: session closed with status 0", 15_000),

            // 3. Сеанс знает, кого впустили, — а не «кого-нибудь».
            Step::Ssh(SshRun {
                identity: Identity::Authorized,
                command: "id",
                stdin: "",
                expect: &["uid=1000(roman)", "home=/home/roman"],
                absent: &[],
                timeout_ms: 60_000,
            }),

            // 4. Файл `0600` в домашнем каталоге читается — по классу владельца.
            // Его положил установщик ровно для этой проверки. С фазы 38b это
            // делает **программа** `/bin/cat`, запущенная от имени вошедшего;
            // путь абсолютный, потому что текущего каталога в этой системе ещё
            // нет и подставлять его сервером значило бы выдумать его на ходу.
            Step::Ssh(SshRun {
                identity: Identity::Authorized,
                command: "cat /home/roman/notes.txt",
                stdin: "",
                expect: &["This file belongs to you and to nobody else"],
                absent: &["permission denied"],
                timeout_ms: 60_000,
            }),

            // 5. И главное утверждение фазы: по сети видно ровно то же, что
            // видно этому человеку за терминалом. `/root/notes.txt` — файл
            // `0644` в каталоге `0700`, то есть права файла разрешают, а путь
            // не пускает. Правильный ответ — отказ, и текста файла в ответе нет.
            Step::Ssh(SshRun {
                identity: Identity::Authorized,
                command: "cat /root/notes.txt",
                stdin: "",
                expect: &["cat: /root/notes.txt: permission denied"],
                absent: &["world-readable"],
                timeout_ms: 60_000,
            }),

            // 6. Сеанс без команды — оболочка, читающая канал построчно.
            // Клиенту нечем набирать, поэтому команды подаются ему на вход, как
            // это делает всякий скрипт с `ssh`.
            Step::Ssh(SshRun {
                identity: Identity::Authorized,
                command: "",
                stdin: "whoami\nuptime\nexit\n",
                expect: &["freeos-ssh> ", "roman", "up "],
                absent: &["Permission denied"],
                timeout_ms: 60_000,
            }),
            Step::AwaitAny("sshd: interactive session for roman", 30_000),

            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "ssh-exec",
        about: "По сети исполняются программы из /bin — от имени вошедшего, с правами от ядра.",
        // Та же установленная система и то же место в списке, что у
        // `ssh-shell`: нужны настоящие права на файлах, то есть ext2, который
        // записал установщик.
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: true,
        e1000: false,
        guest_port: 22,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: true,
        memory: "",
        extra: &[],
        steps: &[
            Step::AwaitAny("dhcp: lease 10.0.2.15/24", BOOT),
            Step::AwaitAny("sshd: listening on port 22", 90_000),
            Step::Wait(1_000),

            // 1. Программа из `/bin`, а не команда внутри сервера. Строка
            // «user : #N '/bin/ls' as 1000:1000» печатается ядром при запуске —
            // и она же доказывает, от чьего имени всё происходит.
            Step::Ssh(SshRun {
                identity: Identity::Authorized,
                command: "ls /etc",
                stdin: "",
                expect: &["passwd", "system.cfg"],
                absent: &["Permission denied"],
                timeout_ms: 60_000,
            }),
            // Строку печатает ядро при запуске программы, и в ней главное —
            // личность: `uid 1000`, а не `uid 0`. Сервер исполняется от root,
            // и если бы он запускал программы от себя, здесь стоял бы ноль.
            Step::AwaitAny("'/bin/ls' as uid 1000 gid 1000", 30_000),

            // 2. Отказ приходит **от ядра**: `cat` спрашивает `open`, а не
            // сервер — свои таблицы. Проверка та же, что за терминалом, и
            // текста файла в ответе нет.
            Step::Ssh(SshRun {
                identity: Identity::Authorized,
                command: "cat /root/notes.txt",
                stdin: "",
                expect: &["cat: /root/notes.txt: permission denied"],
                absent: &["world-readable"],
                timeout_ms: 60_000,
            }),

            // 3. Свой файл читается той же программой — то есть отказ выше был
            // про права, а не про то, что `cat` не работает вовсе.
            Step::Ssh(SshRun {
                identity: Identity::Authorized,
                command: "cat /home/roman/notes.txt",
                stdin: "",
                expect: &["This file belongs to you and to nobody else"],
                absent: &["permission denied"],
                timeout_ms: 60_000,
            }),

            // 4. Код возврата программы доезжает до клиента: `ssh` в чужом
            // скрипте проверяет именно его.
            Step::AwaitAny("sshd: session closed with status 1", 30_000),

            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "sftp",
        about: "Файлы туда и обратно штатными sftp и scp: мегабайты байт со сверкой, каталоги, отказ ядра, программа WinForms из «Файлов».",
        // Установленная система по той же причине, что у `ssh-shell`: вход по
        // ключу требует учётной записи, а отказ записи в `/etc` — настоящих
        // прав на ext2. Место в списке — рядом с ним, до `install4k`.
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: true,
        e1000: false,
        guest_port: 22,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: true,
        memory: "",
        extra: &[],
        steps: &[
            Step::AwaitAny("dhcp: lease 10.0.2.15/24", BOOT),
            Step::AwaitAny("sshd: listening on port 22", 90_000),
            Step::AwaitAny("freeos> ", 90_000),
            Step::Wait(1_000),

            // 1. Четыре мебибайта в гостя. Такой файл не проходит ни одним
            // окном — ни канала SSH (128 КиБ), ни канала к программе (4 КиБ),
            // ни TCP, — и все три обязаны открываться по ходу, а не один раз.
            Step::Sftp(SftpRun {
                transfer: Transfer::Batch("put payload.bin sftp-payload.bin\nls -l sftp-payload.bin"),
                stage: &[("payload.bin", Source::Random(4 * 1024 * 1024))],
                fresh: &["returned.bin"],
                compare: None,
                measure: Some("payload.bin"),
                expect: &["sftp-payload.bin", "4194304"],
                absent: &[],
                timeout_ms: 600_000,
            }),
            Step::AwaitAny("sshd: sftp for roman", 30_000),
            // Главное утверждение фазы в одной строке: сервер файлов — не root.
            Step::AwaitAny("sftp-server: session for uid 1000 gid 1000 in /home/roman", 30_000),
            Step::AwaitAny(
                "sftp-server: closed /home/roman/sftp-payload.bin after 4194304 bytes written",
                60_000,
            ),

            // 2. И обратно — со сверкой каждого байта на хосте.
            Step::Sftp(SftpRun {
                transfer: Transfer::Batch("get sftp-payload.bin returned.bin"),
                stage: &[],
                fresh: &["returned.bin"],
                compare: Some(("payload.bin", "returned.bin")),
                measure: Some("returned.bin"),
                expect: &[],
                absent: &[],
                timeout_ms: 600_000,
            }),

            // 3. `scp` с OpenSSH 9 ходит тем же SFTP — туда и обратно.
            Step::Sftp(SftpRun {
                transfer: Transfer::ScpPut("note.bin", "/home/roman/scp-note.bin"),
                stage: &[("note.bin", Source::Random(100_000))],
                fresh: &["note-back.bin"],
                compare: None,
                measure: None,
                expect: &[],
                absent: &[],
                timeout_ms: 120_000,
            }),
            Step::Sftp(SftpRun {
                transfer: Transfer::ScpGet("/home/roman/scp-note.bin", "note-back.bin"),
                stage: &[],
                fresh: &[],
                compare: Some(("note.bin", "note-back.bin")),
                measure: None,
                expect: &[],
                absent: &[],
                timeout_ms: 120_000,
            }),

            // 4. Каталоги: создать, переименовать внутри, удалить. Первые три
            // строки с `-` убирают остатки прерванного прогона: диск общий у
            // всей цепочки установленной системы.
            Step::Sftp(SftpRun {
                transfer: Transfer::Batch(
                    "-rm sftp-dir/moved.bin\n-rm sftp-dir/note.bin\n-rmdir sftp-dir\n\
                     mkdir sftp-dir\nput note.bin sftp-dir/note.bin\n\
                     rename sftp-dir/note.bin sftp-dir/moved.bin\nls sftp-dir\n\
                     rm sftp-dir/moved.bin\nrmdir sftp-dir\n\
                     rm sftp-payload.bin\nrm scp-note.bin",
                ),
                stage: &[],
                fresh: &[],
                compare: None,
                measure: None,
                expect: &["moved.bin"],
                absent: &[],
                timeout_ms: 120_000,
            }),
            Step::AwaitAny("sftp-server: made directory /home/roman/sftp-dir", 30_000),
            Step::AwaitAny(
                "sftp-server: renamed /home/roman/sftp-dir/note.bin to /home/roman/sftp-dir/moved.bin",
                30_000,
            ),
            Step::AwaitAny("sftp-server: removed directory /home/roman/sftp-dir", 30_000),

            // 5. Отказ — от ядра, а не от сервера: `/etc` принадлежит root, в
            // `/root` не пускает каталог `0700`. Каждая команда с `-`, чтобы
            // пакет дошёл до конца и показал все три отказа.
            Step::Sftp(SftpRun {
                transfer: Transfer::Batch(
                    "-put note.bin /etc/sftp-denied.bin\n-get /root/notes.txt denied.txt\n\
                     -mkdir /etc/sftp-denied",
                ),
                stage: &[],
                fresh: &["denied.txt"],
                compare: None,
                measure: None,
                expect: &["Permission denied"],
                absent: &["world-readable"],
                timeout_ms: 120_000,
            }),
            Step::AwaitAny(
                "sftp-server: open for writing /etc/sftp-denied.bin refused: permission denied",
                30_000,
            ),
            Step::AwaitAny(
                "sftp-server: mkdir /etc/sftp-denied refused: permission denied",
                30_000,
            ),
            // И файла там действительно нет: отказ, а не «записали и сказали
            // отказ».
            Step::Ssh(SshRun {
                identity: Identity::Authorized,
                command: "ls /etc",
                stdin: "",
                expect: &["passwd"],
                absent: &["sftp-denied"],
                timeout_ms: 60_000,
            }),

            // 6. Ради чего фаза: программа WinForms, привезённая штатным
            // клиентом, запускается из «Файлов». Сборка и её
            // `.runtimeconfig.json` — те же, что лежат в образе, но едут
            // по сети в домашний каталог.
            Step::Sftp(SftpRun {
                transfer: Transfer::Batch(
                    "-mkdir apps\nput winforms.dll apps/winforms.dll\n\
                     put winforms.runtimeconfig.json apps/winforms.runtimeconfig.json",
                ),
                stage: &[
                    ("winforms.dll", Source::Repo("initrd/usr/share/dotnet/samples/winforms.dll")),
                    (
                        "winforms.runtimeconfig.json",
                        Source::Repo("initrd/usr/share/dotnet/samples/winforms.runtimeconfig.json"),
                    ),
                ],
                fresh: &[],
                compare: None,
                measure: None,
                expect: &[],
                absent: &[],
                timeout_ms: 120_000,
            }),
            Step::AwaitAny("sftp-server: closed /home/roman/apps/winforms.runtimeconfig.json after", 30_000),
            Step::Line("run -b /bin/files /home/roman/apps"),
            Step::Await("window      : 'Files' at", 30_000),
            Step::Await("files: /home/roman/apps has ", 30_000),
            Step::Key("end"),
            Step::Await("selected 'winforms.runtimeconfig.json'", 15_000),
            Step::Key("up"),
            Step::Await("selected 'winforms.dll'", 15_000),
            Step::Key("ret"),
            Step::Await("files: started '/bin/dotnet /home/roman/apps/winforms.dll' as #", 15_000),
            Step::Await("dotnet: window 'Form1' opened, 800x450", 60_000),
            Step::Wait(2_500),
            Step::Shot("sftp-winforms"),
            Step::Aim(Aim::Close("Form1")),
            Step::Click,
            Step::Await("dotnet: winforms.dll: Main returned 0", 30_000),
            Step::Aim(Aim::Close("Files")),
            Step::Click,
            Step::Await("files: closing on request", 15_000),

            // Уборка: цепочка установленной системы делит диск.
            Step::Sftp(SftpRun {
                transfer: Transfer::Batch(
                    "rm apps/winforms.dll\nrm apps/winforms.runtimeconfig.json\nrmdir apps",
                ),
                stage: &[],
                fresh: &[],
                compare: None,
                measure: None,
                expect: &[],
                absent: &[],
                timeout_ms: 120_000,
            }),
            Step::AwaitAny("sftp-server: removed directory /home/roman/apps", 30_000),

            Step::Absent("sshd: the client overran the sftp window"),
            Step::Absent("dotnet: error"),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "rollback",
        about: "Заведомо неисправная система в слоте: попытки кончаются, машина возвращается назад.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        // Четыре загрузки в одном процессе QEMU: три неудачные попытки и
        // возврат.
        reboots: true,
        updates: true,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Файл, который обязан пережить всё происходящее. Он на разделе
            // состояния, а слоты меняют только систему — но утверждать это надо
            // проверкой, а не рассуждением.
            // Пауза, а не ожидание приглашения: приглашение к этому моменту уже
            // напечатано, и ждать второго — значит ждать вечно. Пауза же
            // отвечает на настоящий вопрос: гость перерисовывает окно с
            // запрещёнными прерываниями и не разбирает линию, а приёмный FIFO
            // у PL011 — 32 байта, ровно на них строка и обрывалась.
            Step::Wait(2_000),
            Step::Line("echo keep-me > /home/roman/k.txt"),
            Step::Await("wrote 8 bytes", 15_000),

            // Обновление, у которого испорчен корень. Контейнер при этом цел:
            // суммы сходятся, `sysupdate` его принимает и записывает. Ровно та
            // неисправность, ради которой существует откат, — отвергни мы его
            // на входе, откатывать было бы нечего.
            Step::Line("sysupdate apply /media/freeos-broken.fpk"),
            // Сроки — как у `update` и `update-net`, и по той же причине: за
            // этими двумя строками стоит запись двадцати четырёх мегабайт
            // корня, ядра и сорока мегабайт initrd в слот, а рядом на машине
            // работают ещё два гостя. Семи минут не хватило на исправной
            // записи; вопрос, на который отвечает срок, — «не повисло ли».
            Step::Await("sysupdate   : root image written", 900_000),
            Step::Await("slot B is active from the next boot", 900_000),

            // Попытка первая. Загрузчик тратит попытку, ядро стартует, а корень
            // слота не монтируется — значит подтверждать нечего.
            Step::Line("reboot"),
            Step::Await("[slot] booting slot B", BOOT),
            Step::Await("root        : cannot mount ext2", 60_000),
            Step::Await("freeos> ", 60_000),
            // Дальше шнур выдёргивается дважды: система не подтвердилась, и
            // каждая загрузка тратит попытку.
            Step::Reset,
            Step::Await("[slot] booting slot B", BOOT),
            Step::Await("root        : cannot mount ext2", 60_000),
            Step::Reset,
            Step::Await("[slot] booting slot B", BOOT),
            Step::Await("root        : cannot mount ext2", 60_000),
            Step::Reset,

            // И вот теперь — то, ради чего фаза. Попытки кончились, загрузчик
            // сам вернулся на прежний слот, и система об этом сказала.
            Step::Await("used up its attempts", BOOT),
            Step::Await("[slot] booting slot A after a rollback", 30_000),
            // Порядок шагов повторяет порядок строк, а не порядок событий, как
            // их представляет себе человек: корень монтируется **до** того, как
            // система говорит об откате, потому что подтверждать загрузку
            // раньше, чем нашёлся корень, было бы нечем.
            Step::Await("root        : ext2 at LBA", 180_000),
            Step::Await("slot        : this boot came back to the previous slot", 60_000),
            Step::Await("freeos> ", 60_000),
            // Файлы человека на месте: раздел состояния слоты не трогают.
            Step::Line("cat /home/roman/k.txt"),
            Step::Await("keep-me", 15_000),
            // И слот A снова подтверждён — то есть следующая загрузка обычная.
            Step::Line("slots"),
            Step::Await("booted from slot A", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "update",
        about: "Обновление слотами: новая система в свободном слоте, перезагрузка — и версия другая.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        // Две загрузки в одном процессе: до обновления и после.
        reboots: true,
        updates: true,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // На старте — прежняя версия и прежний слот. «Прежняя» — это
            // версия рабочего дерева, а не буква: обновление в `/media` несёт
            // `UPDATE_VERSION`, заведомо более новую, и проверка состоит в том,
            // что после перезагрузки система называет **другое** число.
            Step::Line("slots"),
            Step::Await("booted from slot A", 15_000),
            Step::Line("cat /os-release"),
            Step::Await("version=0.7", 15_000),
            // Файл человека, который обязан пережить смену системы целиком.
            // Пауза, а не ожидание приглашения: приглашение к этому моменту уже
            // напечатано, и ждать второго — значит ждать вечно. Пауза же
            // отвечает на настоящий вопрос: гость перерисовывает окно с
            // запрещёнными прерываниями и не разбирает линию, а приёмный FIFO
            // у PL011 — 32 байта, ровно на них строка и обрывалась.
            Step::Wait(2_000),
            Step::Line("echo keep-me > /home/roman/k.txt"),
            Step::Await("wrote 8 bytes", 15_000),

            // Сначала — чужая подпись. Проверка, без которой все остальные
            // ничего не значат: система, ставящая что угодно, проходит «годное
            // обновление поставилось» ровно так же успешно, как правильная.
            // Образ при этом целый и отличается от годного **только ключом**.
            Step::Line("sysupdate apply /media/freeos-forged.fpk"),
            Step::Await("does not match any key this system trusts", 60_000),
            // И ни байта не записано: отказ случился до первой записи в раздел.
            Step::Absent("sysupdate   : root image written"),
            Step::Wait(2_000),

            Step::Line("sysupdate apply /media/freeos-0.8.fpk"),
            // Подпись годного сходится, и система говорит об этом словами.
            Step::Await("signature checks out against one of", 60_000),
            // Сроки те же, что у `update-net`, и подняты по той же причине:
            // они отвечают на вопрос «не повисло ли», а не «сколько это должно
            // занимать». Двадцать четыре мегабайта корня, ядро и сорок
            // мегабайт initrd пишутся в слот отладочным ядром под эмуляцией, и
            // на машине, где рядом идут ещё два гостя, семи минут на initrd не
            // хватило: сценарий упал на исправной записи, которая шла себе
            // дальше.
            Step::Await("sysupdate   : root image written", 900_000),
            Step::Await("sysupdate   : kernel written", 600_000),
            Step::Await("sysupdate   : initrd written", 900_000),
            Step::Await("slot B is active from the next boot", 120_000),

            Step::Line("reboot"),
            // Загрузка со второго слота: другое ядро, другой initrd, другой
            // корень — и всё это заказано одним файлом.
            Step::Await("[slot] booting slot B", BOOT),
            Step::Await("root        : slot B on", 60_000),
            Step::Await("root        : ext2 at LBA", 60_000),
            // Подтверждение — то, из-за отсутствия которого система откатилась
            // бы обратно.
            Step::Await("slot        : slot B confirmed", 60_000),
            Step::Await("freeos> ", 60_000),
            // Версия новая: это и есть «система обновилась».
            Step::Line("cat /os-release"),
            Step::Await("version=0.8", 15_000),
            Step::Line("slots"),
            Step::Await("booted from slot B", 15_000),
            // А состояние — прежнее: и учётная запись, и файл, записанный до
            // обновления.
            Step::Line("whoami"),
            Step::Await("roman (uid 1000 gid 1000)", 15_000),
            Step::Line("cat /home/roman/k.txt"),
            Step::Await("keep-me", 15_000),
            Step::Shot("update-slot-b"),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "update-net",
        about: "Обновление берётся с сервера по сети: индекс, подпись, загрузка, слот — и другая версия после перезагрузки.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: true,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        // Сервер обновлений на хосте, репозиторий и `/etc/update.cfg` гостю.
        host_repo: true,
        arches: &[],
        // Две загрузки в одном процессе: до обновления и после.
        reboots: true,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Строка про службы печатается **до** приглашения, и ждать её после
            // него значит ждать вечно: ожидание ищет вперёд от курсора. Порядок
            // шагов повторяет порядок строк, а не порядок событий, как их
            // представляет себе человек.
            //
            // Сама строка — половина фазы про умолчания: список служб взят из
            // образа, потому что установщик его в `/etc` больше не пишет.
            Step::Await("services    : described by /usr/share/defaults/etc/services", BOOT),
            Step::Await("freeos> ", 120_000),
            // Сеть поднимает служба, без единой команды.
            Step::AwaitAny("dhcp: lease 10.0.2.15/24", 120_000),
            Step::Wait(2_000),

            // Сначала — чужая подпись индекса. Отказать обязана **программа**, и
            // до единого скачанного байта: индекс говорит, что качать и сколько,
            // и верить неподписанному значило бы тащить по сети что попало.
            Step::Line("sysupdate check /x/"),
            Step::Await("index signature does not match any key this system trusts", 90_000),
            Step::Wait(2_000),

            // Теперь настоящий репозиторий. Три строки — три утверждения:
            // индекс подписан своим ключом, в нём есть запись для этой машины, и
            // она новее установленного.
            Step::Line("sysupdate check"),
            Step::Await("the index is signed by a key this system trusts", 90_000),
            Step::Await("the server offers FreeOS 0.9", 30_000),
            Step::Await("that is newer", 30_000),
            Step::Wait(2_000),

            // Загрузка. Проверяется тем же, чем проверял бы человек: размер
            // сошёлся с обещанным в подписанном индексе и SHA-256 сошёлся тоже.
            Step::Line("sysupdate get"),
            Step::Await("sysupdate: downloading", 60_000),
            // Срок велик не от неуверенности: образ — семьдесят семь мегабайт,
            // и каждый из них проходит через сокет, SHA-256 и запись в ext2 в
            // отладочном ядре под эмуляцией. Программа печатает ход каждые
            // четыре мегабайта — по журналу видно, что она движется, а не
            // висит.
            Step::Await("downloaded and verified 0.9", 2_400_000),
            // Ждём **терминал**, а не две секунды. Разница стоила трёх полных
            // прогонов подряд, и всякий раз на x86-64: программа напечатала
            // последнюю строку, но оболочка ещё убирает за ней — печатает
            // освобождённые страницы, закрытые сокеты и код возврата, и каждая
            // из этих строк перерисовывает окно 1280×800 в отладочной сборке.
            // Команда, набранная в этот промежуток, не доходит ни до кого:
            // программы уже нет, оболочка ещё не вернулась. Дальше сеанс
            // заканчивался по двадцатисекундному пределу простоя, и выглядело
            // это как «система не приняла sysupdate apply».
            Step::Await("tty         : foreground released", 120_000),

            // И установка — тем самым системным вызовом, ради которого фаза
            // завела `SYS_UPDATE`. Подпись контейнера проверяет ядро, а не
            // качавшая программа: она в третьем кольце, а в слот пишет ядро.
            Step::Line("sysupdate apply"),
            Step::Await("signature checks out against one of", 120_000),
            Step::Await("sysupdate   : root image written", 900_000),
            Step::Await("sysupdate   : kernel written", 600_000),
            Step::Await("sysupdate   : initrd written", 900_000),
            Step::Await("is active from the next boot", 120_000),

            Step::Line("reboot"),
            // Загрузка с другого слота — и версия, приехавшая по сети.
            Step::Await("root        : ext2 at LBA", BOOT),
            Step::Await("freeos> ", 120_000),
            Step::Line("cat /os-release"),
            Step::Await("version=0.9", 15_000),
            // Умолчания приехали вместе с образом.
            Step::Expect("services    : described by /usr/share/defaults/etc/services"),
            // А правка человека пережила смену системы целиком: она на разделе
            // состояния, которого обновление не касается.
            Step::Line("cat /etc/update.cfg"),
            Step::Await("server=10.0.2.2", 15_000),
            Step::Shot("update-net"),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "update-tls",
        about: "Запасной канал: первый сервер молчит, чужому корню отказано, а свой отдаёт индекс по HTTPS.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: true,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        // Тот же комплект, что у `update-net`: репозиторий на хосте, `/etc`
        // гостя с тремя адресами и корнем стенда. Серверов при этом поднимается
        // три — обычный и два по HTTPS, с разными корнями.
        host_repo: true,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            Step::AwaitAny("dhcp: lease 10.0.2.15/24", 120_000),
            Step::Wait(2_000),

            // Самое простое утверждение первым: одна страница по HTTPS,
            // руками. Если сломано рукопожатие или проверка цепочки, дальше
            // читать журнал незачем — отказ назван здесь.
            Step::Line("run /bin/fetch https://10.0.2.2:{tls}/index"),
            // Число байт здесь не пишется: индекс у каждой архитектуры свой и
            // длиной отличается. Проверяется то, что важно, — что программа
            // дошла до отчёта об успехе, а не до отказа.
            Step::Await(" bytes in ", 120_000),
            Step::Wait(1_000),

            // Главная строка сценария. Путь `/nothing/` относится **только** к
            // первому серверу — тому, что задан старой записью `server=` без
            // схемы; у записей, заданных полным адресом, путь входит в адрес.
            // Значит первый обязан ответить «нет такого файла», второй —
            // отказать по сертификату, а третий отдать индекс.
            Step::Line("sysupdate check /nothing/"),
            // Все три адреса названы вслух, и в том порядке, в котором их
            // пробуют: молча выбранный сервер — это отказ, причину которого
            // потом не найти.
            Step::Await("repository http://10.0.2.2:{repo}/nothing/", 60_000),
            Step::Await("repository https://10.0.2.2:{stranger}/", 30_000),
            Step::Await("repository https://10.0.2.2:{tls}/", 30_000),
            Step::Await("https is available; the roots come from ca.pem", 30_000),
            // Первый: файла нет.
            Step::Await("index: the server refused the request (404)", 90_000),
            // Второй: рукопожатие состоялось, а цепочка — нет. Это и есть
            // проверка, ради которой стенд держит второй корень: клиент,
            // принимающий любой сертификат, дошёл бы здесь до индекса.
            Step::Await("the chain leads to nobody this system trusts", 120_000),
            // Третий: TLS 1.3 с сертификатом на **адрес** (iPAddress в SAN),
            // проверенной цепочкой и подписанным индексом.
            Step::Await("the index is signed by a key this system trusts", 120_000),
            Step::Await("the server offers FreeOS 0.9", 30_000),
            Step::Wait(1_000),

            // И объёмная загрузка по тому же каналу: полмегабайта — это сотни
            // записей TLS, то есть счётчик, сменившийся много раз, и границы
            // записей, не совпадающие ни с чем. Одного индекса в две тысячи
            // байт для этого мало: он умещается в одну запись.
            // Та же самая половина мегабайта сначала **без** TLS, с того же
            // хоста и тем же клиентом. Пара строк в журнале отвечает на вопрос,
            // который иначе решается на глаз: сколько стоит шифрование. На
            // 2026-08-15 это 427 КиБ/с против 124 — то есть вчетверо, и почти
            // всё это ChaCha20 в неоптимизированной сборке под эмуляцией.
            Step::Line("run /bin/fetch http://10.0.2.2:{repo}/blob -"),
            Step::Await("fetch: 524288 bytes in", 300_000),
            Step::Line("run /bin/fetch https://10.0.2.2:{tls}/blob -"),
            Step::Await("fetch: 524288 bytes in", 300_000),
            Step::Line("cat /os-release"),
            Step::Await("version=", 15_000),
            Step::Shot("update-tls"),

            // Чужой корень отдельной строкой — чтобы отказ был виден и тогда,
            // когда сценарий смотрят глазами.
            Step::Line("run /bin/fetch https://10.0.2.2:{stranger}/index"),
            Step::Await("fetch: the chain leads to nobody this system trusts", 120_000),

            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "winpath",
        about: "Путь, написанный по-виндовому, ведёт туда же; в менеджере — знакомые подписи.",
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),

            // ── Оболочка ─────────────────────────────────────────────────────
            //
            // Сначала юниксовое написание — оно задаёт то, с чем сравнивать.
            // Строка `timezone=` есть в `system.cfg` у всякой установленной
            // системы, и её написал установщик, а не мы.
            Step::Line("cat /etc/system.cfg"),
            Step::Await("timezone=", 15_000),
            // То же самое по-виндовому, с обратными косыми. Отвечать обязано
            // тем же.
            Step::Line(r"cat C:\etc\system.cfg"),
            Step::Await("timezone=", 15_000),
            // И со прямыми косыми после буквы: так пишут те, кто уже привык к
            // обоим мирам.
            Step::Line("ls C:/etc"),
            Step::Await("system.cfg", 15_000),
            // Одиночная обратная косая — «от корня» в виндовом написании.
            Step::Line(r"ls \bin"),
            Step::Await("files", 15_000),

            // Чужая буква — отказ по имени, а не молчаливое совпадение с C:.
            // Это главное утверждение раздела: `D:\etc` и `C:\etc` в той
            // системе, откуда человек пришёл, — разные места, и открыть по ним
            // один файл значило бы соврать ему о его же данных.
            Step::Line(r"cat D:\etc\system.cfg"),
            Step::Await("there is no drive D: in this system", 15_000),
            Step::Absent("timezone= (from D)"),

            // Обратная косая **в имени файла** остаётся знаком имени, а не
            // разделителем: путь без буквы тома и без ведущей косой не
            // переводится вовсе.
            Step::Line("mkdir /home/roman/w"),
            Step::Await("created /home/roman/w", 15_000),
            Step::Line(r"mkdir C:\home\roman\w\inner"),
            Step::Await("created /home/roman/w/inner", 15_000),
            Step::Line(r"rm C:\home\roman\w\inner"),
            Step::Await("removed /home/roman/w/inner", 15_000),
            Step::Line("rm /home/roman/w"),
            Step::Await("removed /home/roman/w", 15_000),

            // ── Файловый менеджер ────────────────────────────────────────────
            Step::Line("run -b /bin/files"),
            Step::Await("window      : 'Files' at", 30_000),
            Step::Await("files: / has ", 15_000),
            Step::Wait(2_500),
            // В знакомом виде: «Программы» вместо `bin`, «Настройки» вместо
            // `etc`, а служебные деревья свёрнуты.
            Step::Shot("01-friendly"),
            // Переключатель показывает настоящие имена — и вместе с ними всё,
            // что было свёрнуто.
            Step::Type("v"),
            Step::Await("files: view plain", 15_000),
            Step::Wait(2_500),
            Step::Shot("02-plain"),
            // И обратно: переключатель работает в обе стороны, а не «включается
            // один раз».
            Step::Type("v"),
            Step::Await("files: view friendly", 15_000),
            Step::Wait(1_500),
            Step::Shot("03-friendly-again"),

            Step::Key("ctrl-w"),
            Step::Await("files: closing on request", 15_000),
            Step::Await("desktop     : focus 'Terminal'", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "settings",
        about: "Настройки, заданные в окне, переживают перезагрузку: пояс, тема и постоянный адрес.",
        // Установленная система, и по существу: `/etc` живёт на разделе
        // состояния, а у системы с носителя корень — образ initrd в FAT, и
        // писать в него нельзя. Проверять «настройка запомнилась» там, где
        // ничего не запоминается, нечем.
        target: Target::Installed,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: true,
        e1000: false,
        guest_port: 2000,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: true,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // До правки — DHCP: адрес приносит служба, и это видно в журнале.
            // Строка нужна как отправная точка: без неё «адрес 10.0.2.15 после
            // перезагрузки» ничего не доказывал бы — его и так дал бы DHCP.
            Step::AwaitAny("dhcp: lease 10.0.2.15/24", 60_000),
            Step::Wait(2_000),

            // ── Всё дальше — клавиатурой, ни одной команды ────────────────────
            //
            // Мышью нельзя: попадание в строку списка зависит от масштаба
            // шрифта, а он на двух архитектурах разный. То же правило, по
            // которому клавиатурой ходит сценарий `pkg`.
            Step::Key("f1"),
            Step::Await("desktop     : menu opened", 15_000),
            // Меню открывается с выбранным «Терминалом»; «Параметры» — сразу за
            // ним. Шаг именно один, и это проверено красным прогоном: два шага
            // открывают «О системе».
            Step::Key("down"),
            Step::Key("ret"),
            Step::Await("desktop     : opened 'Settings'", 15_000),

            // ── Часовой пояс ─────────────────────────────────────────────────
            //
            // «Дата и время» — второй раздел сверху. Enter переводит разбор
            // клавиш из списка разделов в пункты справа.
            Step::Key("down"),
            Step::Key("ret"),
            Step::Wait(1_500),
            Step::Shot("01-clock"),
            // Список поясов начинается с UTC-08:00; девятый сверху — UTC+05:30.
            // Он выбран нарочно и по двум причинам: пять часов, а не три,
            // поэтому ошибка в знаке даёт -5, а не совпадение с тем, что и так
            // стояло; и тридцать минут, поэтому смещение, посчитанное целыми
            // часами, здесь ошибётся на полчаса, а не сойдётся.
            Step::Repeat("down", 8),
            Step::Key("ret"),
            Step::Await("timezone    : UTC+05:30 set from Settings", 15_000),
            Step::Wait(1_000),
            Step::Shot("02-clock-set"),

            // ── Тема ─────────────────────────────────────────────────────────
            //
            // Возврат в список разделов, вверх до «Экрана», и там светлая тема.
            // Она проверяет другое, чем пояс: тема применяется мгновенно и
            // раньше нигде не сохранялась вовсе.
            // С фазы С6 тема живёт в «Оформлении» — девятом разделе; светлая
            // — второй пункт в нём.
            Step::Key("left"),
            Step::Repeat("down", 7),
            Step::Key("ret"),
            Step::Key("down"),
            Step::Key("ret"),
            Step::Await("settings    : theme light saved", 15_000),
            Step::Wait(3_000),
            Step::Shot("03-light"),

            // ── Постоянный адрес ─────────────────────────────────────────────
            // Из «Оформления» (девятого) к «Сети» (третьему): шесть вверх.
            //
            // Тот же адрес, что дал DHCP, и это осознанно: пользовательская сеть
            // QEMU пробрасывает порт на свой постоянный адрес гостя, и машина,
            // переехавшая на другой, стала бы недостижимой с хоста по причине,
            // к системе отношения не имеющей. Проверяется здесь не «адрес
            // другой», а «адрес пришёл из файла, а не от DHCP», — и последнее
            // доказывается тем, что после перезагрузки DHCP не запускается
            // вовсе.
            Step::Key("left"),
            Step::Repeat("up", 6),
            Step::Key("ret"),
            Step::Wait(1_500),
            Step::Shot("04-network"),
            // Второй пункт — «Постоянный адрес»: он же заполняет поля тем, что
            // у машины сейчас, и ставит ввод в первое из них.
            Step::Key("down"),
            Step::Key("ret"),
            Step::Wait(1_000),
            Step::Shot("05-network-fields"),
            // Поле уже заполнено нынешним адресом — стираем и набираем заново.
            // Стирание отдельным шагом: поле, дописанное к прежнему значению,
            // дало бы «10.0.2.1510.0.2.15/24», и отказ выглядел бы как ошибка
            // разбора адреса.
            Step::Repeat("backspace", 20),
            Step::Type("10.0.2.15/24"),
            Step::Key("ret"),
            // Шлюз — четвёртый пункт раздела, а курсор стоит на втором
            // («Постоянный адрес»): выход из поля возвращает клавиши пунктам, но
            // номер пункта не двигает. Отсюда два шага, а не один.
            Step::Repeat("down", 2),
            Step::Key("ret"),
            Step::Repeat("backspace", 20),
            Step::Type("10.0.2.2"),
            Step::Key("ret"),
            Step::Wait(1_000),
            Step::Shot("06-network-typed"),
            // И «Применить» — последний пункт раздела.
            Step::Key("down"),
            Step::Key("ret"),
            Step::Await("settings    : address 10.0.2.15/24 static, saved", 20_000),
            Step::Wait(1_000),
            Step::Shot("07-network-applied"),

            // ── Диски и пользователи ─────────────────────────────────────────
            //
            // Два раздела, которые ничего не настраивают, но и молчать о них
            // нельзя: нарисованный раздел, который никто ни разу не открыл, —
            // это раздел, про который неизвестно, рисуется ли он вообще.
            // Проверка томов вдобавок делает настоящую работу и печатает её
            // итог в журнал.
            Step::Key("left"),
            Step::Key("down"),
            Step::Key("ret"),
            Step::Key("ret"),
            Step::Await("settings    : fsck ", 60_000),
            Step::Wait(1_500),
            Step::Shot("08-disks"),
            Step::Key("left"),
            Step::Key("down"),
            Step::Wait(1_500),
            Step::Shot("09-users"),

            // Окно закрывается **до** команды оболочке: ввод достаётся активному
            // окну, и строка, приехавшая по серийной линии, ушла бы в него и
            // пропала. Куплено красным прогоном 47c.
            Step::Key("ctrl-w"),
            Step::Await("desktop     : closed 'Settings'", 15_000),
            Step::Wait(2_000),

            // ── Перезагрузка: то, ради чего фаза ─────────────────────────────
            Step::Line("reboot"),
            Step::Await("freeos> ", BOOT),
            // Пояс приехал из файла, а не из умолчания.
            Step::AwaitAny("timezone    : UTC+05:30 from /etc/system.cfg", 60_000),
            // Тема — тоже: до этой фазы она не сохранялась нигде.
            Step::AwaitAny("desktop     : theme light from /etc/desktop.cfg", 60_000),
            // Адрес — из файла, и в строке прямо сказано откуда.
            Step::AwaitAny("network     : 10.0.2.15/24 static from /etc/network.cfg", 60_000),
            // И главное доказательство того, что адрес не от DHCP: служба
            // увидела постоянный адрес и отступилась. Без этой строки всё
            // предыдущее объяснялось бы совпадением.
            Step::AwaitAny("dhcp: a static address is configured, standing down", 60_000),

            // Машина по этому адресу достижима с хоста — обычным клиентом TCP,
            // ничего не знающим ни о нашем стеке, ни о том, откуда взялся адрес.
            Step::Line("run -b /bin/echod"),
            Step::AwaitAny("echod: listening on port 2000", 30_000),
            Step::TcpEcho("reachable at the address from the file", 20_000),

            // Местное время сдвинуто на пять с половиной часов, и это видно
            // самой системе — не тем окном, которое пояс задавало.
            Step::Line("date"),
            Step::Await("UTC+05:30", 15_000),

            // ── Уборка за собой ──────────────────────────────────────────────
            //
            // Обязательна, и это свойство стенда, а не вежливость: сценарии
            // установленной системы идут **одной цепочкой** и передают диск друг
            // другу. Оставленный `mode=static` увёл бы у следующего сценария
            // DHCP, оставленная светлая тема — все его снимки, а пояс +05:30 —
            // всё, что сверяет время. Проверка, ломающая соседей, доказывает
            // ровно столько же, сколько сломанные соседи.
            //
            // Убирает **окно**, а не `rm` в оболочке, и это не выбор из удобства:
            // первый прогон этой уборки ответил `rm /etc/network.cfg: permission
            // denied`. Сеанс идёт от `roman`, а `/etc` принадлежит root — то
            // есть настройки правит только тот, кто вправе, и правит их окно, за
            // которым стоит ядро. Заодно уборка проходит по обратному пути,
            // который иначе остался бы непроверенным: «вернуть DHCP».
            Step::Key("f1"),
            Step::Await("desktop     : menu opened", 15_000),
            Step::Key("down"),
            Step::Key("ret"),
            Step::Await("desktop     : opened 'Settings'", 15_000),

            // «Экран» — раздел, на котором окно открывается; тёмная тема шестым
            // пунктом, сразу за пятью разрешениями.
            Step::Repeat("down", 8),
            Step::Key("ret"),
            Step::Key("ret"),
            Step::Await("settings    : theme dark saved", 15_000),
            // Персонализация (фаза С6): за двумя темами — пять акцентов, четыре
            // обоев и три высоты заголовка. Второй акцент — фиолетовый, вторые
            // обои — «Океан», третья высота — крупный заголовок; всё это
            // применяется сразу и записывается в desktop.cfg.
            Step::Repeat("down", 3),
            Step::Key("ret"),
            Step::Await("settings    : accent violet saved", 15_000),
            Step::Await("desktop     : theme dark, accent violet, wallpaper night", 15_000),
            Step::Repeat("down", 5),
            Step::Key("ret"),
            Step::Await("settings    : wallpaper ocean saved", 15_000),
            Step::Repeat("down", 5),
            Step::Key("ret"),
            Step::Await("settings    : title bar 52 saved", 15_000),
            Step::Await("desktop     : title bar 52 px", 15_000),
            Step::Wait(1_500),
            Step::Shot("06-personalized"),
            // Заголовок — обратно к обычному: на его высоту рассчитаны прицелы
            // стенда в остальных сценариях цепочки; акцент и обои остаются —
            // их переживание перезагрузки видно на снимках следующих сценариев.
            Step::Key("up"),
            Step::Key("ret"),
            Step::Await("settings    : title bar 44 saved", 15_000),

            // «Дата и время»: седьмой пояс сверху — UTC+03:00, тот, что записал
            // установщик.
            Step::Key("left"),
            Step::Repeat("up", 7),
            Step::Key("ret"),
            Step::Repeat("down", 6),
            Step::Key("ret"),
            Step::Await("timezone    : UTC+03:00 set from Settings", 15_000),

            // «Сеть»: первый пункт — обратно к DHCP. Он же единственная проверка
            // того, что постоянный адрес можно отменить.
            Step::Key("left"),
            Step::Key("down"),
            Step::Key("ret"),
            Step::Key("ret"),
            Step::Await("settings    : address back to DHCP, /etc/network.cfg removed", 15_000),

            // «Панель управления» — последний, девятый раздел: плитки всех
            // остальных, и Enter на первой открывает «Экран и оформление».
            Step::Key("left"),
            Step::Repeat("down", 7),
            Step::Wait(3_000),
            Step::Shot("07-overview"),
            Step::Key("ret"),
            Step::Key("ret"),
            Step::Await("settings    : opened Экран from the overview", 15_000),

            Step::Key("ctrl-w"),
            Step::Await("desktop     : closed 'Settings'", 15_000),
            Step::Wait(2_000),

            Step::Line("shutdown"),
            Step::Exits(30_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    // `winpath` и `settings` обязаны стоять ДО `install4k`.
    //
    // Оба грузятся с установленного диска на 512-байтовой шине, а `install4k`
    // пересоздаёт тот же файл с сектором 4096 и оставляет его таким. Стоя
    // после него, они получали машину, которой прошивке нечего загрузить:
    // журнал обрывался на `EFI Internal Shell`, а сценарий шесть минут ждал
    // приглашения. Оба были красными весь полный прогон и на обеих
    // архитектурах — и никто этого не видел, потому что полных прогонов не
    // делали с фазы 39a.
    Scenario {
        name: "install4k",
        about: "Установщик размечает диск с сектором 4096 — раскладка считается в его секторах.",
        target: Target::Installer,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Nvme4k,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("FreeOS installer", BOOT),
            Step::Await("[disk]", 30_000),
            Step::Wait(1_500),
            Step::Key("ret"),
            Step::Wait(700),
            Step::Key("ret"),
            Step::Wait(700),
            Step::Shot("01-disk-4k"),
            Step::Key("ret"),
            Step::Wait(700),
            Step::Type("roman"),
            Step::Key("tab"),
            Step::Type("freeos"),
            Step::Key("tab"),
            Step::Type("freeos"),
            Step::Key("ret"),
            Step::Wait(700),
            Step::Key("ret"),
            Step::Wait(700),
            Step::Repeat("down", 3),
            Step::Key("ret"),
            Step::Wait(700),
            Step::Shot("02-confirm-4k"),
            Step::Key("down"),
            Step::Key("ret"),
            // Установка на 4Kn-диск проходит целиком: разметка, ESP с
            // BytsPerSec = 4096 и корень ext2, у которого блок равен сектору.
            //
            // Срок вдвое больше прежнего, и поднят он не наугад: в прогоне
            // вчетвером эта установка на ARM заняла больше четырёх минут и
            // упёрлась в 240 с — а следом упал `sector4k`, которому нужен
            // записанный ею диск. Срок здесь отвечает на вопрос «не повисло
            // ли», и на машине, где рядом работают ещё три эмулятора, ответ
            // «четыре минуты» — не «повисло».
            Step::Await("[install] finished", 480_000),
            Step::Expect("[install] state: /etc/passwd"),
            Step::Wait(1_000),
            Step::Shot("03-done-4k"),
        ],
    },
    Scenario {
        name: "sector4k",
        about: "Корень на диске с сектором 4096: тот же путь GPT → ext2 → VFS, другие адреса.",
        target: Target::LiveAndDisk,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Nvme4k,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Диск объявляет свой блок сам, и ядро его принимает, а не
            // отвергает: до Phase 26c здесь стояло «block size 4096 is not
            // supported», и корень не находился вовсе.
            Step::Await("nvme        : ", BOOT),
            Step::Await("disk        : nvme #0", 30_000),
            // Строка, ради которой заведён сценарий: таблица разделов прочитана
            // на носителе, у которого сектор вчетверо больше обычного.
            Step::Await("4096-byte sectors", 30_000),
            Step::Await("on nvme #0 at LBA", 30_000),
            Step::Await("root        : ext2 at LBA", 30_000),
            // Файл, записанный установщиком, читается: значит и суперблок, и
            // таблицы inode найдены по верным адресам, а не по формуле,
            // делённой на 512.
            Step::Await("account     : /etc/passwd", 30_000),
            Step::Await("freeos> ", 30_000),
            Step::Line("cat /etc/system.cfg"),
            Step::Await("language=", 15_000),
            Step::Line("echo written-on-4kn > /home/roman/sector4k.txt"),
            Step::Await("wrote 15 bytes", 15_000),
            Step::Line("cat /home/roman/sector4k.txt"),
            Step::Await("written-on-4kn", 15_000),
            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "net",
        about: "Карта поднимается, ARP находит шлюз, ping доходит до чужого стека и возвращается.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: true,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Карта найдена и назвала свой аппаратный адрес. Адрес приехал из
            // конфигурации устройства, то есть возможность `VIRTIO_NET_F_MAC`
            // действительно согласована, а не пропущена.
            Step::Await("network     : virtio-net, hardware address ", BOOT),
            Step::Await("network     : receive task ", 30_000),
            Step::Await("freeos> ", 60_000),

            // Адреса не выдуманы: это раскладка пользовательской сети QEMU.
            // Гость — .15, шлюз — .2, и оба заданы не нами.
            Step::Line("ip 10.0.2.15/24 10.0.2.2"),
            Step::Await("address 10.0.2.15/24 set", 15_000),

            // Главное утверждение фазы. Чтобы эта строка появилась, обязаны
            // сработать все четыре слоя подряд: кадр Ethernet уехал через
            // очередь virtio, ARP нашёл аппаратный адрес шлюза, заголовок IPv4
            // сошёлся по контрольной сумме, эхо сошлось по своей — **другой** —
            // сумме, и ответ вернулся через приёмную очередь в задачу.
            //
            // Отвечает при этом чужая реализация: стек SLIRP внутри QEMU. Любую
            // нашу ошибку в суммах или в порядке байт он молча отбрасывает, а не
            // прощает, — ради этого сеть и проверяется им, а не собой.
            Step::Line("ping 10.0.2.2 2"),
            Step::Await("reply from 10.0.2.2, seq 1", 30_000),
            Step::Await("reply from 10.0.2.2, seq 2", 30_000),
            Step::Await("2 of 2 answered", 15_000),

            // Шлюз попал в таблицу ARP — то есть ответ на запрос разобран, а не
            // «как-то само доехало».
            Step::Line("arp"),
            Step::Await("10.0.2.2", 15_000),

            // Счётчики сходятся с тем, что произошло: два запроса ушли, два
            // ответа пришли. Ноль в любом из них означал бы систему, которая
            // печатает правильные слова, ничего не отправляя.
            //
            // Порядок проверок повторяет порядок строк вывода: ожидание ищет
            // вперёд от того места, где остановилось прошлое, и строка адреса,
            // напечатанная выше строки `icmp`, для него уже позади.
            Step::Line("ip"),
            Step::Await("address  10.0.2.15/24", 15_000),
            // И обратная сторона: ни один кадр не потерян на приёме. Приёмная
            // очередь, из которой забыли вернуть буфер, кончается молча — и
            // видно это только здесь.
            Step::Await("0 dropped in", 15_000),
            Step::AtLeast("  icmp     ", 2, 15_000),

            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "ssh-kex",
        about: "Настоящий ssh с хоста доходит до обмена ключами и шифрования и говорит об этом.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: true,
        e1000: false,
        guest_port: 22,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::AwaitAny("dhcp: lease 10.0.2.15/24", BOOT),
            // Ключ хоста делается при первом запуске, и качество источника
            // случайности названо в журнале ядра, а не подразумевается.
            Step::AwaitAny("sshd: listening on port 22", 60_000),
            Step::Wait(1_000),

            // И главное: с хоста приходит **настоящий** OpenSSH. Он придирчив к
            // каждому полю формата, и всё, в чём мы ошиблись, назовёт вслух.
            // Проверяются три утверждения: договорились об обмене ключами,
            // приняли ключ хоста ed25519, договорились о шифре в обе стороны.
            Step::Ssh(SshRun {
                // Без ключа: этот сценарий проверяет транспорт, а не вход.
                identity: Identity::None,
                command: "",
                stdin: "",
                expect: &[
                    "kex: algorithm: curve25519-sha256",
                    "kex: host key algorithm: ssh-ed25519",
                    "chacha20-poly1305@openssh.com",
                    // Аутентификация обязана быть отвергнута, и здесь это
                    // успех сценария: отказ означает, что зашифрованный канал
                    // работает и по нему прошёл разбор сообщений.
                    //
                    // На живой системе она отвергается **всегда**, кем бы ни
                    // был клиент: учётных записей на образе initrd нет, а
                    // класть их туда нельзя — этот образ уезжает в выпуск.
                    "Authentications that can continue: publickey",
                ],
                absent: &[],
                timeout_ms: 60_000,
            }),
            // И гость называет причину своими словами.
            Step::AwaitAny("sshd: no account file here, so nobody can log in", 30_000),

            // Гость видел то же самое со своей стороны.
            Step::AwaitAny("sshd: encrypted, curve25519-sha256 with chacha20-poly1305", 30_000),
            Step::AwaitAny("sshd: service accepted", 15_000),

            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "tcp",
        about: "Эхо-сервер в системе: чужой клиент с хоста шлёт байты и получает их обратно.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: true,
        e1000: false,
        guest_port: 2000,
        host_echo: true,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Адрес берётся у DHCP: сервер, которому его задали руками, проверял
            // бы меньше.
            Step::AwaitAny("dhcp: lease 10.0.2.15/24", BOOT),
            Step::Wait(2_000),

            Step::Line("run -b /bin/echod"),
            Step::AwaitAny("echod: listening on port 2000", 30_000),

            // Первое соединение. Всё, что должно сработать, срабатывает разом:
            // рукопожатие из трёх сегментов, подтверждения, закрытие с обеих
            // сторон. Клиент — обычный `TcpStream` с хоста, то есть чужая
            // реализация протокола.
            Step::TcpEcho("hello from the host", 20_000),
            Step::AwaitAny("echod: client accepted", 15_000),

            // Второе соединение подряд — проверка того, что первое закрылось
            // по-настоящему: слот вернулся, порт остался слушающим.
            Step::TcpEcho("and again", 20_000),

            // А теперь поток, который не помещается в один сегмент и в одно
            // окно: восемь килобайт туда и обратно, с узором, по которому видно
            // перестановку байт. Здесь работают и продолжение потока, и
            // подтверждения по ходу, и приёмный буфер на той стороне.
            Step::TcpBulk(8, 60_000),

            // Обратное направление: теперь `SYN` посылаем мы. Это другая
            // половина автомата (`SYN_SENT` и ожидание `SYN+ACK`), и без неё
            // она осталась бы написанной, но непроверенной. Отвечает эхо-сервер
            // **на хосте**: гость видит его как `10.0.2.2` — так устроена
            // пользовательская сеть QEMU.
            Step::Line("run /bin/echoc 10.0.2.2 {echo} outbound-hello"),
            Step::Await("echoc: got back 14 bytes: outbound-hello", 30_000),

            // Соединений после обмена не осталось — ни одного повисшего.
            Step::Line("tcp"),
            Step::Await("listen", 15_000),

            // И счётчики: сегменты ходили, повторных передач не потребовалось.
            // Ненулевой повтор в эмуляторе означал бы, что мы теряем сегменты
            // сами — сеть здесь идеальная.
            Step::Line("ip"),
            Step::AtLeast("  tcp      ", 20, 15_000),
            // Ожидание, а не мгновенная проверка: строка длинная, и к моменту,
            // когда стенд прочитал из неё число, хвост ещё дописывался. `Expect`
            // смотрит на то, что уже пришло, и падал здесь через раз — на
            // «0 retransmitte».
            Step::Await("0 retransmitted", 15_000),

            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "dns",
        about: "Имя превращается в адрес: запрос уходит серверу, которого назвал DHCP.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: true,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Адрес и сервер имён приезжают от DHCP — руками здесь не задаётся
            // ничего, и это часть проверки: `resolve` обязан работать сразу
            // после загрузки, а не после настройки.
            Step::AwaitAny("dhcp: lease 10.0.2.15/24 gw 10.0.2.2 dns 10.0.2.3", BOOT),
            Step::Wait(2_000),

            // Адрес, записанный адресом, спрашивать у сервера незачем — и это
            // проверяется первым, потому что путь короче и ломается заметнее.
            Step::Line("resolve 10.0.2.2"),
            Step::Await("10.0.2.2 is 10.0.2.2", 15_000),

            // А теперь настоящее имя. Отвечает на него резолвер **хоста**: DNS
            // внутри SLIRP пересылает вопрос туда, то есть разобрать ответ
            // придётся такой, какой пришлёт настоящий сервер, — со сжатием имён
            // и, возможно, цепочкой CNAME перед адресом.
            //
            // Сценарий поэтому требует выхода в интернет с машины разработчика.
            // Это единственная такая проверка в стенде, и она названа здесь, а
            // не выясняется по загадочному отказу.
            Step::Line("resolve example.com"),
            Step::Await("example.com is ", 20_000),

            // Имени, которого нет, ответ тоже приходит — и это ответ, а не
            // молчание: путь «сервер сказал нет» обязан отличаться от «сервер не
            // ответил», иначе первая же опечатка выглядит как поломка сети.
            Step::Line("resolve nothing.invalid"),
            Step::Await("no such name", 20_000),

            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "dhcp",
        about: "Адрес система получает сама, службой под супервизором; убитая служба берёт его заново.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: true,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Служба поднимается супервизором при загрузке, без единой команды.
            Step::Await("network     : virtio-net", BOOT),
            Step::Capture("init: started 'dhcp' as #", 90_000),

            // Четыре шага RFC 2131, и оба ответа — от чужой реализации: сервер
            // DHCP живёт внутри QEMU. Адрес `10.0.2.15` не выдуман нами и не
            // задан командой: его выдали.
            Step::AwaitAny("dhcp: offer 10.0.2.15 from 10.0.2.2", 60_000),
            Step::AwaitAny("dhcp: lease 10.0.2.15/24 gw 10.0.2.2 dns 10.0.2.3", 30_000),

            // Пауза, а не ожидание приглашения. Приглашение печатает оболочка, а
            // строки выше — служба, и порядок между двумя задачами не определён:
            // здесь приглашение вышло **раньше** аренды и оказалось позади
            // курсора, а второго не будет. Пауза же отвечает на настоящий
            // вопрос — успел ли гость разобрать линию.
            Step::Wait(2_000),
            // Ядро приняло настройки от службы: адрес виден оболочке, которая
            // его не задавала.
            Step::Line("ip"),
            Step::Await("address  10.0.2.15/24", 15_000),
            Step::Await("dns      10.0.2.3", 15_000),

            // И связь по этому адресу работает: шлюз отвечает на эхо.
            Step::Line("ping 10.0.2.2 1"),
            Step::Await("reply from 10.0.2.2, seq 1", 30_000),

            // Вторая половина фазы 33, проверенная на настоящей службе: убить
            // её и увидеть, что она вернулась и снова взяла аренду. Номер задачи
            // взят из журнала, а не написан числом.
            Step::Line("kill {}"),
            Step::Await("init: 'dhcp' ended with code", 30_000),
            Step::Await("init: restarting 'dhcp'", 15_000),
            Step::Await("init: started 'dhcp' as #", 30_000),
            Step::Await("dhcp: lease 10.0.2.15/24", 60_000),

            // Вторая аренда доказывает главное про сокеты: порт 68 освободился.
            // Служба закрывает его сама, а убитая — не может, и тогда его
            // закрывает ядро; без этого `bind` во второй жизни службы упёрся бы
            // в порт, занятый задачей, которой уже нет. Строка ниже — обратная
            // сторона того же: после аренды не осталось ни одного открытого
            // сокета, то есть за каждую аренду мы не теряем по одному.
            //
            // Порядок проверок повторяет порядок строк вывода: ожидание ищет
            // вперёд, и строка, напечатанная выше, для него уже позади.
            Step::Line("ip"),
            // Датаграммы действительно ходили — это счётчик стека, а не слова
            // службы о себе.
            Step::AtLeast("  udp      ", 2, 15_000),
            Step::Await("sockets  0 open", 15_000),
            Step::Absent("port 68 is taken"),

            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "abi",
        about: "Вызовы, которых требует libc: dup, fstat, isatty, часы, nanosleep, poll, times.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            Step::Line("run /bin/posix"),

            // Каждое утверждение ждётся отдельно и по порядку печати: программа
            // печатает их одной строкой каждое, и строка, которой нет, — это
            // проверка, до которой она не дошла.
            Step::Await("posix: abi version 65536", 60_000),

            // Позиция у копий дескриптора общая. Число здесь важнее слова:
            // `dup` с независимой позицией сказал бы «четыре».
            Step::Await("posix: dup shares the position, both say 8", 30_000),
            Step::Await("posix: fstat matches stat, size ", 30_000),
            Step::Await("posix: isatty stdout 1, file 0", 30_000),
            Step::Await("posix: monotonic advanced ", 30_000),

            // Сон длится не меньше запрошенного — проверено часами внутри
            // программы, а сюда приезжает уже готовый ответ.
            Step::Await("posix: nanosleep asked 30 ms, slept ", 30_000),
            Step::Await("posix: times cpu ", 30_000),

            // Главное утверждение фазы: две программы, соединённые каналом.
            // `poll` просыпается на чужом выводе, а не по сроку, и видит конец
            // трубы отдельно от её содержимого.
            Step::Await("posix: poll saw the pipe readable, 1 ready", 60_000),

            // Чтение канала через дескриптор не ждёт: пустой канал с живым
            // писателем отвечает `ERR_AGAIN`, и ждать обязан сам читатель —
            // этим вызовом. Строка печатается всегда, а вот **число** ожиданий
            // проверять нельзя: успел ли `hello` написать всё разом, решает
            // планировщик, и ноль здесь так же законен, как тройка.
            Step::Await("posix: poll waited on the pipe ", 30_000),

            Step::Await("posix: poll saw the hangup after ", 30_000),
            Step::Await("posix: all checks passed", 30_000),

            // Ни одна проверка не провалилась. Отдельным шагом, потому что
            // «строка не напечаталась» и «проверка не прошла» — разные беды, и
            // первую поймали бы ожидания выше, а вторую — только это.
            Step::Absent("posix: FAILED"),

            // Время на процессоре теперь печатает и `tasks`: без него счётчик
            // переключений не отличает задачу, которая считает, от той, которую
            // просто часто будят.
            Step::Line("tasks"),
            Step::Await(" ms on cpu", 15_000),

            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "window",
        about: "Окно у программы: своя поверхность, свои события, снятие за умершей.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),

            // Запуск уходит в **серийную линию**: оболочка читает её независимо
            // от того, какое окно на столе в фокусе. Это важно для шагов ниже —
            // как только окно программы откроется, клавиатура станет её.
            // Запуск **один** на весь сценарий, и команда короткая — оба раза
            // вынужденно. Вторая команда, посланная в серийную линию, пока
            // система много печатает, до оболочки не доходит, и чем она длиннее,
            // тем вернее теряется: приёмник переполняется (открытый дефект
            // UART). Поэтому один запуск проходит весь путь целиком.
            //
            // Чего этот сценарий из-за этого **не** проверяет: как программа
            // закрывает окно сама. Это делает `sysmon` в сценариях с мышью —
            // там крестик просит, а программа соглашается.
            Step::Line("run /bin/winshow"),

            // Окно завёл композитор, и говорит об этом он, а не программа.
            // Геометрия в строке — не украшение: по ней стенд наводит мышь, и
            // без неё «окно есть» подтверждалось бы только снимком экрана.
            Step::Await("desktop     : opened 'winshow' for ", 60_000),
            Step::Await("window      : 'winshow' at ", 30_000),

            // Поверхность — настоящая память программы. Узор зависит от обеих
            // координат: отображение, где две страницы ведут на один кадр,
            // залитую константой область прошло бы незамеченным.
            Step::Await("winshow: wrote and read back 64000 points, no mismatches", 30_000),
            Step::Await("winshow: committed the whole window", 30_000),

            // Названный предел договора, проверенный, а не только записанный:
            // вернуть кадры поверхности по просьбе программы нельзя.
            Step::Await("winshow: munmap refused the surface, as promised", 30_000),

            // Событие доходит. Клавиша идёт через **клавиатуру**, а не линию, и
            // достаётся фокусному окну — то есть окну программы, а не оболочке.
            // `97` — это `a`: договор присылает символ, а не код клавиши.
            Step::Type("a"),
            Step::Await("winshow: key 97", 30_000),

            // Клавиша **без символа** — то, чего до фазы 47c не приходило
            // вовсе. Стрелка вниз приезжает именем из договора, и программа
            // печатает его словом: число `16777220` не сказало бы ничего ни
            // человеку, ни этой строке, а сверять стенд с числом значило бы
            // вписать в него границу диапазона второй раз.
            //
            // Две стрелки, а не одна: одна доказывала бы только, что «что-то
            // пришло». Разные имена доказывают, что они **различаются**, — а
            // именно этого не умел прежний договор, где им всем полагался ноль.
            Step::Key("down"),
            Step::Await("winshow: key named down", 30_000),
            Step::Key("left"),
            Step::Await("winshow: key named left", 30_000),

            // И программа их различает: `q` заканчивает ожидание. Без второй
            // клавиши строка выше доказывала бы только, что события считают.
            Step::Type("q"),
            Step::Await("winshow: key 113", 30_000),

            // Программа уходит, не закрыв окно. Снять его и вернуть кадры
            // обязано ядро — и обязано успеть **до** разбора её адресного
            // пространства.
            Step::Await("winshow: leaving the window to the teardown", 60_000),

            // Строка ядра, а не программы: её к этому моменту уже нет. Число
            // кадров здесь — единственное доказательство, что поверхность
            // вернулась целиком, а не осталась висеть за умершей задачей.
            Step::Await("user        : reclaimed ", 30_000),

            // Два способа сделать это неправильно, каждый со своей строкой.
            // Первый — сдаться перед занятым столом и оставить кадры навсегда;
            // второй — недосчитаться страниц при снятии отображения.
            Step::Absent("WARNING: the desktop stayed busy"),
            Step::Absent("WARNING: the window surface gave back"),

            // Ни одна проверка программы не провалилась. Отдельным шагом,
            // потому что «строка не напечаталась» и «проверка не прошла» —
            // разные беды, и первую поймали бы ожидания выше, а вторую только
            // это.
            Step::Absent("winshow: FAILED"),

            Step::Absent("sysmon: FAILED"),

            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "btrfs-read",
        about: "Том btrfs, созданный mkfs.btrfs, монтируется в /data и читается.",
        target: Target::LiveAndBtrfs,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Диск есть, корня FreeOS на нём нет — и это часть проверки:
            // раздел данных обязан находиться сам по себе, а не как довесок к
            // установленной системе.
            Step::Await("root        : no FreeOS root partition", BOOT),
            // Дальше — то, что ядро вычитало из ЧУЖОГО тома. Метка и поколение
            // взяты из суперблока, число кусков — из дерева кусков, то есть
            // одной этой строкой доказан весь путь до B-деревьев.
            Step::Await("data        : btrfs at LBA", 60_000),
            Step::Await("128 MiB,", 10_000),
            Step::Await("label 'FREEOS-FIXTURE'", 10_000),
            Step::Await("4096 B sectors, 16384 B nodes", 10_000),
            Step::Await("data        : mounted read-write", 10_000),
            Step::Await("/data comes from the data partition", 10_000),
            Step::Await("freeos> ", 60_000),

            // Каталог: имена приходят из дерева, а размеры и права — из inode
            // каждого файла, то есть на каждое имя идёт отдельный спуск.
            // Имена ждутся по одному и в том порядке, в каком их клали:
            // перечисление идёт по `DIR_INDEX`, то есть по порядку создания, а
            // не по хешу имени. `Step::Expect` здесь не годится — он смотрит в
            // буфер сразу, а строки приезжают по линии медленнее, чем ядро их
            // печатает.
            Step::Line("ls /data"),
            Step::Await("hello.txt", 30_000),
            Step::Await("dir/", 30_000),
            Step::Await("holes.bin", 30_000),
            Step::Await("many/", 30_000),

            // Встроенный экстент: содержимое файла лежит внутри узла дерева, и
            // отдельного блока данных у него нет вовсе.
            Step::Line("cat /data/hello.txt"),
            Step::Await("hello from btrfs", 30_000),

            // Две тысячи файлов — дерево высотой больше нуля. Имя ищется по
            // хешу, а не перебором: перебор на такой каталог не уложился бы ни
            // в один срок стенда, и это само по себе проверка.
            Step::Line("cat /data/many/f1999"),
            Step::Await("file 1999", 30_000),

            // Вложенность и файл, который больше предела встраивания, но меньше
            // сектора: экстент есть, и он длиннее самого файла.
            Step::Line("cat /data/dir/sub/small.txt"),
            Step::Await("small", 30_000),
            Step::Line("stat /data/mixed.bin"),
            Step::Await("file, 3000 bytes", 30_000),

            // Настоящее чтение данных с проверкой сумм — через программу, то
            // есть через системные вызовы, а не командой ядра. `wc` читает файл
            // кусками по 512 байт: каждый такой кусок заставляет ядро найти
            // экстент, прочитать сектор и сверить его crc32c.
            Step::Line("run /bin/wc /data/mixed.bin"),
            Step::Await("3000 bytes in /data/mixed.bin", 60_000),

            // Размер большого файла берётся из inode, без чтения: четыре
            // мегабайта, сверенные до байта, проверяются на хосте
            // (`cargo test -p btrfs`), где это стоит доли секунды, а не минут
            // эмулятора. Здесь доказывается, что ядро видит тот же файл.
            Step::Line("stat /data/dir/big.bin"),
            Step::Await("file, 4194304 bytes", 30_000),

            // Проверки тома здесь нет: с фазы 50d она идёт в `btrfs-write`,
            // после записи, и обходит тот же образец вместе с записанным.
            // Обход — единственный шаг, чья работа пропорциональна тому (две
            // минуты на aarch64), и платить за него дважды незачем.
            Step::Absent("KERNEL PANIC"),

            Step::Line("exit"),
            Step::Await("finishing the session", 15_000),
        ],
    },
    Scenario {
        name: "files",
        about: "Файловый менеджер по логике Windows: двойной щелчок, контекстное меню, переименование, удаление, свойства, плитка, «Этот компьютер».",
        target: Target::LiveAndBtrfs,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            // Том btrfs на `/data` — единственное место, куда живая система
            // умеет писать: переименование и удаление проверяются там.
            Step::Await("data        : mounted read-write", 60_000),
            Step::Await("freeos> ", 60_000),
            Step::Line("mkdir /data/c4"),
            Step::Await("created /data/c4", 30_000),
            Step::Line("run -b /bin/files /data/c4"),
            Step::Await("window      : 'Files' at", 30_000),
            Step::Await("files: /data/c4 has 0 entries", 15_000),
            Step::Wait(1_500),
            // Правая кнопка по пустому месту — меню каталога; первый пункт
            // создаёт папку с именем, как в Windows.
            Step::Aim(Aim::FilesBody),
            Step::RightClick,
            Step::Await("files: menu opened for the folder", 15_000),
            Step::Wait(800),
            Step::Shot("01-folder-menu"),
            Step::Key("ret"),
            Step::Await("files: created 'Новая папка' in /data/c4", 15_000),
            Step::Await("files: /data/c4 has 1 entries, selected 'Новая папка'", 15_000),
            // Клавиша меню — меню выбранной строки. Второй пункт —
            // переименовать: поле с прежним именем, Ctrl+U его чистит.
            // `M` — то же, что клавиша меню; сама клавиша проверяется ниже.
            Step::Key("m"),
            Step::Await("files: menu opened for 'Новая папка'", 15_000),
            Step::Wait(800),
            Step::Shot("02-entry-menu"),
            Step::Key("down"),
            Step::Key("ret"),
            Step::Wait(500),
            Step::Key("ctrl-u"),
            Step::Type("notes"),
            Step::Wait(500),
            Step::Shot("03-rename"),
            Step::Key("ret"),
            Step::Await("files: renamed 'Новая папка' to 'notes'", 15_000),
            Step::Await("files: /data/c4 has 1 entries, selected 'notes'", 15_000),
            // Удаление — третий пункт, с вопросом; `y` отвечает «да».
            Step::Key("m"),
            Step::Await("files: menu opened for 'notes'", 15_000),
            Step::Repeat("down", 2),
            Step::Key("ret"),
            Step::Wait(800),
            Step::Shot("04-confirm"),
            Step::Key("y"),
            Step::Await("files: deleted 'notes'", 15_000),
            Step::Await("files: /data/c4 has 0 entries", 15_000),
            // Вверх до корня и дальше — на страницу томов, как из `C:\` в
            // «Этот компьютер». Плиткой.
            Step::Key("t"),
            Step::Await("files: view tiles", 15_000),
            Step::Key("backspace"),
            Step::Await("files: entered '/data'", 15_000),
            Step::Wait(1_500),
            Step::Shot("05-tiles"),
            Step::Key("backspace"),
            Step::Await("files: entered '/'", 15_000),
            Step::Key("backspace"),
            Step::Await("files: this computer has 2 volume(s), selected '/'", 15_000),
            Step::Wait(1_500),
            Step::Shot("06-computer"),
            Step::Key("t"),
            Step::Await("files: view table", 15_000),
            Step::Wait(1_000),
            Step::Shot("07-computer-table"),
            // Enter на томе открывает его. Двойной щелчок по первой строке
            // корня входит в `/bin`: одиночный только выбирает.
            Step::Key("ret"),
            Step::Await("files: entered '/'", 15_000),
            Step::Aim(Aim::FilesRow(0)),
            Step::Click,
            Step::Wait(1_200),
            Step::Absent("files: entered '/bin'"),
            Step::Click,
            Step::Click,
            Step::Await("files: entered '/bin'", 15_000),
            // Свойства — пятый пункт меню записи. Клавиша меню у QEMU зовётся
            // `compose`: это она даёт `E0 5D` и usage 0x65; `menu` у него —
            // другая, мультимедийная.
            Step::Key("compose"),
            Step::Await("files: menu opened for 'ask'", 15_000),
            Step::Repeat("down", 4),
            Step::Key("ret"),
            Step::Await("files: properties of 'ask'", 15_000),
            Step::Wait(1_000),
            Step::Shot("08-properties"),
            Step::Key("esc"),
            Step::Key("ctrl-w"),
            Step::Await("files: closing on request", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "devices",
        about: "Диспетчер устройств: перепись PCI, USB и дисков из ядра, драйвер и состояние у каждого.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Та же причина, что у `taskmgr`: строка, набранная под шум трёх
            // падений `sshd`, теряется.
            Step::Await("init: 'sshd' failed 3 time(s) in a row, giving up", 60_000),
            Step::Wait(1_000),
            Step::Line("run -b /bin/devmgr"),
            Step::Await("window      : 'Device Manager' at", 30_000),
            // Перепись — строками журнала, по одной на устройство. Проверяется
            // то, что есть на обеих архитектурах: контроллер xHCI с клавиатурой
            // на нём и диск virtio-blk, с которого система запущена. Каждое —
            // вместе с драйвером и состоянием: «найдено» без «чем обслуживается»
            // — это не то, ради чего окно.
            Step::AwaitAny("usb controller (xhci): xhci active", 15_000),
            Step::AwaitAny(" keyboard: xhci active", 15_000),
            Step::AwaitAny("devmgr: disk virtio-blk #0 ", 15_000),
            Step::AwaitAny(" disk(s); ", 15_000),
            Step::Wait(2_000),
            Step::Shot("01-devices"),
            // Окно в фокусе после запуска: стрелка выбирает следующее устройство.
            Step::Key("down"),
            Step::Await("devmgr: selected ", 15_000),
            Step::Wait(1_500),
            Step::Shot("02-selected"),
            Step::Aim(Aim::Close("Device Manager")),
            Step::Click,
            Step::Await("devmgr: closing on request", 15_000),
        ],
    },
    Scenario {
        name: "taskmgr",
        about: "Диспетчер задач: список задач из ядра, снятие задачи, расположение файла, меню, вкладки, запуск из трея.",
        target: Target::Live,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("freeos> ", BOOT),
            // Без сети `sshd` три раза падает и печатает об этом, а строка,
            // набранная в серийную линию под этот шум, теряется (см. заметку
            // об UART). Дождаться, пока супервизор сдастся.
            Step::Await("init: 'sshd' failed 3 time(s) in a row, giving up", 60_000),
            Step::Wait(1_000),
            // Жертва: монитор системы — программа с окном, которую не жалко.
            Step::Line("run -b /bin/sysmon"),
            Step::Await("window      : 'System' at", 30_000),
            // Новое окно забирает фокус, и строка из серийной линии ушла бы в
            // него, а не в оболочку: Alt+Tab возвращает терминал.
            Step::Await("sysmon: theme", 15_000),
            Step::Key("alt-tab"),
            Step::Await("desktop     : focus 'Terminal'", 15_000),
            Step::Line("run -b /bin/taskmgr"),
            Step::Await("window      : 'Task Manager' at", 30_000),
            Step::Await("taskmgr: ", 15_000),
            Step::Await(" task(s): ", 15_000),
            Step::Wait(1_500),
            Step::Shot("01-processes"),
            // Программы стоят новыми сверху: первый — сам диспетчер, второй —
            // монитор. Delete снимает его; ядро говорит, что сняло, стол —
            // что окно ушло.
            Step::Key("down"),
            Step::Await("taskmgr: selected #", 15_000),
            Step::Expect("'sysmon'"),
            Step::Key("delete"),
            Step::Await("taskmgr: asked #", 15_000),
            // Стол убирает окно раньше, чем ядро дописывает строку о снятии.
            Step::Await("desktop     : closed the window of ", 15_000),
            Step::Await("/bin/sysmon: killed by request", 15_000),
            Step::Wait(2_500),
            Step::Shot("02-ended"),
            // Enter на первой строке — расположение файла самого диспетчера:
            // открываются «Файлы» в /bin.
            Step::Key("home"),
            Step::Key("ret"),
            Step::Await("taskmgr: opened location /bin for #", 15_000),
            Step::Await("files: /bin has 36 entries", 30_000),
            Step::Wait(1_500),
            Step::Shot("03-location"),
            Step::Aim(Aim::Close("Files")),
            Step::Click,
            Step::Await("files: closing on request", 15_000),
            Step::Await("desktop     : closed the window of ", 15_000),
            // Правая кнопка по строке — меню задачи.
            Step::Aim(Aim::Title("Task Manager")),
            Step::Click,
            Step::Await("desktop     : focus 'Task Manager'", 15_000),
            Step::Aim(Aim::TaskRow(0)),
            Step::RightClick,
            Step::Await("taskmgr: menu opened for #", 15_000),
            Step::Wait(800),
            Step::Shot("04-menu"),
            Step::Key("esc"),
            Step::Await("taskmgr: menu closed", 15_000),
            // Вкладки — цифрами: Tab на столе переключает окна.
            Step::Key("2"),
            Step::Await("taskmgr: tab performance", 15_000),
            Step::Wait(800),
            Step::Shot("05-performance"),
            Step::Key("3"),
            Step::Await("taskmgr: tab services", 15_000),
            Step::Await("taskmgr: services: 3 described, ", 15_000),
            Step::Wait(800),
            Step::Shot("06-services"),
            Step::Key("ctrl-w"),
            Step::Await("taskmgr: closing on request", 15_000),
            Step::Await("desktop     : closed the window of ", 15_000),
            // Из меню трея: пятый пункт, над ним две черты.
            Step::Aim(Aim::Tray),
            Step::RightClick,
            Step::Await("for tray", 15_000),
            Step::Aim(Aim::ContextItem(4, 2)),
            Step::Click,
            Step::Await("desktop     : started '/bin/taskmgr'", 15_000),
            Step::Await("window      : 'Task Manager' at", 30_000),
            Step::Key("ctrl-w"),
            Step::Await("taskmgr: closing on request", 15_000),
            Step::Absent("KERNEL PANIC"),
        ],
    },
    Scenario {
        name: "btrfs-write",
        about: "Ядро пишет в том btrfs, созданный mkfs.btrfs, а после выключения том сверяется с хоста.",
        target: Target::LiveAndBtrfs,
        usb_only: false,
        tablet: false,
        ohci: false,
        ehci: false,
        disk_bus: DiskBus::Virtio,
        network: false,
        e1000: false,
        guest_port: 0,
        host_echo: false,
        host_repo: false,
        arches: &[],
        reboots: false,
        updates: false,
        big_file: false,
        ssh_key: false,
        memory: "",
        extra: &[],
        steps: &[
            Step::Await("root        : no FreeOS root partition", BOOT),
            // Писатель открыл чужой том: отказ писателя назывался бы здесь же,
            // с причиной, и том остался бы на чтение.
            Step::Await("data        : btrfs at LBA", 60_000),
            Step::Await("data        : mounted read-write", 10_000),
            Step::Await("freeos> ", 60_000),

            // Новый каталог и встроенный файл: данные лежат в листе дерева.
            Step::Line("mkdir /data/notes"),
            Step::Await("created /data/notes", 30_000),
            Step::Line("echo written by the freeos kernel > /data/notes/first.txt"),
            Step::Await("wrote 29 bytes to /data/notes/first.txt", 30_000),
            Step::Line("cat /data/notes/first.txt"),
            Step::Await("written by the freeos kernel", 30_000),

            // Файл, который положил Linux: усечение и запись поверх — две
            // транзакции подряд над чужим элементом.
            Step::Line("echo replaced by freeos > /data/hello.txt"),
            Step::Await("wrote 19 bytes to /data/hello.txt", 30_000),
            Step::Line("cat /data/hello.txt"),
            Step::Await("replaced by freeos", 30_000),

            // Удаление в каталоге на две тысячи имён: дерево там в несколько
            // уровней, и перестраивается оно целиком. Соседнее имя обязано
            // остаться — иначе «удалилось» могло значить «удалилось всё».
            Step::Line("rm /data/many/f0000"),
            Step::Await("removed /data/many/f0000", 30_000),
            Step::Line("cat /data/many/f0000"),
            Step::Await("no such file or directory", 30_000),
            Step::Line("cat /data/many/f1999"),
            Step::Await("file 1999", 30_000),

            // Каталог: непустой не удаляется, опустевший — да.
            Step::Line("rm /data/dir/sub"),
            Step::Await("the directory is not empty", 30_000),
            Step::Line("rm /data/dir/sub/small.txt"),
            Step::Await("removed /data/dir/sub/small.txt", 30_000),
            Step::Line("rm /data/dir/sub"),
            Step::Await("removed /data/dir/sub", 30_000),

            // Копия из третьего кольца. `mc` пишет кусками по 512 байт, и файл
            // в 3000 байт проходит весь путь: четыре записи встроенным, на пятой
            // он перестаёт помещаться в лист и переезжает в экстент, шестая —
            // запись поверх сектора с его сверкой. Семь транзакций на копию.
            //
            // С фазы С9 панель сортирует как Far — папки сверху, внутри по
            // имени: «..», `dir`, `many`, `notes`, `hello.txt`, `holes.bin`,
            // `mixed.bin`. Шесть вниз. Прежние четыре попадали в `hello.txt`, и
            // сценарий падал на исправной системе.
            Step::Line("mc /data /data/notes"),
            Step::Await("mc: started", 30_000),
            Step::Raw(b"\x1b[B"),
            Step::Wait(500),
            Step::Raw(b"\x1b[B"),
            Step::Wait(500),
            Step::Raw(b"\x1b[B"),
            Step::Wait(500),
            Step::Raw(b"\x1b[B"),
            Step::Wait(500),
            Step::Raw(b"\x1b[B"),
            Step::Wait(500),
            Step::Raw(b"\x1b[B"),
            Step::Wait(500),
            // F5 сначала спрашивает — окном, где уже стоит путь другой панели,
            // — и Enter соглашается (как в сценарии `mc`).
            Step::Raw(b"\x1b[15~"),
            Step::Wait(1_500),
            Step::Raw(b"\n"),
            Step::Await("mc: copied /data/mixed.bin -> /data/notes/mixed.bin", 60_000),
            // Переименование файла Linux: запись каталога, а не данные.
            Step::Raw(b"\x1b[17~"),
            Step::Wait(500),
            Step::Raw(b"mixed-renamed.bin\n"),
            Step::Await("mc: renamed /data/mixed.bin -> /data/mixed-renamed.bin", 30_000),
            Step::Raw(b"\x1b[21~"),
            Step::Await("mc: done", 15_000),
            Step::Await("tty         : foreground released", 15_000),
            Step::Await("freeos> ", 15_000),

            // Копия читается программой, то есть сектор за сектором со сверкой
            // суммы, которую посчитал наш писатель.
            Step::Line("run /bin/wc /data/notes/mixed.bin"),
            Step::Await("3000 bytes in /data/notes/mixed.bin", 60_000),
            Step::Line("stat /data/mixed-renamed.bin"),
            Step::Await("file, 3000 bytes", 30_000),

            // Обход тома со сверкой каждого сектора — и чужого, и нашего. Числа
            // посчитаны: из 2010 inode образца ушли три (`f0000`, `small.txt`,
            // `sub`) и пришли три (`notes`, `first.txt`, копия); секторов стало
            // на один больше — копия, единственный новый файл вне листа.
            Step::Line("fsck"),
            Step::Await("/data: 2010 inode(s) and 1028 block(s) in use", 240_000),
            Step::Await("/data: the volume is consistent", 30_000),

            // Выключение, и только потом — взгляд с хоста.
            Step::Line("shutdown"),
            Step::Await("volume      : /data flushed and marked clean", 30_000),
            Step::Exits(30_000),
            Step::Absent("KERNEL PANIC"),
            // Тот же список сверяет Linux: `SCENARIO_ROUND` в `xtask/src/btrfsfix.rs`.
            Step::DataVolume(VolumeCheck {
                files: &[
                    ("/notes/first.txt", Content::Text("written by the freeos kernel\n")),
                    ("/hello.txt", Content::Text("replaced by freeos\n")),
                    ("/notes/mixed.bin", Content::Repeat(b'M', 3000)),
                    ("/mixed-renamed.bin", Content::Repeat(b'M', 3000)),
                    ("/many/f1999", Content::Text("file 1999\n")),
                ],
                gone: &["/mixed.bin", "/many/f0000", "/dir/sub"],
            }),
        ],
    },
];
