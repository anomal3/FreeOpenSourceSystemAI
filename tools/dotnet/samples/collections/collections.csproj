<Project Sdk="Microsoft.NET.Sdk">

  <!-- Образец для фазы N4b: List, Dictionary, HashSet, Queue, Stack,
       foreach по массивам и коллекциям, итераторы yield return, равенство
       ключей через IEquatable. Вывод сверяется с настоящим dotnet побайтно. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
