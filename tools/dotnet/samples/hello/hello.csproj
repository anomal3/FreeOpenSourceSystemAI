<Project Sdk="Microsoft.NET.Sdk">

  <!-- Шаблон `dotnet new console` без правок: первая программа, которую своя
       среда .NET обязана запустить (фаза N2). Из неё же собрана фикстура
       crates/clr-meta/fixtures/hello.dll. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
